# G20 风险清单

## 1. 范围
用于记录重构收官发布前后风险、等级、缓解动作和责任人。

## 2. 风险矩阵
| 风险ID | 风险描述 | 影响 | 概率 | 等级 | 缓解措施 | 责任人 | 状态 |
|---|---|---|---|---|---|---|---|
| R-001 | 中断恢复路径在高并发下触发重复调用 | 高 | 中 | 高 | 强制校验 `invocation_dedup`、执行并发恢复回归用例 | 待填 | open |
| R-002 | Tool Hook 插件异常导致调用链不稳定 | 中 | 中 | 中 | 非严格模式下容错降级，严格模式发布前压测 | 待填 | open |
| R-003 | 术语语义约束误判导致错误工具选择 | 中 | 中 | 中 | 增加 `semantic_type` 典型样例回归集合 | 待填 | open |
| R-004 | 发布后文档与实际行为不一致 | 中 | 低 | 低 | 发版前按 `发布清单.md` 执行文档校验 | 待填 | open |

## 3. 发布阻断规则
1. 任一高等级风险状态为 `open` 且无临时豁免说明时，阻断发布。
2. 任一必测回归失败，阻断发布。
3. 回滚预案未演练或演练失败，阻断发布。

