# 歧义处理指南

## 核心原则

1. **直接映射**：信息明确时直接执行
2. **自动消歧**：语义相似度匹配，选择最可能的字段，无感自动消歧
2. **优雅降级**：字段可能有多种理解时，返回所有含该词的字段，让用户选，精确追问，不猜

## 歧义类型与处理策略

### 类型1：模糊表述

**场景**：用户表述不完整或有歧义。

**示例**："请查询商机名称、签约金额、商机贡献度，仅查询2条数据"
- 处理：
  - "商机贡献度"可能不是标准字段，需确认