# datacloud-analysis 代码质量审计清单（2026-04-15，状态更新：2026-04-16）

## 0. 状态总览
- 已完成：P0-1、P0-2、P0-3、P1-2、P1-3、P1-4、废弃项中的 `.py.disabled`、`bootstrap.py:get_pg_pool()`。
- 部分完成：P1-1、P2-2、P2-5、P2-6、废弃项中的 `_shared_workspace_dir`。
- 未完成：P2-1、P2-3、P2-4，以及其余遗留项。

### 0.1 任务标记（截至 2026-04-16）
**已完成**
- [x] P0-1：`workspace.paths` 模块缺失
- [x] P0-2：`knowledge_enhancer` 接口签名不一致
- [x] P0-3：`getFileByPage` 失败响应协议错误
- [x] P1-2：打包脚本入口无效
- [x] P1-3：内置技能未加载
- [x] P1-4：`by_framework` 兼容问题

**部分完成**
- [ ] P1-1：Windows 路径兼容（仅完成关键点修复）
- [ ] P2-2：Lint 债务（持续收敛中）
- [ ] P2-5：导出工具 TODO/占位实现清理（已完成部分）
- [ ] P2-6：执行工具安全边界治理（已完成部分）
- [ ] 废弃项：`_shared_workspace_dir`（类型修复已完成，架构清理未完成）

**未完成**
- [ ] P2-1：mypy 严格类型全面收敛
- [ ] P2-3：配置边界统一治理（`os.getenv` 分散）
- [ ] P2-4：状态契约类型统一

## 1. 审计范围
- 目录：`packages/datacloud-analysis/src/datacloud_analysis`、`packages/datacloud-analysis/tests/dca`
- Python：3.13（当前环境）
- 工具链：`ruff`、`mypy`、`pytest`

## 2. 执行证据（可复现）
### 2.1 初始基线（2026-04-15）
- `uv run ruff check .`
  - 结果：`188 errors`（其中 `src` 目录 `80 errors`）
- `uv run python -m mypy .`
  - 结果：`279 errors in 44 files`
- `uv run python -m pytest -q`
  - 结果：测试收集阶段失败（`ModuleNotFoundError: datacloud_analysis.workspace.paths`）

### 2.2 最新复测（2026-04-16）
- `uv run ruff check .`
  - 结果：`115 errors`
- `uv run python -m mypy .`
  - 结果：`224 errors in 26 files`
- `uv run python -m pytest -q tests/dca/unit/test_get_file_by_page_command.py`
  - 结果：`6 passed`
- `uv run python -m pytest -q tests/dca/unit/test_worker_resume_regressions.py tests/dca/unit/test_worker_skill_capabilities.py`
  - 结果：`8 passed`
- `uv run python -m pytest -q tests/dca/unit/test_refactored_nodes.py::TestReactLoop::test_stop_on_finish_tool tests/dca/unit/test_get_file_by_page_command.py::test_shared_workspace_dir_coerces_purepath_to_path`
  - 结果：`2 passed`
- `uv run python -m pytest -q tests/dca/unit/test_config_new_settings.py::test_settings_propagates_unexpected_llm_loader_errors`
  - 结果：先红后绿，最终 `1 passed`
- `uv run python -m pytest -q tests/dca/unit/test_llm_checkpoint.py`
  - 结果：`17 passed`
- `uv run python -m pytest -q tests/dca/unit/test_memory_tools.py`
  - 结果：先红后绿，最终 `2 passed`
- `uv run python -m pytest -q tests/dca/unit/test_sandbox_tools.py`
  - 结果：先红后绿，最终 `2 passed`
- `uv run python -m pytest -q tests/dca/unit/test_bootstrap_pool.py`
  - 结果：先红后绿，最终 `2 passed`
- `uv run python -m pytest -q tests/dca/unit/test_new_tools.py::TestCodeExecTools`
  - 结果：`5 passed`
- `uv run python -m pytest -q tests/dca/unit/test_skills_loader.py`
  - 结果：删除内置技能关联用例后 `4 passed`

---

## 3. 高优先级缺陷（P0/P1）

### P0-1：`workspace.paths` 模块缺失，导致测试与接口断裂（状态：✅ 已完成）
- 完成说明：已恢复 `src/datacloud_analysis/workspace/paths.py` 并在 `workspace/__init__.py` 导出。
- 结果：不再出现 `ModuleNotFoundError: datacloud_analysis.workspace.paths` 收集级阻断。

### P0-2：`knowledge_enhancer` 接口签名不一致，功能实际失效（状态：✅ 已完成）
- 完成说明：已统一为兼容调用（1/2/3 参数），并将类型声明收敛为 `Callable[..., Any]`。
- 关键位置：`agent.py`、`orchestration/graph_builder.py`、`orchestration/intend/node.py`。

### P0-3：`getFileByPage` 失败响应协议结构错误（状态：✅ 已完成）
- 完成说明：错误响应已与成功响应统一 envelope（`code/message/data`）。
- 验证：`test_get_file_by_page_error_payload_uses_standard_envelope` 通过。

### P1-1：Windows 下 POSIX 路径处理会触发 `AttributeError`（状态：🟡 部分完成）
- 完成说明：关键调用点已补齐 `PurePath -> Path` 兜底，`_shared_workspace_dir` 也已做 `Path(str(...))` 强制转换。
- 新增验证：`test_shared_workspace_dir_coerces_purepath_to_path`（红->绿）。
- 未完成点：路径兼容性尚未完成全仓统一收敛，仍建议继续做全链路梳理。

### P1-2：打包脚本入口无效（安装后命令不可用）（状态：✅ 已完成）
- 完成说明：已补齐 `src/datacloud_analysis/cli.py`，入口可导入。

### P1-3：内置技能未被加载（默认能力空）（状态：✅ 已完成）
- 完成说明：原缺陷已修复；后续根据需求已移除默认技能文件 `group_agg/time_series` 及其 `.py.disabled`，并同步删除关联测试断言。

### P1-4：与 `by_framework` 新抽象接口不兼容（跨包集成回归）（状态：✅ 已完成）
- 完成说明：`DataCloudWorker` 已补齐 `get_agent_types`，并恢复 `_load_skill_capabilities`；`sub_step` 调用已增加兼容保护。
- 验证：`test_worker_resume_regressions.py`、`test_worker_skill_capabilities.py` 通过。

---

## 4. 中优先级问题（P2）

### P2-1：严格类型策略名义开启，但当前无法落地（状态：❌ 未完成）
- 当前：`mypy` 仍有 `224` 错误（较基线持续下降，但未收敛）。
- 本轮进展：修复 `session/checkpointer.py` 泛型标注，`mypy` 统计从 `228` 进一步下降。

### P2-2：Lint 债务高，基础工程卫生不达标（状态：🟡 部分完成）
- 当前：`ruff` 从 `188` 降至 `115`。
- 本轮已收敛：`tools/sandbox.py`、`tools/code_exec.py` 与部分测试文件的 lint 问题。

### P2-3：配置边界被破坏（与 env 设计目标冲突）（状态：❌ 未完成）
- 当前：`os.getenv` 分散读取问题仍在多个模块存在。
- 本轮进展：`Settings` 已修复“吞掉非预期异常”的问题，新增回归测试覆盖。

### P2-4：状态契约类型前后不一致（状态：❌ 未完成）
- 当前：`knowledge_snippets` 的声明与实际写入/消费类型仍未统一。

### P2-5：导出工具存在未实现/占位逻辑（状态：🟡 部分完成）
- 本轮进展：`tools/sandbox.py` 的 `sbx_read_file/sbx_write_file` 已从 `NotImplemented` 落地为可用实现（含路径越界校验与回归测试）。
- 未完成点：`tools/report.py` 仍有上传 attachment 的 TODO，占位逻辑未接入真实调度端。

### P2-6：执行工具安全边界薄弱（状态：🟡 部分完成）
- 本轮进展：`sbx_run_code` 增加受限导入白名单与更严格路径边界；新增回归测试覆盖关键路径。
- 未完成点：仍为进程内 `exec()`，尚未接入真正沙箱隔离（容器/远程执行后端）。

---

## 5. 废弃/疑似遗留代码
- `src/datacloud_analysis/skills/builtin/*.py.disabled`（状态：✅ 已完成）
  - 说明：已按需求删除 `.py.disabled` 与对应默认技能文件。
- `src/datacloud_analysis/bootstrap.py:get_pg_pool()`（状态：✅ 已完成）
  - 说明：已实现惰性初始化与缓存复用，`teardown()` 支持池关闭；回归测试通过。
- `src/datacloud_analysis/command_plugins/get_file_by_page_command.py:_shared_workspace_dir()`（状态：🟡 部分完成）
  - 说明：类型兼容已修复并补测试，但是否需要保留该函数仍待架构清理决策。

---

## 6. 后续建议修复顺序（最小化回归）
1. 先完成 P2-1（mypy）主干收敛，优先处理 `orchestration/execution`、`respond/formatter`、`session/*`。
2. 同步推进 P2-2（ruff）自动可修项，再处理剩余人工项。
3. 再处理 P2-3/P2-4 的架构一致性（配置入口统一、状态契约统一）。
4. 最后处理 P2-5/P2-6 剩余事项与遗留函数的架构收敛（重点是 report 上传链路与真正沙箱后端）。
