# datacloud-analysis 完整重构实施计划

**制定日期**: 2026-04-05  
**重构范围**: 《本体推理引擎重构方案.md》第2章和第3.1章  
**目标版本**: datacloud-analysis v2.0 (Deep Agents Architecture)  
**重构策略**: 彻底重构，旧代码已备份至 `D:\data\code\baiying\whale_datacloud_dev\whale_datacloud`

---

## 1. 重构目标

### 1.1 核心目标

**完全按照设计文档实现**:
1. ✅ OQL协议完整实现（JSON Schema + 工具）
2. ✅ Deep Agents架构完整迁移
3. ✅ 所有中间件实现
4. ✅ 删除旧的orchestration目录
5. ✅ 解决P1-P6所有痛点

### 1.2 设计原则

1. **以 `create_deep_agent()` 为核心** - 完全替换自研StateGraph
2. **删多于增** - `orchestration/` 目录整体删除
3. **保留Gateway适配层** - worker.py的LRU缓存、SSE流式、Resume机制
4. **6001协议兼容** - 维持现有gateway输出格式
5. **checkpointing兼容** - 沿用OpenGauss作为checkpoint后端

---

## 2. 完整实施计划

### 阶段1: OQL协议完整实现 (第1次会话, 4-6小时)

#### 任务清单

**A. JSON Schema定义**
- [ ] 创建 `docs/OQL.schema.json`
  - [ ] QueryObjects工具完整Schema
  - [ ] ExecuteAction工具完整Schema
  - [ ] Pipeline格式Schema
  - [ ] 类型系统定义
  - [ ] 操作符枚举
  - [ ] 聚合函数枚举
  - [ ] 所有示例验证

**B. OQL工具实现**
- [ ] 创建 `src/datacloud_analysis/tools/oql/`目录
- [ ] 实现 `tools/oql/query_objects.py`
  - [ ] 参数验证（基于JSON Schema）
  - [ ] 调用datacloud-data的OqlRouter
  - [ ] 响应格式转换
  - [ ] 错误处理
- [ ] 实现 `tools/oql/execute_action.py`
  - [ ] 参数验证
  - [ ] 调用datacloud-data的Action Service
  - [ ] 响应格式转换
  - [ ] 错误处理
- [ ] 实现 `tools/oql/__init__.py` - 统一导出

**C. 测试**
- [ ] 单元测试 `tests/tools/oql/test_query_objects.py`
- [ ] 单元测试 `tests/tools/oql/test_execute_action.py`
- [ ] Mock datacloud-data响应
- [ ] 测试覆盖率>90%

**D. 文档**
- [ ] 创建 `docs/OQL工具使用指南.md`
- [ ] 包含完整示例

**验收标准**:
- ✅ JSON Schema完整且可验证
- ✅ 工具能正确调用datacloud-data
- ✅ 所有测试通过
- ✅ 文档完整

---

### 阶段2: Deep Agents架构迁移 (第2次会话, 6-8小时)

#### 任务清单

**A. 创建新的agent核心**
- [ ] 重写 `src/datacloud_analysis/agent.py`
  - [ ] 使用 `create_deep_agent()` 替换 `build_analysis_graph()`
  - [ ] 配置中间件栈
  - [ ] 集成checkpointer
- [ ] 删除 `src/datacloud_analysis/orchestration/` 整个目录
  - [ ] 删除 `graph_builder.py`
  - [ ] 删除 `intend/`
  - [ ] 删除 `execution/`
  - [ ] 删除 `respond/`
  - [ ] 删除 `state.py`

**B. 工具注册**
- [ ] 创建 `src/datacloud_analysis/tools/registry.py`
- [ ] 注册OQL工具（QueryObjects, ExecuteAction）
- [ ] 保留现有工具（ask_user, code_exec, file_io）
- [ ] 移除knowledge工具（改用中间件）

**C. 基础测试**
- [ ] 测试agent创建
- [ ] 测试工具注册
- [ ] 测试基本对话流程

**验收标准**:
- ✅ agent使用create_deep_agent()创建
- ✅ orchestration目录已删除
- ✅ 工具正确注册
- ✅ 基本流程可运行

---

### 阶段3: 自定义中间件实现 (第3次会话, 6-8小时)

#### 任务清单

**A. KnowledgeInjectionMiddleware**
- [ ] 创建 `src/datacloud_analysis/middlewares/knowledge_injection.py`
- [ ] 实现消息预处理
- [ ] 调用datacloud-knowledge检索Schema
- [ ] 注入到system prompt
- [ ] 单元测试

**B. DatacloudOutputMiddleware**
- [ ] 创建 `src/datacloud_analysis/middlewares/datacloud_output.py`
- [ ] 实现emit_result工具
- [ ] 转换为6001协议格式
- [ ] 集成到gateway
- [ ] 单元测试

**C. 中间件注册**
- [ ] 创建 `src/datacloud_analysis/middlewares/__init__.py`
- [ ] 在agent.py中配置中间件栈顺序

**验收标准**:
- ✅ KnowledgeInjectionMiddleware自动注入Schema
- ✅ DatacloudOutputMiddleware正确输出6001格式
- ✅ 所有测试通过

---

### 阶段4: 命令插件适配 (第4次会话, 4-6小时)

#### 任务清单

**A. CommandPluginManager重构**
- [ ] 重写 `src/datacloud_analysis/command_plugins/manager.py`
- [ ] 适配Deep Agents架构
- [ ] 保留现有命令插件功能

**B. 现有插件迁移**
- [ ] 迁移 `ext_command_dispatcher.py`
- [ ] 迁移 `get_file_by_page_command.py`
- [ ] 迁移 `update_terms_name_command.py`

**C. 测试**
- [ ] 测试命令插件调用
- [ ] 测试命令路由

**验收标准**:
- ✅ 命令插件系统正常工作
- ✅ 所有现有命令可用

---

### 阶段5: Gateway集成与端到端测试 (第5次会话, 6-8小时)

#### 任务清单

**A. Gateway适配**
- [ ] 检查 `worker.py` 是否需要调整
- [ ] 确保LRU缓存正常工作
- [ ] 确保SSE流式输出正常
- [ ] 确保Resume机制正常

**B. 端到端测试**
- [ ] 创建 `tests/e2e/test_oql_workflow.py`
- [ ] 测试完整的查询流程
- [ ] 测试完整的动作执行流程
- [ ] 测试Pipeline流程
- [ ] 测试中断恢复

**C. 性能测试**
- [ ] 对比新旧架构性能
- [ ] 确保性能不降低

**验收标准**:
- ✅ 所有端到端测试通过
- ✅ 6001协议输出正确
- ✅ 性能达标

---

### 阶段6: 文档与清理 (第6次会话, 2-4小时)

#### 任务清单

**A. 文档更新**
- [ ] 更新 `README.md`
- [ ] 创建 `docs/架构说明.md`
- [ ] 创建 `docs/中间件开发指南.md`
- [ ] 创建 `docs/迁移指南.md`（从v1到v2）

**B. 代码清理**
- [ ] 删除未使用的导入
- [ ] 删除未使用的文件
- [ ] 代码格式化（ruff）
- [ ] 类型检查（mypy）

**C. 最终验证**
- [ ] 运行所有测试
- [ ] 检查测试覆盖率（>90%）
- [ ] 代码质量检查

**验收标准**:
- ✅ 文档完整
- ✅ 代码质量达标
- ✅ 所有测试通过

---

## 3. 详细任务分解

### 3.1 阶段1详细任务（本次会话）

#### 任务1.1: 创建OQL JSON Schema (1.5小时)

**文件**: `docs/OQL.schema.json`

**内容结构**:
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OQL Protocol Schema",
  "definitions": {
    "QueryObjects": { ... },
    "ExecuteAction": { ... },
    "Pipeline": { ... },
    "Condition": { ... },
    "Metric": { ... },
    "GroupByClause": { ... },
    "OrderByClause": { ... },
    "LinkClause": { ... }
  }
}
```

**参考**:
- 设计文档 §2.2.5 QueryObjects
- 设计文档 §2.2.6 ExecuteAction
- 设计文档 §2.2.7 Pipeline

#### 任务1.2: 实现QueryObjects工具 (1.5小时)

**文件**: `src/datacloud_analysis/tools/oql/query_objects.py`

**核心逻辑**:
```python
from langchain_core.tools import tool
from datacloud_data_sdk.oql import OqlRouter, format_oql_response

@tool
def query_objects(
    object_type: str,
    select: list[str] | None = None,
    where: list[dict] | None = None,
    include_links: list[dict] | None = None,
    metrics: list[dict] | None = None,
    group_by: list[dict] | None = None,
    having: list[dict] | None = None,
    order_by: list[dict] | None = None,
    limit: int = 100,
    offset: int = 0,
) -> dict:
    """查询本体对象或视图的实例列表、详情、聚合统计。"""
    # 1. 构建OQL参数
    # 2. 调用OqlRouter.route()
    # 3. 使用format_oql_response转换格式
    # 4. 返回标准响应
```

#### 任务1.3: 实现ExecuteAction工具 (1小时)

**文件**: `src/datacloud_analysis/tools/oql/execute_action.py`

**核心逻辑**:
```python
@tool
def execute_action(
    action_type: str,
    target_objects: list[str] | None = None,
    payload: dict | None = None,
) -> dict:
    """执行有副作用的业务动作。"""
    # 1. 构建Action参数
    # 2. 调用Action Service
    # 3. 返回执行结果
```

#### 任务1.4: 编写测试 (1小时)

**文件**: 
- `tests/tools/oql/test_query_objects.py`
- `tests/tools/oql/test_execute_action.py`

**测试用例**:
- 参数验证
- Mock OqlRouter响应
- 响应格式验证
- 错误处理

---

## 4. 时间估算

| 阶段 | 任务 | 预计时间 | 会话数 |
|------|------|---------|--------|
| 阶段1 | OQL协议实现 | 4-6小时 | 1 |
| 阶段2 | Deep Agents迁移 | 6-8小时 | 1 |
| 阶段3 | 中间件实现 | 6-8小时 | 1 |
| 阶段4 | 命令插件适配 | 4-6小时 | 1 |
| 阶段5 | 端到端测试 | 6-8小时 | 1 |
| 阶段6 | 文档与清理 | 2-4小时 | 1 |
| **总计** | | **28-40小时** | **6次会话** |

---

## 5. 风险管理

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| Deep Agents API变更 | 高 | 已锁定版本deepagents>=0.4.7 |
| 6001协议兼容性 | 高 | 保留DatacloudOutputMiddleware专门处理 |
| 性能下降 | 中 | 性能测试对比，必要时优化 |
| 测试覆盖不足 | 中 | 每个阶段都有测试要求 |

---

## 6. 本次会话计划

**目标**: 完成阶段1 - OQL协议完整实现

**任务清单**:
1. ✅ 创建 `docs/OQL.schema.json`
2. ✅ 实现 `tools/oql/query_objects.py`
3. ✅ 实现 `tools/oql/execute_action.py`
4. ✅ 编写单元测试
5. ✅ 编写使用文档

**预期产出**:
- 完整的JSON Schema定义
- 可用的OQL工具实现
- 通过测试的代码
- 使用文档

**下一步**: 用户确认后立即开始实施
