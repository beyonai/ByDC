# 渐进式数据检索架构提升方案

> 版本：v1.0 | 日期：2026-04-18

---

---

## 1. 现状梳理

### 1.1 整体调用链路

> **现状说明**：以下描述改造前的调用链路。当前系统通过 `DATACLOUD_ONTOLOGY_LOAD_MODE` 环境变量在两种模式之间二选一，两种模式能力不互补。新方案统一以 `query_*` / `compute_*` 为入口，通过 `before_callback` 内部路由，不再依赖模式切换（详见第 2 章）。

```
用户问题
  ↓
DataCloudWorker._handle_message()          [byclaw-data/worker.py]
  ↓
create_agent() → execution_node()          [datacloud-analysis/agent.py]
  ↓  (ReAct 循环，LLM 选工具)
query_{code} / compute_{code}              [DATACLOUD_ONTOLOGY_LOAD_MODE=ontology_query]
   或 data_query_{code}                    [DATACLOUD_ONTOLOGY_LOAD_MODE=db_query]
  ↓
QueryExecutor / ComputeExecutor            [datacloud-data-sdk]
   或 PlanAgent → QueryExecutionPlan       [datacloud-data-sdk]
```

### 1.2 工具挂载逻辑（init_agent_conf.py）

**ontology_query 模式**：注入 `query_{code}` / `compute_{code}` 虚拟动作工具。
- 工具通过 `inject_virtual_actions(loader)` → `ActionToolGenerator.generate_tools()` 挂载，每个 OWL 对象/视图各一对。
- 描述由 `virtual_action/generator.py` 的 `build_query_description()` / `build_compute_description()` **动态生成**，内容极为丰富：

  ```
  {对象描述}

  按条件查询{对象名}明细。**field 统一使用字段编码（field_code）**；支持字段过滤、排序、分页；不支持聚合统计。

  **何时使用**：查看具体记录列表时使用；不适用于统计汇总，如需统计请用 compute 动作。

  **可用字段**：
  | 字段编码 | 中文名 | 角色 | 类型 | 可过滤 | 可分组 | 可聚合 | 特殊说明 |
  | --- | --- | --- | --- | --- | --- | --- | --- |
  | field_a   | 字段A | dimension | name       | eq/in            | direct | -          | |
  | amount    | 金额  | measure   | raw_number | gt/gte/lt/lte/eq | -      | sum/avg    | |
  | period    | 账期  | dimension | period     | eq/between       | direct | -          | 必须过滤 |
  ...

  **常见错误**：
  - 使用了字段不支持的 op 操作符
  - field 填了字段中文名而非字段编码（field_code）
  ```

- `compute_{code}` 描述同样包含**维度+度量全部字段的能力矩阵**，并额外说明拍照指标限制、`metrics.agg` 键名规范等。
- 结论：**Agent 选工具阶段已能完整看到字段列表及其操作能力**，ontology_query 模式下字段感知不是问题。

**db_query 模式**：注入 `data_query_{code}` 工具。

- schema 为 `{query: str, contextKnowledge: str}`，接受自然语言直通。
- 工具描述由 `_build_single_db_query_tool()` 生成：`f"数据查询工具: {label}。{desc_suffix}"`，**仅含资源名称和简短说明，不含字段信息**。
- 字段信息依赖 `ObjectViewPayload` 序列化后注入给 `PlanAgent`，Agent 选工具阶段**无法感知字段细节**。

**两种模式的工具描述对比**：

| 维度 | ontology_query（query_* / compute_*） | db_query（data_query_*） |
|------|---------------------------------------|--------------------------|
| 工具描述 | 完整字段能力矩阵（Markdown 表格） | 仅含资源名+简短说明 |
| Agent 选工具时可见信息 | 字段编码、中文名、角色、过滤/分组/聚合操作符 | 对象名称 + 资源描述 |
| 字段映射时机 | 选工具阶段（工具描述即字段规格） | 执行阶段（PlanAgent 内部推理） |
| 选错对象的风险 | 低（字段差异在描述中可见） | 较高（只能靠对象名语义判断） |

### 1.3 query_{code} / compute_{code} 执行路径

```
query_{code}(query, ...)
  ↓
query_clarification_plugin.before_call_back()   [tool_hook_plugin]
  ↓
  ├── 剥除元字段（intent_reason / extraction_confidence / ambiguous_params）
  ├── ambiguous_params 为空 → return None（直接放行）
  └── ambiguous_params 非空 → analyze_query_clarification()
        ↓
        ├── needs_clarification=False → 注入 contextKnowledge，继续查询
        └── needs_clarification=True  → langgraph interrupt，等待用户选择
  ↓
QueryExecutor.execute() / ComputeExecutor.execute()
  → 生成单表 SQL → 执行 → 返回 records
```

**旧方案中断恢复防重触发机制**

旧方案 `interrupt` 后，`react_loop.py` 在 resume 时重放 `react_pending_tool_calls`，重放前**自动剥除三个元字段**：

```python
# react_loop.py — resume 重放时剥除元字段
_tc_args.pop("ambiguous_params", None)
_tc_args.pop("intent_reason", None)
_tc_args.pop("extraction_confidence", None)
```

`before_callback` 收到不含 `ambiguous_params` 的参数后，判断 `ambiguous_params` 为空 → 直接放行，不再触发 `analyze_query_clarification()`，从而避免了重复中断。

**新方案移除了 `ambiguous_params` 依赖**，改由 `before_callback` 主动检测歧义，因此需要新的防重触发机制——在 resume 重放前将用户选择结果写回 `tool_params`，使通道 1 直接命中 1:1 映射，跳过歧义流程（详见 2.2.3）。

---

**现状问题一：单一 analyze_query_clarification 开销高**

`analyze_query_clarification` 是一个重型全流程函数，包含三次 LLM 调用：

| 步骤 | 调用 | 耗时估算 |
|------|------|---------|
| Step 1 | `expand_query`（NL → NatQuery，LLM tool call）| ~1-2s |
| Step 2 | `build_paradigm_resolution_state`（术语召回，向量检索）| ~0.5-1s |
| Step 3 | `llm_confirm`（LLM 审查召回结果）| ~1-2s |

即使是无歧义的简单查询（如"查询本月订单数量"），也会跑完全部三步，**总耗时 2-5s**，用户体验差。

**现状问题二：无复杂度判断，无法路由**

`query_{code}` 和 `compute_{code}` 只能走 OQL 单表路径，遇到以下问题**完全无法处理**：
- 跨对象关联（如"亩产效益后30%地块上的企业"）
- 百分比截断（PERCENT_RANK / 窗口函数）
- 同比环比等时间计算
- 多步依赖（先求阈值，再过滤）

**现状问题三：模式二选一，能力不互补**

`DATACLOUD_ONTOLOGY_LOAD_MODE` 是二值环境变量，同一部署中只能选一种模式：
- `ontology_query`：精度高，但复杂查询无法表达
- `db_query`：全能，但 Schema Linking 依赖 LLM 推理，简单查询成本高

两种能力**无法在同一会话中互补**。

### 1.4 data_query_{code} 执行路径

```
data_query_{code}(query, contextKnowledge)
  ↓
view.query() / object.query()
  ↓
LangGraphPlanGenerator.generate()
  ↓
PlanAgent（LLM + SYSTEM_PROMPT）
  → 生成 QueryExecutionPlan JSON
    ├── steps: [{type: "SQL", sqlTemplate: "WITH ... SELECT ..."}]
    └── aggregation: {strategy: "DIRECT" | "SQLITE_MEM"}
  ↓
执行引擎（跑 SQL / 调 API / SQLITE_MEM 内存聚合）
```

**现状优点**：
- SYSTEM_PROMPT 已内置完整的跨对象/窗口函数/百分比截断规则（150行）
- 支持 CTE、PERCENT_RANK、CASE WHEN 等复杂 SQL 生成
- SQLITE_MEM 策略支持跨数据源查询

**现状不足**：
- SQL 执行错误（如列名歧义）仅靠静态 `PlanValidator` 检测，不反馈实际执行报错
- Schema 输入为 ObjectViewPayload JSON，无字段值样本（M-Schema 缺失），字段映射依赖 description 质量
- `data_query_*` 工具描述同样不含字段信息，LLM 选错视图时整个计划作废

### 1.5 analyze_query_clarification 内部原子能力

```
analyze_query_clarification()
  ├── expand_query()                     → NatQuery（结构化，含 select/where/group_by）
  ├── natquery_to_five_stage()           → 五段式 keys（轻量，纯逻辑）
  ├── build_paradigm_resolution_state()  → 术语召回（向量检索）
  └── llm_confirm()                      → ConfirmedQuery（LLM 判断是否歧义）
```

这四个原子方法**可以独立调用**，为渐进式判断提供基础。

### 1.6 工具入参 / 出参规格

> ⚠️ **基线说明**：本节记录**改造前**的入参规格，反映现状代码实现。新方案在此基础上新增 `query`（必填）和 `complex_conditions`（溢出区），并移除三个元字段（`intent_reason` / `extraction_confidence` / `ambiguous_params`），完整新参数协议详见 2.2.1。

#### 1.6.1 query_{code}（明细查询）

**入参**（全部可选，无必填）：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `select` | `array[string]` | — | 返回字段列表（field_code），为空时返回全部非关联字段 |
| `filters` | `array[FilterItem]` | — | 过滤条件列表，多条件默认 AND |
| `filter_relation` | `"AND"` \| `"OR"` | `"AND"` | 过滤条件连接方式；含账期强制约束时禁止 OR |
| `order_by` | `array[{field, direction}]` | — | 排序规则，`direction` 为 `"asc"` / `"desc"` |
| `limit` | `integer` [1, 1000] | `100` | 返回行数上限 |
| `offset` | `integer` ≥ 0 | `0` | 分页偏移量 |

`FilterItem` 结构：
```json
{"field": "field_code（字段编码）", "op": "eq|in|gt|gte|lt|lte|between|is_null|is_not_null", "value": "字面量或数组"}
```

**入参 JSON 示例**：

```json
// 示例：查询本月新增、营收大于100万、属于"制造业"或"农业"的企业，
//       返回企业名称和营收，按营收降序，取前20条
{
  "select": ["enterprise_name", "revenue", "created_date", "industry"],
  "filters": [
    {"field": "created_date", "op": "between", "value": ["2026-04-01", "2026-04-30"]},
    {"field": "revenue",      "op": "gt",      "value": 1000000},
    {"field": "industry",     "op": "in",      "value": ["manufacturing", "agriculture"]}
  ],
  "filter_relation": "AND",
  "order_by": [{"field": "revenue", "direction": "desc"}],
  "limit": 20,
  "offset": 0
}
```

**出参**（SDK 层，service 层再包装 `{code, message, data}`）：

```json
// 正常返回
{
  "result_type": "normal",
  "records": [{"field_code": "value", ...}, ...],
  "total": 123,
  "meta": {
    "objectId": "对象编码",
    "objectName": "对象名称",
    "columns": [{"name": "field_code", "label": "字段名", "type": "string|number|date"}, ...],
    "total": 123
  },
  "trace": {"request_id": "uuid", "question": "...", "object_id": "..."},

  // 数据量超过阈值（默认10条）时额外返回：
  "pagination": {"page": 1, "page_size": 5, "total": 123, "total_pages": 25, "has_next": true},
  "file": {"file_url": "/path/to/export.csv", "file_id": "uuid"},
  "overflow_notice": "数据量较大（共123条），此处仅返回前5条预览..."
}

// 无法回答（CannotAnswerError）
{"result_type": "rejected", "overflow_notice": "错误信息", "trace": {...}}

// 术语歧义（TermAmbiguousError）
{"result_type": "ask_user", "overflow_notice": "错误信息", "trace": {...}}
```

---

#### 1.6.2 compute_{code}（分组统计）

**入参**（`metrics` 必填，其余可选）：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `metrics` | `array[MetricItem]` | **是** | 统计指标，至少一项 |
| `dimensions` | `array[string \| DimensionItem]` | 否 | 分组维度；字符串简写等价于 `group_op:"self"` |
| `filters` | `array[FilterItem]` | 否 | 行级过滤（WHERE），同 `query_{code}` 的 filters |
| `having` | `array[HavingItem]` | 否 | 聚合后过滤（HAVING），`field` 必须是 `metrics.as` 别名 |
| `order_by` | `array[{field, direction}]` | 否 | `field` 可为 `metrics.as` 别名或维度 field_code |
| `limit` | `integer` [1, 1000] | 否 | 默认 100 |
| `filter_relation` | `"AND"` \| `"OR"` | 否 | 默认 `"AND"` |

---

`MetricItem` 结构（五种形态，`field` 与 `expr` 二选一，`filters` 可选叠加）：

```json
// ① 行数统计（无需 field / expr）
//    → COUNT(*)
{"agg": "count_all", "as": "enterprise_count"}

// ② 普通字段聚合
//    → SUM(revenue)
{"field": "revenue", "agg": "sum", "as": "total_revenue"}

// ③ 条件字段聚合（指标级 filters → CASE WHEN）
//    → SUM(CASE WHEN region = 'north' THEN revenue ELSE 0 END)
{
  "field": "revenue",
  "filters": [{"field": "region", "op": "eq", "value": "north"}],
  "agg": "sum",
  "as": "north_revenue"
}

// ④ 公式聚合（字段本身是表达式）
//    → AVG(revenue - cost)
{"expr": "revenue - cost", "agg": "avg", "as": "avg_profit"}

// ⑤ 条件公式聚合（expr + filters 叠加）
//    → SUM(CASE WHEN enterprise_level = 'leading' THEN (revenue - cost) ELSE 0 END)
{
  "expr": "revenue - cost",
  "filters": [{"field": "enterprise_level", "op": "eq", "value": "leading"}],
  "agg": "sum",
  "as": "leading_profit"
}
```

---

`DimensionItem` 结构（支持字符串简写）：

```json
// 字符串简写：直接分组，等价于 group_op:"self"
//    → SELECT industry  /  GROUP BY industry
"industry"

// 对象：时间粒度分组
//    → SELECT DATE_FORMAT(created_date,'%Y-%m')  /  GROUP BY DATE_FORMAT(...)
{"field": "created_date", "group_op": "year|month|quarter|day"}

// 对象：range 分桶（需带 buckets）
//    → SELECT CASE WHEN revenue<500000 THEN '低' ... END  /  GROUP BY CASE WHEN ...
{"field": "revenue", "group_op": "range", "buckets": [{"from": null, "to": 500000, "label": "低"}, ...]}
```

> `dimensions` 同时决定 SQL 的 `SELECT` 维度列和 `GROUP BY` 子句，两者表达式完全一致，无需单独维护 `GROUP BY`。

---

**入参 JSON 示例**：

```json
// 示例1：普通字段聚合 + 行数统计
//        按行业、地区统计企业数和总营收，过滤营收>0，按总营收降序取前50
//        → SELECT industry, region, COUNT(*), SUM(revenue) FROM t
//             WHERE revenue > 0 GROUP BY industry, region ORDER BY SUM(revenue) DESC LIMIT 50
{
  "dimensions": ["industry", "region"],
  "metrics": [
    {"agg": "count_all", "as": "enterprise_count"},
    {"field": "revenue", "agg": "sum", "as": "total_revenue"}
  ],
  "filters": [
    {"field": "revenue", "op": "gt", "value": 0}
  ],
  "order_by": [{"field": "total_revenue", "direction": "desc"}],
  "limit": 50
}

// 示例2：条件字段聚合（CASE WHEN）
//        按行业统计总营收，同时单独统计龙头企业营收占比
//        → SUM(revenue) + SUM(CASE WHEN level='leading' THEN revenue ELSE 0 END)
{
  "dimensions": ["industry"],
  "metrics": [
    {"agg": "count_all", "as": "enterprise_count"},
    {"field": "revenue", "agg": "sum", "as": "total_revenue"},
    {
      "field": "revenue",
      "filters": [{"field": "enterprise_level", "op": "eq", "value": "leading"}],
      "agg": "sum",
      "as": "leading_revenue"
    }
  ],
  "order_by": [{"field": "total_revenue", "direction": "desc"}],
  "limit": 50
}

// 示例3：公式聚合 + 条件公式聚合（expr）
//        按行业统计平均利润，以及龙头/骨干企业的总利润
//        → AVG(revenue-cost) + SUM(CASE WHEN level IN(...) THEN (revenue-cost) ELSE 0 END)
{
  "dimensions": ["industry", "region"],
  "metrics": [
    {"agg": "count_all", "as": "enterprise_count"},
    {"field": "revenue", "agg": "sum", "as": "total_revenue"},
    {"expr": "revenue - cost", "agg": "avg", "as": "avg_profit"},
    {
      "expr": "revenue - cost",
      "filters": [{"field": "enterprise_level", "op": "in", "value": ["leading", "backbone"]}],
      "agg": "sum",
      "as": "key_enterprise_profit"
    }
  ],
  "filters": [
    {"field": "created_date", "op": "between", "value": ["2026-01-01", "2026-03-31"]}
  ],
  "having": [
    {"field": "total_revenue", "op": "gt", "value": 1000000}
  ],
  "order_by": [{"field": "avg_profit", "direction": "desc"}],
  "limit": 50
}

// 示例4：时间分组 + 营收分桶 + having
//        按月份 + 营收区间统计企业数和各桶平均利润，过滤不足5家的分组
//        → GROUP BY DATE_FORMAT(created_date,'%Y-%m'), CASE WHEN revenue<500000...
{
  "dimensions": [
    {"field": "created_date", "group_op": "month"},
    {
      "field": "revenue",
      "group_op": "range",
      "buckets": [
        {"from": null,    "to": 500000,  "label": "低收入"},
        {"from": 500000,  "to": 2000000, "label": "中收入"},
        {"from": 2000000, "to": null,    "label": "高收入"}
      ]
    }
  ],
  "metrics": [
    {"agg": "count_all", "as": "enterprise_count"},
    {"field": "revenue", "agg": "avg", "as": "avg_revenue"},
    {"expr": "revenue - cost", "agg": "avg", "as": "avg_profit"}
  ],
  "having": [
    {"field": "enterprise_count", "op": "gte", "value": 5}
  ],
  "order_by": [{"field": "avg_profit", "direction": "desc"}],
  "limit": 100
}
```

**出参**：与 `query_{code}` 结构相同，`records` 中字段名为维度的 field_code 和 metrics 的 `as` 别名。

---

#### 1.6.3 data_query_{code}（自然语言查询）

**入参**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `query` | `string` | **是** | 自然语言查询问题（工具调用时由 Agent 填写） |
| `contextKnowledge` | `string` | 否 | 知识增强上下文，由 `before_callback` 在执行前注入，Agent 不需手动填写 |

**出参**：与 `query_{code}` 结构相同，当 `include_plan=True` 时额外返回 `plan`：

```json
{
  "result_type": "normal",
  "records": [...],
  "total": 10,
  "meta": {"objectId": "...", "columns": [...], "total": 10},
  "trace": {...},

  // include_plan=True 时额外返回：
  "plan": {
    "question": "原始查询问题",
    "can_answer": true,
    "steps": [
      {"type": "SQL", "datasource_alias": "...", "sqlTemplate": "WITH ... SELECT ...", "output_ref": "step1"}
    ],
    "aggregation": {"strategy": "DIRECT|SQLITE_MEM", "final_step_id": "step1"}
  }
}
```

---

### 1.7 现状核心问题汇总

| # | 问题 | 影响 |
|---|------|------|
| P1 | `data_query_*` 工具描述不含字段信息，Agent 选工具时无法感知字段细节（ontology_query 模式已有完整字段描述，此问题仅存在于 db_query 模式） | db_query 模式下选错对象/视图 |
| P2 | 所有查询都跑全量澄清流程，简单查询浪费 2-5s | 响应慢，用户体验差 |
| P3 | 无复杂度分类，复杂查询在 OQL 模式下静默失败 | 功能缺口 |
| P4 | 二值模式切换，简单精确 vs 复杂全能不能共存 | 部署灵活性差 |
| P5 | SQL 执行错误不回流重试 | data_query 失败率偏高 |

---



## 2. 概要设计

### 2.1 设计原则

**工具即对象/视图**：系统中的每个 OWL 对象或预定义视图，对应一对工具 `query_{code}` / `compute_{code}`，其中 `{code}` 是该对象或视图在本体中的**唯一编码**。Agent 选工具即选定了操作目标，工具名本身携带了完整的对象定位信息，无需额外的对象选择步骤。

每一层只做一件事，决策权归属清晰：

| 层次 | 决策主体 | 决策内容 |
|------|----------|----------|
| Agent 选工具 | LLM | 确定目标对象/视图（靠工具描述中的字段能力表），`{code}` 为对象/视图唯一编码 |
| Agent 选工具 | LLM | 确定意图：查明细 → `query_{code}`；做统计 → `compute_{code}` |
| Agent 填参 | LLM | 用自然语言表达意图（字段名、过滤值填中文术语，**不做 code 转换**） |
| before_callback Step 1 | **规则引擎** | `complex_conditions` 非空 → 标记 COMPLEX（< 10ms，无 LLM） |
| before_callback Step 2 通道 1 | **规则引擎** | 参数术语与字段 Schema 直接比对，能否 1:1 映射（< 100ms，无 LLM） |
| before_callback Step 2 通道 2 | 规则 + 轻量 LLM | query 文本关键词补充检测，仅通道 1 存在歧义项时触发（~1s） |
| before_callback Step 2 结果 | 规则 + 按需 LLM | CLEAR → 术语替换为 code；AUTO_DISAMBIG → 自动选定；NEED_CONFIRM → 中断追问 |
| 执行层 | Step 1 + Step 2 组合结果 | SIMPLE → OQL 直查；COMPLEX → redirect → data_query（PlanAgent） |

**两条核心哲学：**

1. **把"能不能用 OQL 表达"的判断，从 LLM 的主观认知转移到参数结构本身**——通过 `complex_conditions` 溢出字段，用规则而非 LLM 推理来识别复杂查询。

2. **把术语→code 的转换职责，从 LLM 转移到系统内部**——LLM 只负责表达意图，系统在 `before_callback` 内部做映射和歧义检测，确保转换错误可被捕获，而非静默执行。

> **工具描述字段表继续保留**：`query_*` / `compute_*` 工具描述中的字段能力矩阵（字段编码、中文名、类型、过滤/分组/聚合操作符）供 Agent **选工具阶段**参考，内容不做删减。调整点仅在于**填参策略**——LLM 选对工具后，不再被要求强制将意图转为 field_code，而是以自然语言填写；术语 → code 的映射由 `before_callback` Step 2 内部完成（见 2.2.2）。

---

### 2.2 流程设计

整体调用链以 `query_{code}` / `compute_{code}` 为**统一入口**，通过 `before_callback` 串联复杂度识别与歧义识别两道判断，最终路由到对应执行路径。

```
用户问题
    ↓
Agent 工具选择（LLM）
    ├── 靠工具描述中的字段能力表 → 确定目标对象/视图
    ├── 查明细 → query_{code}(query, select?, filters?, complex_conditions?, ...)
    └── 做统计 → compute_{code}(query, dimensions?, metrics?, filters?, complex_conditions?, ...)
    ↓
before_callback（两步串行，均在工具调用前执行）
    │
    ├─ Step 1：复杂度识别（< 10ms，纯规则）
    │         complex_conditions 非空 → is_complex = True
    │         complex_conditions 为空 → is_complex = False
    │         ⚠️ 仅做标记，不立即路由，等待 Step 2 完成
    │
    └─ Step 2：歧义识别（约 1s，轻量 LLM，简单/复杂查询均执行）
              │（复杂查询同样存在术语歧义，必须先澄清才能交给 PlanAgent）
              │
              ├── CLEAR（参数无歧义）
              │       is_complex=False → OQL 直查（QueryExecutor）→ 结果
              │       is_complex=True  → redirect → data_query_{code} → PlanAgent → 结果
              │
              ├── AUTO_DISAMBIG（可自动消歧）
              │       补全 filters 参数（自然语言值 → field_code）
              │       is_complex=False → OQL 直查 → 结果
              │       is_complex=True  → redirect → data_query_{code} → PlanAgent → 结果
              │
              └── NEED_CONFIRM（需用户确认）
                      ↓ interrupt() —— State 保存 + 节点挂起
                      用户选择 paradigm
                      ↓ resume —— 节点从顶部重新执行，重放工具调用
                      apply_resume → 用户选择写回 tool_params（自然语言值 → code）
                      before_callback 重放：Step 1 is_complex 重新计算，Step 2 通道1直接 CLEAR
                      is_complex=False → OQL 直查 → 结果
                      is_complex=True  → redirect → data_query_{code} → PlanAgent → 结果
```

**6 种路径组合**：

| 复杂度（Step 1） | 歧义状态（Step 2） | 最终执行路径 |
|-----------------|------------------|-------------|
| SIMPLE | CLEAR | OQL 直查 |
| SIMPLE | AUTO_DISAMBIG | 自动补全参数 → OQL 直查 |
| SIMPLE | NEED_CONFIRM | interrupt → 恢复重放 → OQL 直查 |
| COMPLEX | CLEAR | redirect → data_query（PlanAgent）|
| COMPLEX | AUTO_DISAMBIG | 自动补全参数 → redirect → data_query |
| COMPLEX | NEED_CONFIRM | interrupt → 恢复重放 → redirect → data_query |

> ⚠️ **COMPLEX + NEED_CONFIRM 恢复后路由说明**：用户消歧选择写回 `tool_params`（如将 `{field:"营收"}` 更新为 `{field:"revenue"}`）后，`complex_conditions` 仍然保留——因为复杂度条件本身（"亩产效益后30%"）不因字段歧义消除而消失。Step 1 重新计算仍得 `is_complex=True`，恢复后正确路由到 `data_query`，这是**预期行为**，不是 bug。

---

#### 2.2.1 复杂问题识别机制

##### 问题根因：旧参数协议依赖 LLM 自报

旧协议中有三个"元字段"由 LLM 在填参时一并自报：

```
intent_reason          → LLM 解释选择理由（主观，不可验证）
extraction_confidence  → LLM 自评置信度（不可靠，高置信≠正确）
ambiguous_params       → LLM 自报歧义字段（漏报率高）
```

依赖 LLM 自报的问题在于：LLM 不知道自己"不知道"，复杂查询在单表路径下静默执行并返回错误结果，而非在入口处被拦截。

##### 新参数协议：用参数结构承载复杂度信号

**新增两个字段，移除三个元字段：**

| 变更 | 字段 | 说明 |
|------|------|------|
| ➕ 新增 | `query: str` | 必填，用户原始问题的完整自然语言描述 |
| ➕ 新增 | `complex_conditions: list[str]` | 溢出区，存放 filter.value 无法表达为字面常量的条件 |
| ➖ 移除 | `intent_reason` | 改由 `query` 字段承担意图表达 |
| ➖ 移除 | `extraction_confidence` | 改由 before_callback 规则主动推断 |
| ➖ 移除 | `ambiguous_params` | 改由 before_callback Step 2 主动检测 |

##### 核心判据：filter.value 能否为字面常量

OQL 单表查询的 filter 结构如下：

```json
{"field": "yield_efficiency", "op": "lt", "value": 500}
```

`value` 必须是一个**查询前已知的字面常量**。当过滤条件的值需要先从数据中动态计算时，LLM 无法填写 `value`，这就是"溢出"：

```
已知（字面常量）→ 填入 filters
  示例：效能等于"中效" / 创建时间在本月 / 营收大于100万

不知道（需先从数据计算）→ 填入 complex_conditions（自然语言描述该条件）
  示例：亩产效益后30% / 营收高于行业平均 / 排名前10名 / 同比增长超过20%
```

`select / dimensions / metrics / order_by` 的值（字段编码、排序方向、聚合函数）在查询时均是已知的，**溢出只发生在 filters**。

##### 识别规则：一条规则，确定性 100%

```python
is_complex = bool(tool_params.get("complex_conditions"))
```

`complex_conditions` 非空 → 标记为 **COMPLEX**（需 PlanAgent 处理）；为空 → 标记为 **SIMPLE**（走 OQL 单表）。

LLM 在填参时只需回答一个问题：**"这个条件的 value，我现在知道是多少吗？"** 不需要理解"窗口函数"或"百分位"的概念。

**参数示例（复杂查询）：**

```json
{
  "query": "找出亩产效益后30%的地块，查询这些地块上的中、低效能的企业清单",
  "select": ["enterprise_name", "efficiency_level", "yield_efficiency"],
  "filters": [
    {"field": "efficiency_level", "op": "in", "value": ["中效", "低效"]}
  ],
  "complex_conditions": ["亩产效益后30%的地块"],
  "order_by": [{"field": "yield_efficiency", "direction": "asc"}],
  "limit": 100
}
```

- `efficiency_level` 的过滤值（中效/低效）是字面常量 → 填入标准 `filters`
- 亩产效益后30% 的阈值需先从数据计算 → 填入 `complex_conditions`
- `select / order_by` 无溢出，照常填写

---

#### 2.2.2 歧义问题识别机制

##### 参数填写策略：鼓励自然语言，禁止强行转码

**旧策略的问题**：工具描述要求 LLM 将用户意图中的中文术语强行转换为 `field_code` 和枚举 code 后再填入参数。这带来两个隐患：

1. **转错**：LLM 按语义猜测 code，`"营收"` 可能错填为 `net_revenue`（净营收），而实际应为 `gmv`
2. **无法检测歧义**：一旦 LLM 已经完成转换，系统看到的是确定的 code，无从知道原始意图是否有歧义

**新策略**：LLM **不做 code 转换**，直接填写它理解的中文业务术语或自然语言词语；系统在 `before_callback` 内部完成术语 → code 的映射，并在映射过程中检测歧义。

```
旧策略（LLM 强制转码）：
  用户："查询高营收的龙头企业"
  LLM 填参：filters = [{field: "revenue", op: "gt", value: 1000000}]
                                ↑ 猜测转换，可能错

新策略（LLM 填自然语言，系统映射）：
  用户："查询高营收的龙头企业"
  LLM 填参：filters = [{field: "营收", op: "gt", value: "高"}]
                              ↑ 填业务术语    ↑ 填自然语言描述
  → before_callback 做映射检查，发现 "营收" 对应 revenue/net_revenue/gmv 三个候选 → 歧义
```

LLM 的职责从"转码"变为"表达意图"：填写它**想要什么**，不需要知道系统内部的 field_code 是什么。

---

##### 歧义的本质：术语与字段 Schema 无法 1:1 映射

歧义来自**参数中的自然语言术语（字段名、过滤值、select 项）与工具字段 Schema 中已知的 field_code / 枚举 code 之间，存在 0:1 或 1:N 的映射关系**：

| 映射关系 | 含义 | 处理 |
|---------|------|------|
| 1:1 精确匹配 | 术语唯一对应已知 field_code 或枚举值 | CLEAR，直接替换为 code |
| 1:N 多候选 | 术语对应 2+ 个候选 | 进入歧义处理 |
| 0:1 无对应 | 术语在字段 Schema 中无任何匹配 | 进入歧义处理 |

---

##### 识别流程：双路并行检测，合并后判断

歧义来源有两条并行通道：**参数直接检测**和 **query 文本检测**，合并结果统一进入歧义处理阶段。

```
Step 2 歧义识别

┌─ 通道 1：参数直接检测（< 100ms，纯规则）──────────────────────────────┐
│  遍历 LLM 填写的参数术语：                                             │
│    filters.field  → 映射到 field_code（1:1 / 1:N / 无对应）           │
│    filters.value  → 映射到枚举 code（1:1 / 1:N / 无对应）             │
│    select 项      → 映射到 field_code                                  │
│    dimensions/metrics 项 → 映射到 field_code                           │
│  优先检测，能快速发现参数中的明显歧义                                   │
└────────────────────────────────────────────────────────────────────────┘
          ↓ 有歧义项                    ↓ 全部 1:1
┌─ 通道 2：query 文本检测（~1s，1次 LLM 调用）──────────────────────────┐
│  expand_query(query) → NatQuery（含 select/where/group_by 等结构）     │
│  natquery_to_five_stage(NatQuery) → 五段式关键词（纯规则，无 LLM）     │
│  关键词 → 映射到 field_code / 枚举值                                   │
│  补充捕获参数中未显式填写、但 query 中隐含的歧义概念                    │
└────────────────────────────────────────────────────────────────────────┘
          ↓
    合并两个通道的映射结果
          ↓
    ┌── 全部 1:1 映射，且无时间歧义 → CLEAR
    │     直接将术语替换为对应 code，继续执行
    │
    ├── 存在 1:N 或 0:1 → 进入阶段 C：paradigm 召回
    │     build_paradigm_resolution_state() 召回候选 paradigm
    │       ├── 多候选，LLM 能从 query 上下文唯一确定
    │       │     → AUTO_DISAMBIG：自动选定 code，不打断用户
    │       └── 多候选，无法自动确定
    │             → NEED_CONFIRM：触发 interrupt 追问
    │
    └── complex_conditions 中的术语有多义 → 同上进入阶段 C
```

> **为什么参数直接检测优先于 query 文本检测？**
> 参数是 LLM 已经明确表达意图的地方，直接检测无需 LLM 调用（< 100ms），可以在无歧义时完全跳过 expand_query 的 1s 开销。只有参数中存在无法确定的项时，才需要用 query 文本做补充分析。

---

##### 触发歧义处理（阶段 C）的规则

| 规则 | 检测来源 | 触发条件 | 示例 |
|------|---------|---------|------|
| A1 | 参数 / query | 一个术语对应 2+ 个候选 field_code | `"营收"` → `revenue / net_revenue / gmv` |
| A2 | 参数 `filters.value` | enum 字段的过滤值是自然语言，无法映射到已知枚举 code | `"高效"` → `enterprise_level` 枚举值不明确 |
| A3 | 参数 / query | 术语在字段 Schema 中无任何匹配 | `"收益能力"` → 不是任何 field_code |
| A4 | 参数 `filters.value` / query | 时间范围使用相对表达 | `"最近"` / `"今年"` / `"上季度"` → 起止日期不确定 |
| A5 | `complex_conditions` | 条件中的术语本身有多种语义 | `"高亩产地块"` → 绝对值阈值还是相对排名？ |

**直接 CLEAR（跳过阶段 C）的条件：**
- 参数中所有术语均 1:1 映射到已知 field_code 或枚举 code
- 时间过滤值均为绝对日期（如 `"2026-04-01"`），无相对时间表达
- `query` 文本检测补充的关键词同样全部 1:1 映射（参数检测 CLEAR 时才触发此步）

> `llm_confirm` 仅在 NEED_CONFIRM 分支调用，CLEAR 和 AUTO_DISAMBIG 路径均不调用，节省 1-2s。

---



##### 参数填写示例对比

**示例：查询高营收的龙头企业**

```json
// ❌ 旧策略（LLM 强制转码，风险高）
{
  "query": "查询高营收的龙头企业",
  "filters": [
    {"field": "revenue",          "op": "gt",  "value": 1000000},
    {"field": "enterprise_level", "op": "eq",  "value": "LEADING"}
  ]
}
// LLM 把"高营收"猜测为 revenue>100万，把"龙头"猜测为 LEADING，都可能猜错
// 系统无法感知歧义，直接执行，结果错误也无从排查

// ✅ 新策略（LLM 填自然语言，系统检测歧义）
{
  "query": "查询高营收的龙头企业",
  "filters": [
    {"field": "营收",     "op": "gt", "value": "高"},
    {"field": "企业等级", "op": "eq", "value": "龙头"}
  ]
}
// before_callback 通道 1 检测：
//   "营收" → revenue / net_revenue / gmv（A1 触发）
//   "高"   → 无法映射为数值（A3 触发）
//   "企业等级" → enterprise_level（1:1 OK）
//   "龙头" → 枚举值 LEADING（1:1 OK）
// → 进入阶段 C → NEED_CONFIRM → interrupt 追问"营收"指哪个字段、"高"的具体阈值
```

---

#### 2.2.3 问题中断及恢复机制

##### 前提：三类工具均为虚拟注入，不能互相直接调用

`query_{code}`、`compute_{code}`、`data_query_{code}` 都是通过 `inject_virtual_actions()` 动态生成后注册到同一个 `tools_map` 中的**虚拟工具**——它们是注册在映射表里的异步可调用对象，而非真正的类方法。

因此：

- `query_{code}` 的执行体**无法直接调用** `data_query_{code}`，两者之间不存在函数调用关系。
- 路由发生在 **`before_callback` 层**：`tool_wrapper.py` 的 `dispatch_tool()` 在执行真正的工具函数前，先运行 `hook_manager.run_before(ctx)`；如果 hook 返回 `redirect` 决策，`dispatch_tool()` 便从 `tools_map` 查找目标工具并直接调用，**原工具函数不再执行**。

```
tools_map = {
    "query_Enterprise":      <coroutine>,   # 虚拟注入
    "compute_Enterprise":    <coroutine>,   # 虚拟注入
    "data_query_Enterprise": <coroutine>,   # 虚拟注入（db_query 模式）
    ...
}

dispatch_tool("query_Enterprise", args)
  ↓
hook_manager.run_before(ctx)           # before_callback 运行
  ├── COMPLEX → HookDecision(action="redirect", tool="data_query_Enterprise", params=...)
  │      ↓
  │   tools_map["data_query_Enterprise"](**params)   # 从 tools_map 查找并直接调用
  │
  └── SIMPLE → 继续执行 tools_map["query_Enterprise"](**args)
```

> **`redirect` 是需要新增的 HookDecision 类型**：当前 `tool_wrapper.py` 只处理 `short_circuit` 和 `fail`，需扩展以支持 `redirect`。`redirect` 后不再触发 `before_callback`，避免递归。

---

##### 中断粒度：工具调用级重放，而非代码行级

LangGraph 的 `interrupt()` 在本项目中采用**方案 B（State 保存 + 节点重放）**，而非协程帧级挂起。

**中断时**（`interrupt()` 抛出 `GraphBubbleUp`）：

```python
# react_loop.py — 捕获 GraphBubbleUp，保存现场到 State
except GraphBubbleUp:
    _pending = list(ai_msg.tool_calls[tc_idx:])      # 从当前工具调用开始，包括后续未执行的
    state["react_messages"]          = _serialize_messages(messages)
    state["react_pending_tool_calls"] = _pending      # 含原始工具名 + 完整参数
    state["react_round_idx"]         = round_idx
    state["react_last_query_data"]   = _last_query_data
    raise                                              # 继续向上抛，LangGraph 挂起节点
```

**恢复时**（用户完成选择，LangGraph 重新触发节点）：

```python
# react_loop.py — 节点从顶部重新执行，检测到 State 中有保存的现场
_state_msgs = state.get("react_messages")
if _state_msgs is not None:
    # 重建上下文，对 pending_tool_calls 逐个重放
    for tc in pending_tool_calls:
        _tc_args = dict(tc.get("args") or {})
        # 剥除旧版元字段（新方案中这些字段已移除，此处仍保留兼容）
        _tc_args.pop("ambiguous_params", None)
        _tc_args.pop("intent_reason", None)
        _tc_args.pop("extraction_confidence", None)
        tool_id, result = await dispatch_tool(tc, tools_map, state, ...)
```

**关键结论**：

1. **恢复粒度是工具调用级**——节点从顶部重新执行，通过检测 State 快速跳过已完成步骤，然后对 `react_pending_tool_calls` 中的工具调用重新分派（`dispatch_tool`）。
2. **`before_callback` 在恢复时会再次运行**——因为重放走的是完整的 `dispatch_tool()` 路径。
3. **`is_complex` 是重新计算的**，不是保存的局部变量——`complex_conditions` 保存在 `tool_params` 里，而 `tool_params` 保存在 `react_pending_tool_calls` 的 args 中，因此恢复后可以重新读到，结果与中断前完全一致。

---

##### 恢复时 before_callback 重复触发问题

由于 `before_callback` 在恢复时会再次运行，**歧义判断（Step 2）存在重复触发中断的风险**：用户刚完成了一次选择，恢复后 `before_callback` 若再次进入歧义流程，会再次 `interrupt()`，陷入死循环。

**解决方案：用户选择结果写回 tool_params**

用户的选择（paradigm 消歧结果）在 resume 时由框架注入，系统在重放前将选择结果**应用到 `tool_params`** 中（更新 filters 的自然语言值为已确认的 code），使恢复后 `before_callback` 的通道 1 参数直接检测命中 1:1 映射，从而**跳过歧义流程，直接 CLEAR**：

```
用户选择 "营收 → revenue"
  ↓
apply_resume_to_tool_params()
  → tool_params["filters"] 中 {field:"营收"} 更新为 {field:"revenue"}
  ↓
重放 dispatch_tool("query_Enterprise", updated_tool_params)
  ↓
before_callback 通道 1：{field:"revenue"} → 1:1 精确匹配 → CLEAR
  ↓
路由到 OQL 直查（is_complex=False）或 data_query（is_complex=True）
```

此机制同时保证了两条路由的正确性：

| 原始工具 | is_complex（重新计算） | 恢复后执行 | HookDecision |
|---------|----------------------|-----------|--------------|
| `query_{code}` | False | 继续原 `query_{code}`（OQL） | `patch`（或直接放行） |
| `compute_{code}` | False | 继续原 `compute_{code}`（OQL） | `patch`（或直接放行） |
| `query_{code}` / `compute_{code}` | True | `data_query_{scope_code}` | `redirect` |

---

##### 歧义澄清流程

> **设计校对说明**（对原始设想的核对与完善）：
>
> | 原始设想 | 实际实现 | 说明 |
> |---------|---------|------|
> | 返回 `dict`，判断 `needXX=true` | 返回 `ClarificationResult`（含 `.needs_clarification`, `.form`, `.knowledge`） | 有两个字段：`form` 透传前端，`knowledge` 是 format 步骤必需的内部元数据 |
> | `format_*(dict, form)` | `format_*(query, structured, form, knowledge)` | 需要额外传入 `knowledge` |
> | 拿着 dict 继续调用 `query_{code}` 或 `data_query` | 路由由 Step 1 的 `is_complex` 决定，与澄清流程正交 | 澄清完成后写回 `tool_params`，路由逻辑不变 |
> | `knowledge` 不知道怎么传 | 通过 interrupt payload 暗字段 `_clarify_knowledge` 保存，前端随 resume 原样回传 | 是横跨 interrupt 的关键状态持久化点 |

###### 1. 查询澄清中断 & 恢复流程

```
①  before_callback Step 2 检测到 NEED_CONFIRM（来自 query_* 工具）
       ↓
②  _analyze_clarification("query", query, scope_code, tool_params)
       内部调用：
         analyze_query_clarification_query(query, scope_code, structured_query)
       返回 ClarificationResult:
         .needs_clarification = True
         .form     = '{"paradigmList":[...]}'  ← 追问表单，透传给前端
         .knowledge = '{...}'                  ← 内部元数据（KnowledgeMeta），format 步骤必需
       ↓
③  interrupt({
         "ask_user_payload":   {"paradigmList": [...]},    ← 前端展示用
         "_clarify_knowledge": clarification_result.context_knowledge,  ← 暗字段，前端随 resume 回传
         "_clarify_query":     query,                      ← 暗字段，备用
     })
     ─────────────── 节点挂起，等待用户 ───────────────
       ↓  用户填写 paradigmList（选择 choiceKeyword/choiceField/choiceValue）
④  resume_value = {
         "paradigmList": [{...用户已填的选择...}],
         "_clarify_knowledge": "...",   ← 前端原样回传的暗字段
         "_clarify_query":     query,
     }
       ↓
⑤  _apply_resume("query", ctx, resume_value, query, is_complex)
       内部调用：
         knowledge = resume_value["_clarify_knowledge"]
         form_str  = json.dumps({"paradigmList": resume_value["paradigmList"]})
         updated   = format_clarification_query(query, tool_params, form_str, knowledge)
       → updated 中所有术语已替换为 field_code
       → ctx["tool_params"] = updated
       ↓
⑥  路由决策（Step 1 的 is_complex，不受澄清流程影响）
       is_complex = False  →  query_{code}(updated_tool_params)   [OQL 直查]
       is_complex = True   →  redirect → data_query_{code}(query, contextKnowledge)  [PlanAgent]
```

###### 2. 统计澄清中断 & 恢复流程

与查询澄清完全对称，仅函数名不同：

```
①  before_callback Step 2 检测到 NEED_CONFIRM（来自 compute_* 工具）
②  analyze_query_clarification_compute(query, scope_code, structured_compute)
③  interrupt(payload)         ← 与查询澄清相同
④  resume_value               ← 与查询澄清相同
⑤  format_clarification_compute(query, structured_compute, form_str, knowledge)
    → updated_structured_compute（包含 dimensions / metrics / filters / having / order_by，均为 field_code）
    → ctx["tool_params"] = updated
⑥  is_complex=False → compute_{code}(OQL 统计)
    is_complex=True  → redirect → data_query_{code}(PlanAgent)
```

###### 3. ClarificationResult 数据结构（SDK 层）

```python
@dataclass(frozen=True, slots=True)
class ClarificationResult:
    query: str                        # 规范化后的查询文本
    needs_clarification: bool = False # True → 需要追问用户
    form: str = ""                    # JSON 字符串：'{"paradigmList":[...]}', 透传给前端
    knowledge: str = ""               # JSON 字符串：KnowledgeMeta，format_* 步骤必需
```

`needs_clarification = False` 时：`form` 为空，`knowledge` 含自动消歧后的完整映射，不触发 interrupt。

###### 4. paradigmList（form 字段）结构

前端按 `paradigmList` 渲染选择界面，用户填写 `choiceKeyword` / `choiceField` / `choiceValue` 后原样回传：

```json
{
  "paradigmList": [
    {
      "paradigmId": "1",
      "paradigmName": "查询值",
      "paradigmResult": [
        {
          "kid": "k1",
          "keyword": "营收",
          "candidates": ["total_revenue", "net_revenue"],
          "choiceKeyword": "total_revenue",  ← 用户填写后回传
          "ktype": "select",
          "reason": "存在多个候选字段"
        }
      ]
    },
    {
      "paradigmId": "3",
      "paradigmName": "过滤条件",
      "paradigmResult": [
        {
          "kid": "k2",
          "keyword": "高效",
          "candidates": ["high", "medium_high"],
          "choiceField": "efficiency_level",  ← 用户填写
          "choiceValue": "high",              ← 用户填写
          "ktype": "whereKey"
        }
      ]
    }
  ]
}
```

###### 5. knowledge 的保存与传递（关键约束）

`analyze_*` 返回的 `knowledge`（KnowledgeMeta JSON）是 `format_*` 所必需的内部状态，包含 path_mapping、字段路径等上下文。由于澄清流程横跨一次 `interrupt`，`knowledge` 必须持久化：

```
analyze → ClarificationResult.knowledge ──→ interrupt payload["_clarify_knowledge"]
                                                    ↓ (前端保存并原样回传)
format  ← resume_value["_clarify_knowledge"] ←────┘
```

- **前端约定**：提交 resume 时，必须将收到的 `_clarify_knowledge` / `_clarify_query` 原样包含在 resume body 中（即使前端不使用这两个字段）。
- **降级处理**：若 `_clarify_knowledge` 丢失，`format_*` 以空 knowledge 降级运行，可能导致部分字段映射不精确，但不影响主流程继续执行。

###### 6. 复杂查询（COMPLEX）+ 澄清的组合路径

当 `is_complex=True` 且 `NEED_CONFIRM` 时：

```
澄清完成 → updated_tool_params（术语已替换为 field_code）
         ↓
before_callback 主流程（恢复后重放）：
  Step 1：complex_conditions 仍非空 → is_complex=True
  Step 2：通道 1 全部 1:1 CLEAR（已替换）
         ↓
redirect → data_query_{code}(
    query=query,
    contextKnowledge=json.dumps(updated_tool_params)  ← 包含已确认的 filters/select/dimensions/metrics
)
```

`contextKnowledge` 将已澄清的结构化参数（field_code 版本）作为上下文传给 PlanAgent，提升复杂查询的 Schema Linking 精度。



##### 完整中断/恢复时序

```
① 用户提问
      ↓
② Agent 选工具：query_Enterprise(query="...", filters=[{field:"营收",...}], complex_conditions=[])
      ↓
③ dispatch_tool → before_callback（Step 1 SIMPLE, Step 2 通道1检测 "营收" 有多候选）
      ↓
④ interrupt({reason_code:"PARADIGM_CLARIFICATION", paradigmList:[...]})
   → GraphBubbleUp 抛出
   → react_loop.py 捕获，保存到 State：
       react_pending_tool_calls = [{name:"query_Enterprise", args:{...原始参数...}}]
       react_messages = [...当前消息历史...]
   → LangGraph 挂起节点，等待用户
      ↓
⑤ 用户选择："营收 = revenue（总营收）"
      ↓
⑥ LangGraph resume → 节点重新从顶部执行
   → 检测到 react_messages / react_pending_tool_calls（State 中有保存现场）
   → apply_resume_to_tool_params()：将 {field:"营收"} 更新为 {field:"revenue"}
   → 重放 dispatch_tool("query_Enterprise", updated_args)
      ↓
⑦ before_callback 再次运行：
   Step 1：complex_conditions=[] → is_complex=False
   Step 2：通道 1 {field:"revenue"} → 1:1 匹配 → CLEAR
      ↓
⑧ tools_map["query_Enterprise"](updated_args) 执行
   → QueryExecutor → OQL 单表查询 → 结果返回
```



## 3.详细设计

本章按改动范围从小到大逐模块描述，每节给出：**改什么、在哪改、怎么改（含关键代码片段）**。

### 3.1 参数 Schema 变更（generator.py）

**文件**：`packages/datacloud-data/src/datacloud_data_sdk/virtual_action/generator.py`

**目标**：在 `build_query_schema()` 和 `build_compute_schema()` 生成的 JSON Schema 中新增 `query` 和 `complex_conditions` 两个字段，同时将 `query` 加入 `required` 列表；**并放开现有字段的 `const`/`enum` 约束，允许 LLM 填中文业务术语**。

> **执行层约束（关键前提）**：`QueryExecutor` / `VirtualActionValidator` 强制要求传入的 `filters.field`、`select` 项、`order_by.field`、`dimensions.field`、`metrics.field` 均为 **field_code**——底层以 `field_map`（键为 field_code）查找字段元数据，传中文名会抛 `UNSUPPORTED_FIELD` 异常。
>
> 因此，"鼓励自然语言"策略不能穿透到 Executor。**`before_callback` 在放行给 Executor 之前，必须将所有中文术语替换为对应 field_code**（见 §3.4.3）。Schema 层的 `const`/`enum` 放开，只是让 LLM 能填中文；真正的映射和替换发生在 hook 层。

#### 3.1.1 build_query_schema 变更

**① 放开 `_build_filters_schema()` 中的 `const` 约束**（generator.py 约第 145 行）：

```python
# ── 改前：const 限制只能填 field_code ────────────────────────────────────────
"field": {"type": "string", "const": field_code},

# ── 改后：放开 const，description 同时给出 field_code 和中文名供参考 ──────────
"field": {
    "type": "string",
    "description": (
        f"可填字段编码 '{fcode}' 或中文名 '{fname}'，系统自动映射；"
        "不确定时优先填中文业务术语，由系统识别。"
    ),
},
```

**② 放开 `select` 的 `items.enum` 约束**（generator.py 约第 230 行）：

```python
# ── 改前：enum 限制只能填已知 field_code ─────────────────────────────────────
"select": {
    "type": "array",
    "items": {"type": "string", "enum": all_codes},
    ...
},

# ── 改后：去掉 enum 约束，保留 x-dc-field-catalog 供 LLM 参考 ────────────────
"select": {
    "type": "array",
    "items": {"type": "string"},
    "description": "返回字段列表，可填 field_code 或中文字段名，系统自动映射；为空时返回全部非关联字段。",
    "x-dc-field-catalog": [{"name": _fn(f), "code": _fc(f)} for f in queryable],
},
```

`order_by.field` 的 `enum` 同理放开，`description` 说明可填 field_code 或中文名。

**③ 新增 `query` 和 `complex_conditions` 字段**（在 `"select"` 条目之前插入）：

```python
"query": {
    "type": "string",
    "description": (
        "【必填】用户原始查询意图的完整自然语言描述。"
        "用于歧义判断、复杂查询路由及参数缺失时的兜底推断。"
        "不得为空，不得使用通配符代替。"
    ),
},
"complex_conditions": {
    "type": "array",
    "items": {"type": "string"},
    "default": [],
    "description": (
        "溢出过滤区：每个元素用自然语言描述一个"
        "「过滤条件的值无法在填参时确定为字面常量」的条件，"
        "例如 '亩产效益后30%的地块'、'营收高于行业平均'。"
        "能写成字面值的过滤条件仍放入 filters；"
        "此列表非空时系统自动路由到全能查询路径（data_query）。"
    ),
},
```

同时将 `schema["required"]` 从空列表改为 `["query"]`（当前该 schema 无 required 字段，需新增）：

```python
# build_query_schema 返回前
schema["required"] = ["query"]
return schema
```

**改造后 query_{code} 完整参数协议示例**：

```json
// 场景A：标准明细查询（simple，无 complex_conditions）
// 用户问："查询本月新增的、营收大于100万的制造业或农业企业，按营收降序取前20条"
//
// ✅ 新策略：LLM 填自然语言，不做 code 转换
// before_callback 行为：
//   Step 1：complex_conditions=[] → is_complex=false
//   Step 2 通道1：
//     "企业名称"→ enterprise_name（1:1）
//     "营收"    → revenue / net_revenue / gmv（1:N，A1 触发歧义）
//     "创建日期"→ created_date（1:1）
//     "行业"    → industry（1:1）
//     "制造业"  → 枚举 manufacturing（1:1 CLEAR）
//     "农业"    → 枚举 agriculture（1:1 CLEAR）
//     仅 "营收" 多候选 → 进入阶段C → AUTO_DISAMBIG 或 NEED_CONFIRM
{
  "query": "查询本月新增的、营收大于100万的制造业或农业企业，按营收降序取前20条",
  "select": ["企业名称", "营收", "创建日期", "行业"],
  "filters": [
    {"field": "创建日期", "op": "between", "value": ["2026-04-01", "2026-04-30"]},
    {"field": "营收",     "op": "gt",      "value": 1000000},
    {"field": "行业",     "op": "in",      "value": ["制造业", "农业"]}
  ],
  "filter_relation": "AND",
  "order_by": [{"field": "营收", "direction": "desc"}],
  "limit": 20,
  "offset": 0,
  "complex_conditions": []
}

// 场景B：含复杂条件（complex，redirect → data_query）
// 用户问："找出亩产效益后30%的地块，查询这些地块上的中、低效能企业清单"
//
// ✅ 新策略：filters.value 填中文枚举描述，complex_conditions 填无法字面化的条件
// before_callback 行为：
//   Step 1：complex_conditions 非空 → is_complex=true
//   Step 2 通道1：
//     "效能等级" → efficiency_level（1:1 CLEAR）
//     "中效"    → 枚举 medium（1:1 CLEAR）
//     "低效"    → 枚举 low（1:1 CLEAR）
//     → 全部 CLEAR
//   路由：redirect → data_query_Enterprise(query=..., contextKnowledge=...)
{
  "query": "找出亩产效益后30%的地块，查询这些地块上的中、低效能企业清单",
  "select": ["企业名称", "效能等级", "亩产效益"],
  "filters": [
    {"field": "效能等级", "op": "in", "value": ["中效", "低效"]}
  ],
  "order_by": [{"field": "亩产效益", "direction": "asc"}],
  "limit": 100,
  "complex_conditions": [
    "亩产效益后30%的地块"
  ]
}
```

#### 3.1.2 build_compute_schema 变更

同 §3.1.1，对 compute 侧的约束做相同放开：

**① 放开 `dimensions` 的字段约束**（`_dim_item()` 约第 343 行）：

```python
# ── 改前：const 限制 field 只能填 field_code ──────────────────────────────────
"field": {"type": "string", "const": fcode},

# ── 改后：放开，description 给出参考 ─────────────────────────────────────────
"field": {
    "type": "string",
    "description": f"可填字段编码 '{fcode}' 或中文名 '{fname}'，系统自动映射。",
},
```

**② 放开 `metrics` 的字段约束**（`_msr_item()` 约第 380 行），同时新增 `expr` 和指标级 `filters`：

```python
# ── 改前：const 限制 field 只能填 field_code ──────────────────────────────────
"field": {"type": "string", "const": fcode},

# ── 改后：放开，同时新增 expr（公式）和 filters（条件聚合）──────────────────────
"field": {
    "type": "string",
    "description": f"可填字段编码 '{fcode}' 或中文名 '{fname}'（与 expr 互斥）。",
},
"expr": {
    "type": "string",
    "description": "公式表达式（与 field 互斥），可填中文运算式如 '营收 - 成本'，系统映射后生成 SQL。",
},
"filters": {
    "type": "array",
    "description": "条件聚合过滤（CASE WHEN），field/value 可填中文，规则同行级 filters。",
    "items": {"type": "object"},
},
```

`required` 从 `["field", "agg", "as"]` 放宽为 `["agg", "as"]`（`field` 与 `expr` 二选一，均可选填）。

**③ 新增 `query` 和 `complex_conditions` 字段**（在 `"dimensions"` 条目之前插入，定义同 §3.1.1），并设置 `schema["required"] = ["query"]`。


**改造后 compute_{code} 完整参数协议示例（覆盖 MetricItem 五种形态）**：

```json
// 示例1：① 行数统计 + ② 普通字段聚合
// 用户问："按行业、地区统计企业数和总营收，过滤营收>0，按总营收降序取前50"
// SQL → SELECT industry, region, COUNT(*), SUM(revenue) FROM t
//           WHERE revenue > 0 GROUP BY industry, region ORDER BY SUM(revenue) DESC LIMIT 50
//
// ✅ 新策略：dimensions/metrics/filters 全部填自然语言
// before_callback 通道1：
//   "行业" → industry（1:1）"地区" → region（1:1）
//   "营收" → revenue / net_revenue / gmv（1:N A1 触发）→ 歧义处理
//   → AUTO_DISAMBIG 自动确认"营收=revenue" 或 NEED_CONFIRM 追问
{
  "query": "按行业、地区统计企业数和总营收，过滤营收大于0，按总营收降序取前50",
  "dimensions": ["行业", "地区"],
  "metrics": [
    {"agg": "count_all", "as": "enterprise_count"},
    {"field": "营收", "agg": "sum", "as": "total_revenue"}
  ],
  "filters": [
    {"field": "营收", "op": "gt", "value": 0}
  ],
  "order_by": [{"field": "total_revenue", "direction": "desc"}],
  "limit": 50,
  "complex_conditions": []
}

// 示例2：③ 条件字段聚合（field + 指标级 filters → CASE WHEN）
// 用户问："按行业统计总营收，同时单独统计龙头企业营收"
// SQL → SUM(revenue) + SUM(CASE WHEN enterprise_level='leading' THEN revenue ELSE 0 END)
//
// before_callback 通道1：
//   "行业" → industry（1:1）
//   "营收" → revenue/net_revenue/gmv（1:N）→ 歧义处理
//   "企业级别" → enterprise_level（1:1）
//   "龙头" → 枚举 leading（1:1）
{
  "query": "按行业统计总营收，同时单独统计龙头企业营收，按总营收降序",
  "dimensions": ["行业"],
  "metrics": [
    {"agg": "count_all", "as": "enterprise_count"},
    {"field": "营收", "agg": "sum", "as": "total_revenue"},
    {
      "field": "营收",
      "filters": [{"field": "企业级别", "op": "eq", "value": "龙头"}],
      "agg": "sum",
      "as": "leading_revenue"
    }
  ],
  "order_by": [{"field": "total_revenue", "direction": "desc"}],
  "limit": 50,
  "complex_conditions": []
}

// 示例3：④ 公式聚合（expr）+ ⑤ 条件公式聚合（expr + filters）
// 用户问："按行业和地区统计平均利润及龙头骨干企业总利润，过滤一季度数据"
// SQL → AVG(revenue-cost) + SUM(CASE WHEN level IN(...) THEN (revenue-cost) ELSE 0 END)
//
// before_callback 通道1：
//   "行业"/"地区" → 1:1
//   "营收"/"成本" → expr 中的业务术语，1:N → 歧义处理
//   "龙头"/"骨干" → 枚举值，1:1 CLEAR
{
  "query": "按行业和地区统计平均利润及龙头骨干企业总利润，过滤一季度数据，按平均利润降序",
  "dimensions": ["行业", "地区"],
  "metrics": [
    {"agg": "count_all", "as": "enterprise_count"},
    {"field": "营收", "agg": "sum", "as": "total_revenue"},
    {"expr": "营收 - 成本", "agg": "avg", "as": "avg_profit"},
    {
      "expr": "营收 - 成本",
      "filters": [{"field": "企业级别", "op": "in", "value": ["龙头", "骨干"]}],
      "agg": "sum",
      "as": "key_enterprise_profit"
    }
  ],
  "filters": [
    {"field": "创建日期", "op": "between", "value": ["2026-01-01", "2026-03-31"]}
  ],
  "having": [
    {"field": "total_revenue", "op": "gt", "value": 1000000}
  ],
  "order_by": [{"field": "avg_profit", "direction": "desc"}],
  "limit": 50,
  "complex_conditions": []
}

// 示例4：时间粒度分组（group_op:month）+ range 分桶（DimensionItem 对象形式）
// 用户问："按月份和营收区间统计企业数及平均利润，过滤分组内不足5家的"
// SQL → GROUP BY DATE_FORMAT(created_date,'%Y-%m'),
//              CASE WHEN revenue<500000 THEN '低收入' ... END
//
// before_callback 通道1：
//   "创建月份" → created_date（1:1，time 类型字段 group_op=month）
//   "营收区间" → revenue（1:1，range 分桶）
//   "营收"/"成本" → 多候选 → 歧义处理
{
  "query": "按月份和营收区间统计企业数及平均利润，过滤分组内不足5家的",
  "dimensions": [
    {"field": "创建月份", "group_op": "month"},
    {
      "field": "营收",
      "group_op": "range",
      "buckets": [
        {"from": null,    "to": 500000,  "label": "低收入"},
        {"from": 500000,  "to": 2000000, "label": "中收入"},
        {"from": 2000000, "to": null,    "label": "高收入"}
      ]
    }
  ],
  "metrics": [
    {"agg": "count_all", "as": "enterprise_count"},
    {"field": "营收", "agg": "avg", "as": "avg_revenue"},
    {"expr": "营收 - 成本", "agg": "avg", "as": "avg_profit"}
  ],
  "having": [
    {"field": "enterprise_count", "op": "gte", "value": 5}
  ],
  "order_by": [{"field": "avg_profit", "direction": "desc"}],
  "limit": 100,
  "complex_conditions": []
}

// 示例5：含 complex_conditions → redirect 到 data_query（复杂路由）
// 用户问："统计各地区亩产效益后30%地块上企业的平均营收"
//
// before_callback：
//   Step 1：complex_conditions 非空 → is_complex=true
//   Step 2 通道1："地区"→1:1 CLEAR，"营收"→多候选 → 歧义处理后 CLEAR
//   路由：redirect → data_query_Enterprise(query=..., contextKnowledge=...)
//         contextKnowledge 包含已确定维度/指标 + complex_conditions 原文
{
  "query": "统计各地区亩产效益后30%地块上企业的平均营收",
  "dimensions": ["地区"],
  "metrics": [
    {"field": "营收", "agg": "avg", "as": "avg_revenue"}
  ],
  "complex_conditions": [
    "亩产效益后30%的地块"
  ]
}
```

> **参数填写策略小结**（对应 2.2.2 节原则）：
>
> | 参数位置 | 新策略 | 旧策略（禁止） |
> |---------|--------|-------------|
> | `filters.field` | 填中文业务术语（如 `"营收"`、`"企业级别"`） | 强行猜测 field_code（如 `"revenue"`） |
> | `filters.value` | 填中文描述（如 `"龙头"`、`"高"`）或已知绝对值（数字、日期） | 强行猜测枚举 code（如 `"LEADING"`） |
> | `select` 项 | 填中文字段名（如 `"企业名称"`、`"营收"`） | 强行猜测 field_code |
> | `dimensions.field` | 填中文维度名（如 `"行业"`、`"创建月份"`） | 强行猜测 field_code |
> | `metrics.field` | 填中文指标名（如 `"营收"`） | 强行猜测 field_code |
> | `metrics.expr` | 填中文运算表达式（如 `"营收 - 成本"`） | 强行猜测 `"revenue - cost"` |
> | `metrics.filters[].field/value` | 同 `filters` — 填中文 | 同上 |
> | `complex_conditions` | 无法字面化的条件用中文描述 | — |
> | `order_by.field` | 可引用 `metrics.as` 别名（已确定），或中文字段名 | — |

#### 3.1.3 MetricItem 结构扩展

当前 `_msr_item()` 函数（约第 370 行）生成的 metric schema 仅支持 `field + agg + as` 三字段。根据 1.6.2 节的新协议，需要扩展支持 `expr`（公式字段）和 `filters`（条件聚合）。

在 `_msr_item()` 的 `properties` 字典中补充：

```python
"expr": {
    "type": "string",
    "description": (
        "公式表达式（与 field 互斥）。字段本身是计算表达式时使用，"
        "如 'revenue - cost'。使用 expr 时不填 field。"
    ),
},
"filters": {
    "type": "array",
    "items": {"$ref": "#/definitions/FilterItem"},  # 复用已有 filter 定义
    "description": (
        "条件聚合过滤（CASE WHEN）。仅对满足此过滤条件的行参与当前指标聚合。"
        "与 field/expr 正交，可同时使用。"
    ),
},
```

同时放宽 `required`：`field` 和 `expr` 互斥但均可选，实际 required 仅保留 `["agg", "as"]`：

```python
"required": ["agg", "as"],  # field 与 expr 二选一，均可选填（底层按有无判断）
```

> **注意**：`_msr_item()` 当前返回固定字段的 Schema，`expr`/`filters` 作为可选字段不影响现有调用，向后兼容。

---

### 3.2 移除 inject_ambiguity_fields（node.py）

**文件**：`src/datacloud_analysis/orchestration/execution/node.py`

**目标**：移除向工具 Schema 注入 `intent_reason / extraction_confidence / ambiguous_params` 三个元字段的逻辑，改为注入 `query`（必填）和 `complex_conditions`（可选）。

#### 3.2.1 当前状态

`inject_ambiguity_fields(t: BaseTool) -> BaseTool`（第 38-129 行）：

- 用 `create_model` 动态给工具的 `args_schema` 追加三个字段
- 包装 `t.coroutine` / `t.func`，在实际调用前将这三个字段 `pop` 掉

#### 3.2.2 改造方案

**不删除函数**，改为重命名并修改内容——新函数名 `inject_query_fields`，注入新的两个字段：

```python
def inject_query_fields(t: BaseTool) -> BaseTool:
    """往数据类工具 Schema 注入 query/complex_conditions 字段（由 LLM 在调用时填写，
    执行前自动剥除，消费由 before_callback 完成）。
    """
    try:
        from pydantic import Field, create_model  # noqa: PLC0415

        original_schema = t.args_schema
        if original_schema is None:
            return t

        new_schema = create_model(
            f"{original_schema.__name__}WithQuery",
            __base__=original_schema,
            query=(
                str,
                Field(
                    default="",
                    description=(
                        "【必填】用户原始查询意图的完整自然语言描述。"
                        "用于歧义判断、复杂查询路由及参数缺失时的兜底推断。"
                    ),
                ),
            ),
            complex_conditions=(
                list[str],
                Field(
                    default_factory=list,
                    description=(
                        "溢出过滤区：填入过滤值无法在填参时确定为字面常量的条件（自然语言描述）。"
                        "如 '亩产效益后30%的地块'、'营收高于行业平均'。"
                        "非空时系统路由到全能查询路径（data_query）。"
                    ),
                ),
            ),
        )
        t.args_schema = new_schema

        # 包装 coroutine：调用前剥除两个元字段（before_callback 已消费）
        if hasattr(t, "coroutine") and t.coroutine is not None:
            _orig_coro = t.coroutine

            async def _coro_strip_query_fields(**kw: Any) -> Any:
                kw.pop("query", None)
                kw.pop("complex_conditions", None)
                return await _orig_coro(**kw)

            t.coroutine = _coro_strip_query_fields

        if hasattr(t, "func") and t.func is not None:
            _orig_func = t.func

            def _func_strip_query_fields(**kw: Any) -> Any:
                kw.pop("query", None)
                kw.pop("complex_conditions", None)
                return _orig_func(**kw)

            t.func = _func_strip_query_fields

    except Exception as exc:  # pragma: no cover
        logger.warning(
            "[inject_query_fields] failed for tool=%s: %s", getattr(t, "name", "?"), exc
        )
    return t
```

在 `_build_tools_list()` 中将调用从 `inject_ambiguity_fields(t)` 替换为 `inject_query_fields(t)`。

#### 3.2.3 改造前后注入字段对照

`inject_query_fields` 改造后，LLM 在调用数据工具时看到的 Schema 字段发生如下变化：

| 字段 | 改造前（旧元字段） | 改造后（新协议字段） | 消费层 |
|------|------------------|---------------------|-------|
| `intent_reason: str` | 必填，LLM 自描意图 | **删除** | — |
| `extraction_confidence: float` | 必填，LLM 自评置信度 | **删除** | — |
| `ambiguous_params: list[str]` | 必填，LLM 自报歧义参数名 | **删除** | — |
| `query: str` | 不存在 | **新增，必填** | `before_callback` 读取后 `pop`，不透传底层 |
| `complex_conditions: list[str]` | 不存在 | **新增，默认 `[]`** | `before_callback` 读取后 `pop`，不透传底层 |

**LLM 侧感知对比**（以 `query_Enterprise` 为例）：

```json
// ── 改造前：LLM 需要填写 3 个元字段，且被要求强制转码 ────────────────────────
{
  "select": ["enterprise_name", "revenue"],
  "filters": [{"field": "revenue", "op": "gt", "value": 1000000}],
  //           ↑ LLM 猜测 field_code        ↑ LLM 猜测阈值
  "limit": 20,

  // 3 个元字段（改造后删除）
  "intent_reason": "用户想查营收超过100万的企业列表",
  "extraction_confidence": 0.9,
  "ambiguous_params": []
}

// ── 改造后（无复杂条件）：LLM 填自然语言，不做 code 转换 ─────────────────────
{
  // 必填：完整自然语言意图
  "query": "查询营收超过100万的企业，按营收降序取前20条",

  // select/filters/order_by 全部填中文业务术语
  "select": ["企业名称", "营收"],
  "filters": [{"field": "营收", "op": "gt", "value": 1000000}],
  //          ↑ 中文术语，before_callback 通道1 检测：
  //            "营收" → revenue/net_revenue/gmv（1:N）→ 歧义处理
  "order_by": [{"field": "营收", "direction": "desc"}],
  "limit": 20,

  // 溢出过滤区（无复杂条件时填空列表）
  "complex_conditions": []
}

// ── 改造后（含复杂条件）：complex_conditions 触发 redirect ───────────────────
{
  "query": "统计营收排名后30%企业中龙头企业的平均利润",
  "dimensions": ["企业级别"],
  "metrics": [
    {"expr": "营收 - 成本", "agg": "avg", "as": "avg_profit"}
  ],
  "filters": [
    {"field": "企业级别", "op": "eq", "value": "龙头"}
    //         ↑ 中文字段名    ↑ 中文枚举描述
    // before_callback 通道1：
    //   "企业级别" → enterprise_level（1:1 CLEAR）
    //   "龙头"     → 枚举 leading（1:1 CLEAR）
  ],
  // 「营收排名后30%」无法写成字面值 → 放入 complex_conditions
  // before_callback Step1 读到非空 → is_complex=true → redirect data_query
  "complex_conditions": [
    "营收排名后30%的企业"
  ]
}
```

---

### 3.3 Prompt 更新（prompts.py）

**文件**：`src/datacloud_analysis/i18n/prompts.py`

**目标**：将 `_build_exec_zh()` 中第 166-179 行的"参数提取自检字段"段替换为新的 `query / complex_conditions` 填写规则。

#### 3.3.1 替换内容

将以下旧文本：

```
## 参数提取自检字段（必填）
- 调用 query_*/compute_*/data_query_* 类工具时，除业务参数外，必须同时填写以下三个自检字段：
  · intent_reason ...
  · extraction_confidence ...
  · ambiguous_params ...
```

替换为：

```python
"## query_*/compute_* 核心参数规则\n",
"- **query（必填）**：完整自然语言问题，不得为空，不得使用 */%/ALL 等通配符。\n",
"  如用户原始问题较短，结合上下文改写为完整清晰的查询描述。\n",
"- **标准参数**（select / filters / dimensions / metrics）：\n",
"  能在工具描述字段表中找到明确 field_code 时填写；不确定时留空，系统结合 query 自动推断。\n",
"  filters 的 value 只填字面常量（已知的具体数值、日期、枚举 code）。\n",
"- **complex_conditions（溢出过滤区）**：\n",
"  过滤条件的值在填参时无法确定为字面常量时，用自然语言描述该条件并放入此列表：\n",
"  示例：'亩产效益后30%的地块'、'营收高于行业平均值'、'排名前10名'。\n",
"  此列表非空时系统自动路由到全能查询路径，无需手动调用 data_query。\n",
"- **禁止**：同时将同一条件既写入 filters 又写入 complex_conditions。\n",
```

---

### 3.4 before_callback 插件改造（query_clarification_plugin.py）

**文件**：`src/datacloud_analysis/tool_hook_plugins/builtin/query_clarification_plugin.py`

这是改动量最大的模块。原有 `before_call_back` 仅做单通道澄清（依赖 `ambiguous_params`），需完整替换为**双步双通道**机制。

#### 3.4.1 新主流程

```python
async def before_call_back(ctx: HookContext) -> HookDecision | None:
    """双步判断：Step 1 复杂度路由，Step 2 双通道歧义判断。"""
    tool_name: str = ctx.get("tool_name", "")
    if not _is_query_or_compute_tool(tool_name):
        return None

    tool_params: dict[str, Any] = dict(ctx.get("tool_params") or {})

    # 剥除新元字段（before_callback 消费，不透传给底层执行体）
    query: str = str(tool_params.pop("query", "") or "")
    complex_conditions: list[str] = list(tool_params.pop("complex_conditions", None) or [])

    # 兼容旧版元字段（过渡期保留，新版 LLM 不再填写）
    tool_params.pop("intent_reason", None)
    tool_params.pop("extraction_confidence", None)
    tool_params.pop("ambiguous_params", None)

    ctx["tool_params"] = tool_params

    logger.info(
        "[before_callback] tool=%s query=%.60r complex_conditions=%s",
        tool_name, query, complex_conditions,
    )

    # ── Step 1：复杂度判断 ────────────────────────────────────────────────────
    is_complex: bool = bool(complex_conditions)

    # ── Step 2：双通道歧义判断（简单和复杂查询均执行）────────────────────────
    ambiguity = await _judge_ambiguity(query, tool_name, tool_params, ctx)

    if ambiguity.status == "NEED_CONFIRM":
        # 触发歧义澄清
        clarification_result = await _analyze_clarification(
            query=query,
            tool_name=tool_name,
            tool_params=tool_params,
        )
        resume_value = interrupt({
            "prompt": "查询条件存在歧义，请确认查询维度",
            "reason_code": "PARADIGM_CLARIFICATION",
            "ask_user_payload": {"paradigmList": clarification_result.paradigm_list},
            # 暗字段：format_* 步骤必需，前端随 resume 原样回传
            "_clarify_knowledge": clarification_result.context_knowledge,
            "_clarify_query": query,
        })
        # 用户选择后写回 tool_params，防止恢复时再次触发歧义
        _apply_resume(ctx, tool_name, resume_value, query, is_complex)
        return {"action": "patch", "patch": {"tool_params": dict(ctx["tool_params"])}}

    if ambiguity.status == "AUTO_DISAMBIG" and ambiguity.refined_params:
        tool_params = {**tool_params, **ambiguity.refined_params}
        ctx["tool_params"] = tool_params

    # ── 路由决策 ─────────────────────────────────────────────────────────────
    if is_complex:
        return _build_redirect_decision(tool_name, query, tool_params)

    if ambiguity.refined_params:
        return {"action": "patch", "patch": {"tool_params": tool_params}}

    return None
```

#### 3.4.2 复杂度判断（Step 1）

```python
def _is_query_or_compute_tool(tool_name: str) -> bool:
    """仅处理 query_* 和 compute_* 工具，data_query_* 不再过此 hook。"""
    return tool_name.startswith("query_") or tool_name.startswith("compute_")
```

```python
def _build_redirect_decision(
    tool_name: str,
    query: str,
    tool_params: dict[str, Any],
) -> HookDecision:
    """构造 redirect HookDecision，将调用转发到同 scope 的 data_query_{code}。"""
    # 从 query_Enterprise 或 compute_Enterprise 提取 scope_code = Enterprise
    prefix = "query_" if tool_name.startswith("query_") else "compute_"
    scope_code = tool_name[len(prefix):]
    data_query_tool = f"data_query_{scope_code}"

    context_knowledge = _build_context_knowledge(tool_params)
    return {
        "action": "redirect",
        "tool": data_query_tool,
        "params": {
            "query": query,
            "contextKnowledge": context_knowledge,
        },
    }


def _build_context_knowledge(tool_params: dict[str, Any]) -> str:
    """将已填写的标准参数格式化为上下文提示，帮助 PlanAgent 生成更准确的计划。"""
    lines: list[str] = []
    filters = tool_params.get("filters") or []
    if filters:
        lines.append("已确定的过滤条件：")
        for f in filters:
            lines.append(f"  - {f.get('field')} {f.get('op')} {f.get('value')}")
    complex_conds = tool_params.get("complex_conditions") or []
    if complex_conds:
        lines.append("需要计划处理的复杂条件：")
        for c in complex_conds:
            lines.append(f"  - {c}")
    return "\n".join(lines)
```

#### 3.4.3 双通道歧义判断与术语映射（Step 2）

**Step 2 的双重职责**：

> Step 2 不仅是"检测歧义"，更是"完成术语 → field_code 的替换"。  
> 原因：LLM 填的是中文业务术语，但 `QueryExecutor` / `VirtualActionValidator` 要求所有字段参数必须是 field_code。Step 2 **必须在放行前完成替换**，否则执行层报 `UNSUPPORTED_FIELD`。

| 输出状态 | 含义 | 必须执行的动作 |
|---------|------|-------------|
| `CLEAR` | 所有术语 1:1 唯一映射到 field_code | **替换中文术语为 field_code**，放行给 Executor |
| `AUTO_DISAMBIG` | 多候选但 LLM 可从上下文唯一确认 | **替换中文术语为已确认的 field_code**，放行给 Executor |
| `NEED_CONFIRM` | 存在真实歧义，无法自动确认 | 触发 `interrupt` 追问，resume 后再替换，再放行 |

---

##### 术语集来源：OntologyLoader 本地字段目录

术语映射**不依赖 `whale_datacloud.term_name` 数据库**，而是直接从 `OntologyLoader` 的内存模型读取，保证 < 100ms 的纯本地查找。

**数据模型层级**（已有，无需新增）：

```
OntologyLoader._classes: dict[str, OntologyClass]   # key = object_code
  └── OntologyClass.fields: list[OntologyField]
        ├── field_code: str          # 字段编码，如 "revenue"
        ├── field_name: str          # 字段中文名，如 "营收"
        └── aliases: list[str]       # 字段别名，如 ["总营收", "营业收入"]

OntologyLoader._scenes: dict[str, dict]              # key = view_id
  └── scene["fields"]: list[ViewFieldMeta]           # 注入后的视图字段
        ├── property_code: str       # 视图字段编码，如 "avg_revenue"
        └── property_name: str       # 视图字段中文名，如 "平均营收"
        # ViewFieldMeta 当前无 aliases 字段
```

**术语集构建规则**：

对于 Object（`query_Enterprise` → scope_code = `Enterprise`）：

```
术语集 = {
    field_code  → field_code     (field_code 本身，原样匹配)
    field_name  → field_code     (中文名 → 编码)
    alias_i     → field_code     (每个别名 → 同一编码)
}
```

对于 View（`query_scene_xxx` → scope_code = `scene_xxx`）：

```
术语集 = {
    property_code → property_code
    property_name → property_code
}
# 视图字段走 ViewFieldMeta，当前无 aliases，仅 property_name 作为中文术语
```

**新增访问接口：`_get_field_catalog(tool_name)`**

`OntologyLoader` 实例已通过 `AgentConfig.extra["loader"]` 保存，并在 `create_agent()` 时传入 `state`。`tool_wrapper.py` 构建 `HookContext` 时，`state` 是可访问的——需要**新增一步**：将 loader 写入 `HookContext.metadata`：

```python
# tool_wrapper.py — 构建 HookContext 时（第 466-476 行附近）
ctx: HookContext = {
    "tool_name": tool_name,
    "tool_params": dict(raw_params),
    "session_id": str(state.get("agent_id") or ""),
    "user_query": str(state.get("user_query") or ""),
    "workspace_dir": state.get("workspace_dir"),
    "knowledge_snippets": list(state.get("knowledge_snippets") or []),
    "term_context": list(state.get("confirmed_terms") or []),
    "knowledge_payload": dict(state.get("knowledge_payload") or {}),
    # ── 新增：将 OntologyLoader 注入 metadata，供 hook 插件读取 ──
    "metadata": {
        "loader": state.get("loader"),  # OntologyLoader 实例（可能为 None）
    },
}
```

> `state["loader"]` 由 `init_agent_conf.py` 通过 `AgentConfig.extra["loader"]` → `create_agent()` 写入，路径已存在（第 604 行）。

`query_clarification_plugin.py` 中读取：

```python
def _get_field_catalog(
    tool_name: str,
    ctx: HookContext,
) -> dict[str, str]:
    """从 OntologyLoader 读取当前工具的字段目录。

    返回：{ 术语（field_code / field_name / alias） → field_code } 的平铺映射。
    Object 字段含 aliases；View 字段仅含 property_name（ViewFieldMeta 无 aliases）。

    若 loader 不可用（测试/降级），返回空字典，由调用方决定是否跳过映射。
    """
    from datacloud_data_sdk.ontology.loader import OntologyLoader  # noqa: PLC0415
    from datacloud_data_sdk.virtual_action.models import ViewFieldMeta  # noqa: PLC0415

    loader: OntologyLoader | None = (ctx.get("metadata") or {}).get("loader")
    if loader is None:
        return {}

    # 从 tool_name 提取 scope_code
    if tool_name.startswith("query_"):
        scope_code = tool_name[len("query_"):]
    elif tool_name.startswith("compute_"):
        scope_code = tool_name[len("compute_"):]
    else:
        return {}

    catalog: dict[str, str] = {}

    # ── 尝试 Object ────────────────────────────────────────────────────────
    try:
        ont_class = loader.get_ontology_class(scope_code)
        for f in ont_class.fields:
            fc = f.field_code
            catalog[fc] = fc                        # field_code 本身
            if f.field_name:
                catalog[f.field_name] = fc          # 中文名
            for alias in (f.aliases or []):
                if alias:
                    catalog[alias] = fc             # 每个别名
        return catalog
    except Exception:
        pass  # 不是 Object，尝试 View

    # ── 尝试 View ─────────────────────────────────────────────────────────
    try:
        view = loader.get_view(scope_code)
        for f in (view.fields or []):
            if not isinstance(f, ViewFieldMeta):
                continue
            pc = f.property_code
            catalog[pc] = pc                        # property_code 本身
            if f.property_name:
                catalog[f.property_name] = pc       # 中文名
        return catalog
    except Exception:
        pass

    return {}
```

---

**`AmbiguityResult` 携带已替换参数**：

```python
@dataclass
class AmbiguityResult:
    status: Literal["CLEAR", "AUTO_DISAMBIG", "NEED_CONFIRM"]
    # 无论 CLEAR 还是 AUTO_DISAMBIG，resolved_params 均包含中文术语已替换为 field_code 的完整参数
    resolved_params: dict[str, Any] | None = None
    # 仅 NEED_CONFIRM 时有值，供 interrupt 追问
    paradigm_list: list[dict[str, Any]] | None = None
```

**主判断逻辑**：

```python
async def _judge_ambiguity(
    query: str,
    tool_name: str,
    tool_params: dict[str, Any],
    ctx: HookContext,
) -> AmbiguityResult:
    """双通道歧义判断 + 术语 → field_code 替换。

    通道 1：参数直接检测（< 100ms，纯本地查找）
      - 从 OntologyLoader 读取当前对象/视图字段目录（field_code / field_name / aliases）
      - 遍历 filters.field / filters.value / select / dimensions.field /
        metrics.field / order_by.field
      - 全部 1:1 → CLEAR + resolved_params（中文已替换为 field_code）
      - 有多候选或无对应 → 进入通道 2

    通道 2：query 文本补充分析（~1s，1次 LLM 调用）
      - 仅在通道 1 有多候选时触发
      - expand_query(query) → NatQuery → 五段式关键词
      - 尝试从上下文唯一确定多候选项
      - 能唯一确定 → AUTO_DISAMBIG + resolved_params
      - 无法确定 → NEED_CONFIRM + paradigm_list
    """

    # ── 通道 1：参数遍历检测 + 映射 ──────────────────────────────────────────
    mapping_result = _resolve_terms_from_params(tool_params, tool_name, ctx)

    if mapping_result.all_resolved:
        # 全部 1:1 映射，completed_params 已将中文替换为 field_code
        return AmbiguityResult(
            status="CLEAR",
            resolved_params=mapping_result.completed_params,
        )

    # ── 通道 2：query 文本补充分析 ────────────────────────────────────────────
    if not query:
        return AmbiguityResult(status="NEED_CONFIRM", paradigm_list=mapping_result.ambiguous_items)

    try:
        import asyncio  # noqa: PLC0415
        from datacloud_knowledge.intent import expand_query, natquery_to_five_stage  # type: ignore[import]

        nat_query = await asyncio.to_thread(expand_query, query)
        if nat_query is None:
            return AmbiguityResult(status="NEED_CONFIRM", paradigm_list=mapping_result.ambiguous_items)

        five_stage = natquery_to_five_stage(nat_query)
        auto_resolved = _try_resolve_ambiguous(
            five_stage, mapping_result.ambiguous_items, mapping_result.completed_params
        )
        if auto_resolved is not None:
            return AmbiguityResult(status="AUTO_DISAMBIG", resolved_params=auto_resolved)

        return AmbiguityResult(status="NEED_CONFIRM", paradigm_list=mapping_result.ambiguous_items)
    except Exception as exc:
        logger.warning("[before_callback] channel-2 expand_query failed: %s", exc)
        return AmbiguityResult(status="NEED_CONFIRM", paradigm_list=mapping_result.ambiguous_items)
```

**术语映射核心函数**：

```python
@dataclass
class MappingResult:
    all_resolved: bool                        # True = 全部术语已唯一映射
    completed_params: dict[str, Any]          # 已将确定术语替换为 field_code 的参数
    ambiguous_items: list[dict[str, Any]]     # 尚未确定的多候选项（供通道 2 / interrupt 使用）


def _resolve_terms_from_params(
    tool_params: dict[str, Any],
    tool_name: str,
    ctx: HookContext,
) -> MappingResult:
    """遍历所有参数，将可确定的中文术语替换为 field_code，收集多候选项。

    字段目录从 OntologyLoader 本地内存读取（< 100ms，无 DB 调用）。
    目录结构：{ 术语字符串 → field_code }，包含 field_code 本身、field_name、aliases。

    处理范围：
      filters[].field / filters[].value（枚举映射）
      select[]
      order_by[].field
      dimensions[].field（compute_*）
      metrics[].field / metrics[].expr 中的字段词（compute_*）
      metrics[].filters[].field / value（compute_*）
    """
    # 从 OntologyLoader 读取当前对象/视图的字段目录
    field_catalog = _get_field_catalog(tool_name, ctx)

    params = _deep_copy(tool_params)
    ambiguous: list[dict[str, Any]] = []

    # 处理 select
    params["select"] = _map_field_list(params.get("select") or [], field_catalog, ambiguous)

    # 处理 filters
    params["filters"] = _map_filters(params.get("filters") or [], field_catalog, ambiguous)

    # 处理 order_by
    params["order_by"] = _map_order_by(params.get("order_by") or [], field_catalog, ambiguous)

    # 处理 dimensions（compute_* 专用）
    params["dimensions"] = _map_dimensions(params.get("dimensions") or [], field_catalog, ambiguous)

    # 处理 metrics（compute_* 专用，含 expr 和 metrics.filters）
    params["metrics"] = _map_metrics(params.get("metrics") or [], field_catalog, ambiguous)

    return MappingResult(
        all_resolved=len(ambiguous) == 0,
        completed_params=params,
        ambiguous_items=ambiguous,
    )


def _map_term_to_field_code(
    term: str,
    field_catalog: dict[str, str],
    ambiguous: list[dict[str, Any]],
    context: str = "",
) -> str:
    """将单个术语映射到 field_code。

    field_catalog = { 术语（field_code / field_name / alias） → field_code }

    - term 在 catalog 中有唯一对应 → 返回 field_code（含别名命中）
    - term 不在 catalog 中 → 加入 ambiguous（A3：无对应），返回原始 term
    （catalog 已展平，不存在 1:N 问题；多义来源于 LLM 填写了无法定位的词）
    """
    if term in field_catalog:
        return field_catalog[term]  # 命中：field_code / field_name / alias 均可

    # 未命中：无法映射
    ambiguous.append({
        "term": term,
        "context": context,
        "candidates": [],
        "type": "A3_not_found",
    })
    return term  # 暂时保留原始术语，等通道 2 / interrupt 处理
```

> **为什么 catalog 展平后不存在 1:N？**  
> `_get_field_catalog` 按 `field_name → field_code` 构建，若两个字段的 `field_name` 相同（极少见），后者会覆盖前者。真实的字段歧义（同一中文名对应多个 field_code）极少出现在同一对象内；跨对象的歧义已经由 Agent 选工具阶段解决（工具即对象边界）。  
> 若确实发生同名冲突，`A3_not_found` 会触发通道 2，由 `expand_query` + 上下文进一步消歧。

**主流程中的替换写回**：

```python
# before_call_back 主流程（3.4.1）中，Step 2 完成后：

if ambiguity.status in ("CLEAR", "AUTO_DISAMBIG"):
    # resolved_params 中中文术语已替换为 field_code，直接写回 ctx
    ctx["tool_params"] = ambiguity.resolved_params

    if is_complex:
        return _build_redirect_decision(tool_name, query, ambiguity.resolved_params)
    else:
        return {"action": "patch", "patch": {"tool_params": ambiguity.resolved_params}}

# NEED_CONFIRM：中断前不替换（等用户确认后再替换）
```

##### 本节涉及的新增改动汇总

| 改动 | 文件 | 说明 |
|------|------|------|
| `HookContext.metadata` 写入 `loader` | `tool_wrapper.py` 第 466 行附近 | 构建 ctx 时追加 `metadata={"loader": state.get("loader")}` |
| `_get_field_catalog(tool_name, ctx)` | `query_clarification_plugin.py` | 新增函数，从 loader 读 Object/View 字段目录，返回平铺 `dict[str, str]` |
| `_resolve_terms_from_params` 签名 | `query_clarification_plugin.py` | 增加 `ctx: HookContext` 参数，传给 `_get_field_catalog` |
| `_judge_ambiguity` 签名 | `query_clarification_plugin.py` | 同上，透传 `ctx` |

#### 3.4.4 歧义澄清接口（预留 TODO）

根据 2.2.3 节"歧义澄清流程"的开发规划，澄清接口先以固定值占位，待实现后替换：

```python
@dataclass
class ClarificationResult:
    needs_clarification: bool
    paradigm_list: list[dict[str, Any]]
    context_knowledge: str = ""


async def _analyze_clarification(
    query: str,
    tool_name: str,
    tool_params: dict[str, Any],
) -> ClarificationResult:
    """调用 datacloud_knowledge 澄清接口，返回追问表单和内部元数据。

    根据 tool_name 前缀路由到对应接口：
      - query_*   → analyze_query_clarification_query
      - compute_* → analyze_query_clarification_compute

    Returns:
        ClarificationResult:
          .needs_clarification = True  → 需要追问用户
          .paradigm_list       = 追问表单的 paradigmList（供 interrupt payload 使用）
          .context_knowledge   = 内部元数据 JSON（format 步骤必需，随 interrupt payload 保存）
    """
    import asyncio  # noqa: PLC0415
    import json as _json  # noqa: PLC0415

    from datacloud_knowledge.intent.clarification.api import (  # type: ignore[import]  # noqa: PLC0415
        analyze_query_clarification_compute,
        analyze_query_clarification_query,
    )

    is_compute = tool_name.startswith("compute_")
    prefix = "compute_" if is_compute else "query_"
    scope_code = tool_name[len(prefix):]

    try:
        if is_compute:
            sdk_result = await asyncio.to_thread(
                analyze_query_clarification_compute,
                query,
                scope_code,
                tool_params,
            )
        else:
            sdk_result = await asyncio.to_thread(
                analyze_query_clarification_query,
                query,
                scope_code,
                tool_params,
            )
    except Exception as exc:
        logger.warning("[before_callback] _analyze_clarification failed: %s", exc)
        return ClarificationResult(needs_clarification=True, paradigm_list=[], context_knowledge="")

    if not sdk_result.needs_clarification:
        # AUTO_DISAMBIG：SDK 内部已消歧，无需追问
        return ClarificationResult(
            needs_clarification=False,
            paradigm_list=[],
            context_knowledge=sdk_result.knowledge,
        )

    # 解析 form JSON → paradigm_list（供 interrupt payload 的 ask_user_payload 使用）
    paradigm_list: list[dict[str, Any]] = []
    try:
        form_data = _json.loads(sdk_result.form or "{}")
        paradigm_list = list(form_data.get("paradigmList") or [])
    except Exception:
        logger.warning("[before_callback] _analyze_clarification: invalid form JSON, tool=%s", tool_name)

    return ClarificationResult(
        needs_clarification=True,
        paradigm_list=paradigm_list,
        context_knowledge=sdk_result.knowledge,  # 随 interrupt payload 暗字段保存
    )
```

#### 3.4.5 恢复后参数写回

```python
def _apply_resume(
    ctx: HookContext,
    tool_name: str,
    resume_value: Any,
    query: str,
    is_complex: bool,
) -> None:
    """将用户 paradigm 选择写回 tool_params，确保恢复后通道 1 命中 CLEAR。

    调用 format_clarification_query / format_clarification_compute 将用户选择
    应用到 structured_query / structured_compute，返回字段均为 field_code。

    resume_value 结构（前端回传）：
        {
            "paradigmList": [{...已填 choiceKeyword/choiceField/choiceValue...}],
            "_clarify_knowledge": "...",  ← 随 interrupt payload 原样回传（format 必需）
            "_clarify_query":     "...",  ← 随 interrupt payload 原样回传（备用）
        }

    路由说明：
        SIMPLE（is_complex=False）：updated_params 直接用于 query_{code} / compute_{code} OQL 执行
        COMPLEX（is_complex=True）：updated_params 被序列化为 contextKnowledge，随 redirect 传给 data_query_{code}
        两条路径的路由决策由 before_callback 主流程 Step 1 完成，_apply_resume 只负责写回参数。
    """
    import json as _json  # noqa: PLC0415

    from datacloud_knowledge.intent.clarification.api import (  # type: ignore[import]  # noqa: PLC0415
        format_clarification_compute,
        format_clarification_query,
    )

    # 从 resume_value 提取暗字段
    knowledge = ""
    form_dict: dict[str, Any] = {}
    if isinstance(resume_value, dict):
        knowledge = str(resume_value.get("_clarify_knowledge") or "")
        # 过滤暗字段（以 _ 开头），只保留用户选择部分
        form_dict = {k: v for k, v in resume_value.items() if not k.startswith("_")}

    form_str = _json.dumps(form_dict, ensure_ascii=False)
    is_compute = tool_name.startswith("compute_")
    structured_params = dict(ctx.get("tool_params") or {})

    try:
        if is_compute:
            updated = format_clarification_compute(query, structured_params, form_str, knowledge)
        else:
            updated = format_clarification_query(query, structured_params, form_str, knowledge)

        ctx["tool_params"] = updated
        logger.info(
            "[before_callback] _apply_resume: format ok, is_compute=%s is_complex=%s tool=%s",
            is_compute, is_complex, tool_name,
        )
    except Exception as exc:
        logger.warning(
            "[before_callback] _apply_resume: format_* failed, fallback to query-only. tool=%s err=%s",
            tool_name, exc,
        )
        # 降级：透传 query，让 data_query 路径兜底（format 失败不中断主流程）
        ctx["tool_params"] = {"query": query}

    # COMPLEX 路径的 redirect 由 before_callback 主流程根据 is_complex 决定，此处无需处理
```

---

### 3.5 redirect HookDecision 扩展

#### 3.5.1 types.py 扩展

**文件**：`src/datacloud_analysis/tool_hook_plugins/types.py`

在 `HookAction` 联合类型中增加 `"redirect"` 值：

```python
# 当前（第 8 行）
HookAction = Literal["continue", "patch", "short_circuit", "interrupt", "fail", "recover"]

# 改为
HookAction = Literal["continue", "patch", "short_circuit", "interrupt", "fail", "recover", "redirect"]
```

在 `HookDecision` TypedDict 中增加 redirect 专用字段：

```python
class HookDecision(TypedDict, total=False):
    action: HookAction
    patch: HookPatch
    result: HookResult
    interrupt: HookInterrupt
    audit: HookAudit

    # redirect 专用字段（action="redirect" 时有效）
    tool: str          # 目标工具名，如 data_query_Enterprise
    params: dict[str, Any]  # 目标工具的调用参数

    # Legacy fields（kept for compatibility）
    ...
```

#### 3.5.2 tool_wrapper.py 扩展

**文件**：`src/datacloud_analysis/orchestration/execution/tool_wrapper.py`

在 `_run_tool()` 函数的 hook 决策处理段（当前第 499-507 行）中，在 `"fail"` 分支后增加 `"redirect"` 分支：

```python
if before_decision:
    action = str(before_decision.get("action") or "")
    if action == "short_circuit":
        result_payload = before_decision.get("result") or {}
        ctx["tool_output"] = result_payload.get("tool_output", "（short_circuit）")
        ctx["tool_error"] = None
        return
    if action == "fail":
        raise ToolHookError(before_decision)
    if action == "redirect":
        # ── 新增：redirect 分支 ──────────────────────────────────────────
        redirect_tool_name: str = str(before_decision.get("tool") or "")
        redirect_params: dict[str, Any] = dict(before_decision.get("params") or {})
        redirect_tool = tools_map.get(redirect_tool_name)
        if redirect_tool is None:
            logger.warning(
                "dispatch_tool: redirect target '%s' not found in tools_map",
                redirect_tool_name,
            )
            ctx["tool_output"] = None
            ctx["tool_error"] = {
                "error_type": "RedirectToolNotFound",
                "message": f"redirect 目标工具 '{redirect_tool_name}' 未挂载",
            }
            return
        # 替换 ctx 中的工具信息，调用目标工具
        # 注意：redirect 后不再触发 before_callback（此处直接调用，跳过 hook 链）
        ctx["tool_name"] = redirect_tool_name
        ctx["tool_params"] = redirect_params
        logger.info(
            "dispatch_tool: redirect %s -> %s params=%s",
            tool_name, redirect_tool_name, list(redirect_params.keys()),
        )
        # 直接调用目标工具（bypass hook，避免递归）
        try:
            output = await _invoke_tool(redirect_tool, redirect_params, ctx, gateway_context)
            ctx["tool_output"] = output
            ctx["tool_error"] = None
        except Exception as exc:
            logger.exception("dispatch_tool: redirect tool '%s' raised", redirect_tool_name)
            ctx["tool_output"] = None
            ctx["tool_error"] = {
                "error_type": "RedirectToolError",
                "message": str(exc),
            }
        return
```

其中 `_invoke_tool` 是对实际工具调用的抽取（复用已有逻辑，见 519-560 行的 try 块，将其提取为独立函数以支持复用）：

```python
async def _invoke_tool(
    tool: Any,
    params: dict[str, Any],
    ctx: HookContext,
    gateway_context: Any,
) -> Any:
    """直接调用工具可调用体，不经过 hook 链（用于 redirect 场景）。"""
    # 实现与当前 519-560 行的实际工具调用逻辑相同，
    # 抽取为独立函数以便 redirect 和正常路径复用。
    ...
```

> **实现建议**：将现有 519-560 行的 try 块重构为 `_invoke_tool()` 辅助函数，redirect 分支和正常路径均调用此函数，避免代码重复。

---

### 3.6 react_loop.py 恢复机制调整

**文件**：`src/datacloud_analysis/orchestration/execution/react_loop.py`

**目标**：将 resume replay 时的"剥除三个旧元字段"逻辑，替换为"将用户 paradigm 选择应用到 tool_params"。

#### 3.6.1 当前状态（第 836-844 行）

```python
for tc in pending_tool_calls:
    _tc_args = dict(tc.get("args") or {})
    # 剥除旧版元字段
    _tc_args.pop("ambiguous_params", None)
    _tc_args.pop("intent_reason", None)
    _tc_args.pop("extraction_confidence", None)
    tc = {**tc, "args": _tc_args}
    tool_id, result = await dispatch_tool(tc, tools_map, state, ...)
```

#### 3.6.2 改造后（新增 resume_value 读取和 tool_params 更新）

```python
# 从 LangGraph State 获取用户的 resume 选择（interrupt 后用户提交的内容）
_resume_value = state.get("react_resume_value")

for tc in pending_tool_calls:
    _tc_args = dict(tc.get("args") or {})
    # 兼容旧版：仍然剥除旧元字段（过渡期保留）
    _tc_args.pop("ambiguous_params", None)
    _tc_args.pop("intent_reason", None)
    _tc_args.pop("extraction_confidence", None)

    # 新机制：将用户 paradigm 选择写回 tool_params
    # before_callback 的 _apply_resume 已将 ctx["tool_params"] 更新，
    # 此处 _tc_args 即为 State 中保存的原始参数（中断前的 args），
    # 需要将 resume 选择合并进来，确保通道 1 检测 CLEAR
    if _resume_value is not None:
        _selected = _resume_value.get("paradigmList", []) if isinstance(_resume_value, dict) else []
        # 将已确认的 field_code 写回 filters（覆盖中文术语）
        _tc_args = _merge_resume_to_args(_tc_args, _selected)
        _resume_value = None  # 只对第一个 pending tool call 应用（通常只有一个）

    tc = {**tc, "args": _tc_args}
    tool_id, result = await dispatch_tool(tc, tools_map, state, ...)
```

```python
def _merge_resume_to_args(
    args: dict[str, Any],
    selected: list[dict[str, Any]],
) -> dict[str, Any]:
    """将用户 paradigm 选择合并回 tool_params args。

    将 filters 中字段名为中文术语的项，替换为用户选择的 field_code 和确认值。
    """
    if not selected:
        return args

    args = dict(args)
    filters: list[dict[str, Any]] = list(args.get("filters") or [])
    updated: list[dict[str, Any]] = []

    # 构建 term→field_code 映射（来自用户选择）
    term_to_field: dict[str, str] = {}
    term_to_value: dict[str, Any] = {}
    for item in selected:
        term = str(item.get("name") or item.get("termName") or "")
        field_code = str(item.get("fieldName") or item.get("field_code") or "")
        value = item.get("selectedValue") or item.get("value")
        if term and field_code:
            term_to_field[term] = field_code
            if value is not None:
                term_to_value[term] = value

    for f in filters:
        field = str(f.get("field") or "")
        resolved_code = term_to_field.get(field, field)  # 若无映射则保持原值
        resolved_value = term_to_value.get(field, f.get("value"))
        updated.append({**f, "field": resolved_code, "value": resolved_value})

    args["filters"] = updated
    return args
```

> **说明**：`react_resume_value` 是需要在 LangGraph State schema 中新增的字段，用于承载用户 resume 时提交的内容。具体由 LangGraph 框架层将 `Command(resume=...)` 的值写入 State，再由 react_loop 读取。

---

### 3.7 废弃 DATACLOUD_ONTOLOGY_LOAD_MODE（init_agent_conf.py + prompts.py）

**背景**：`DATACLOUD_ONTOLOGY_LOAD_MODE` 是现有"二选一模式"的核心开关（见 1.1 节），新方案以 `query_*` / `compute_*` 为统一入口、通过 `before_callback` 内部路由复杂查询，**两种模式的能力在同一个入口内完整提供**，环境变量本身不再有意义。

#### 3.7.1 影响范围

| 文件 | 当前行为 | 改后行为 |
|------|---------|---------|
| `byclaw_data/plugins/worker_plugins/init_agent_conf.py` | 读取变量，进入 `db_query` 或 `ontology_query` 两个分支 | 固定走 `ontology_query` 分支逻辑，删除 `db_query` 分支 |
| `datacloud_analysis/i18n/prompts.py` | `_get_query_tool_hint_zh/en()` 根据变量返回不同提示段 | 固定返回 `query_*` / `compute_*` 命名规则段，删除 `db_query` 分支 |

#### 3.7.2 init_agent_conf.py 改造

**文件**：`byclaw-data/src/byclaw_data/plugins/worker_plugins/init_agent_conf.py`

当前第 473-528 行是模式分支的核心逻辑：

```python
# ── 当前：模式分支（需删除）─────────────────────────────────────────────────
load_mode = self._ontology_load_mode()
if load_mode == "db_query":
    db_loader = self._build_shared_loader(rel_resource_list)
    db_query_tools = self._build_db_query_tools(...)
    dynamic_tools = {**dynamic_tools, **db_query_tools}
    mounted_objects = [...]   # 从 rel_resource_list 收集
    shared_loader = db_loader
    skip_action_families = frozenset({"query", "compute"})   # ← 阻止 query_/compute_* 注册
else:
    mounted_objects = [...]
    shared_loader = self._build_shared_loader(rel_resource_list)
    skip_action_families = frozenset()
```

**改为**（移除 `if/else`，只保留原 `else` 分支，同时删除 `_build_db_query_tools` / `_build_single_db_query_tool` / `_ontology_load_mode` 等 `db_query` 专用方法）：

```python
# ── 新：固定走 ontology_query 路径 ───────────────────────────────────────────
mounted_objects: list[str] = []
for rel in rel_resource_list:
    snapshot = self._rel_resource_snapshot(rel)
    if snapshot["resourceBizType"] in {"OBJECT", "VIEW"} and snapshot["resourceCode"]:
        mounted_objects.append(snapshot["resourceCode"])

shared_loader: Any = None
if mounted_objects and OntologyLoader and LangGraphPlanGenerator and TermLoader:
    shared_loader = self._build_shared_loader(rel_resource_list)

skip_action_families: frozenset[str] = frozenset()
# data_query_* 工具由 before_callback redirect 动态路由，不再静态注册
```

**同时删除的方法**（已无调用方）：
- `_ontology_load_mode()`
- `_build_db_query_tools()`
- `_build_single_db_query_tool()`

> **注意**：`data_query_{code}` 工具**仍然需要保留注册**——`before_callback` 的 `redirect` 决策会从 `tools_map` 中按名查找它。该工具的注册由 `OntologyToolLoader` 在 `ontology_query` 模式下通过 `inject_virtual_actions` 正常生成，**无需额外静态挂载**。

#### 3.7.3 prompts.py 改造

**文件**：`datacloud-analysis/src/datacloud_analysis/i18n/prompts.py`

删除 `_get_query_tool_hint_zh()` 和 `_get_query_tool_hint_en()` 中的 `if/elif` 分支，改为直接返回固定文本：

```python
# ── 改前 ─────────────────────────────────────────────────────────────────────
def _get_query_tool_hint_zh() -> str:
    mode = os.environ.get("DATACLOUD_ONTOLOGY_LOAD_MODE", "").strip()
    if mode == "ontology_query":
        return "## 查询工具命名规则\n- 工具名格式: query_{对象编码} / compute_{对象编码}...\n"
    if mode == "db_query":
        return "## 查询工具命名规则\n- 工具名格式: data_query_{对象编码}...\n"
    return ""

# ── 改后 ─────────────────────────────────────────────────────────────────────
def _get_query_tool_hint_zh() -> str:
    """查询工具命名规则提示（固定为 query_*/compute_* 统一入口）。"""
    return (
        "## 查询工具命名规则\n"
        "- 查询明细数据：query_{对象编码}（如 query_Enterprise）\n"
        "- 分组统计分析：compute_{对象编码}（如 compute_Enterprise）\n"
        "- 禁止调用不含对象编码的裸工具名（如直接调用 query 或 compute）。\n"
        "- 复杂过滤（含排名/百分位/子查询）无需切换工具，填写 complex_conditions 参数，"
        "系统自动路由到全能查询路径。\n"
    )
```

`_get_query_tool_hint_en()` 同理。完成后可删除文件顶部的 `import os`（若无其他处使用）。

#### 3.7.4 `.env` / 部署侧清理

| 位置 | 操作 |
|------|-----|
| `byclaw-data/.env` / 生产环境变量配置 | 删除 `DATACLOUD_ONTOLOGY_LOAD_MODE=...` 配置项 |
| 运维文档 / 部署手册 | 标注该变量已废弃，说明新版本无需配置 |
| CI/CD 环境变量 | 删除相关 secret / 变量注入步骤 |

---

### 3.8 改动汇总与依赖关系

| 模块 | 文件 | 改动类型 | 关键函数/类 | 依赖前提 |
|------|------|----------|------------|---------|
| 参数 Schema | `generator.py` | 新增字段 | `build_query_schema`, `build_compute_schema`, `_msr_item` | 无 |
| 字段注入 | `node.py` | 重命名+修改 | `inject_ambiguity_fields` → `inject_query_fields` | generator.py 完成 |
| Prompt（工具规则） | `prompts.py` | 替换文本段 | `_build_exec_zh` | 无 |
| Prompt（命名规则） | `prompts.py` | 删除分支，固定返回 | `_get_query_tool_hint_zh/en` | 无 |
| Hook 类型 | `types.py` | 新增枚举值+字段 | `HookAction`, `HookDecision` | 无 |
| Hook 实现 | `query_clarification_plugin.py` | 完整重写 | `before_call_back` 及辅助函数，含 `_get_field_catalog` | types.py, generator.py |
| Hook 调度 | `tool_wrapper.py` | 新增分支 + 注入 loader | `_run_tool` redirect 分支；构建 ctx 时追加 `metadata={"loader": state.get("loader")}` | types.py |
| Resume 恢复 | `react_loop.py` | 修改 replay 段 | pending_tool_calls replay 循环 | plugin 改造完成 |
| 模式开关废弃 | `init_agent_conf.py`（byclaw-data） | 删除分支+方法 | `_ontology_load_mode`, `_build_db_query_tools`, `_build_single_db_query_tool` | 无 |

**开发顺序建议**：
1. `types.py` — 无依赖，先完成类型定义
2. `generator.py` — 无依赖，扩展 Schema
3. `prompts.py` — 无依赖，更新两处（工具规则段 + 命名规则段）
4. `node.py` — 依赖 generator.py（字段已在 Schema 中），修改注入逻辑
5. `tool_wrapper.py` — 依赖 types.py：① 新增 loader 注入到 `HookContext.metadata`；② 添加 redirect 分支
6. `query_clarification_plugin.py` — 依赖 types.py 和 tool_wrapper（loader 注入），重写 hook 实现（含 `_get_field_catalog`、TODO 占位）
7. `react_loop.py` — 依赖 plugin 改造，调整 resume 写回逻辑
8. `init_agent_conf.py` — 删除 `db_query` 分支及相关方法，固定走 `ontology_query` 路径
9. **联调验证**：确认 `data_query_{code}` 仍在 `tools_map` 中（由 `OntologyToolLoader` 注册），redirect 路由可正常命中；确认 `state["loader"]` 正确传递到 `HookContext.metadata`

### 3.9 澄清接口规格（datacloud-knowledge SDK）

以下四个接口由 `datacloud-knowledge` 包实现，`query_clarification_plugin.py` 通过 `_analyze_clarification()` 和 `_apply_resume()` 调用。

#### 3.9.1 分析接口

```python
# 包路径：datacloud_knowledge.intent.clarification.api

def analyze_query_clarification_query(
    query: str,                     # 用户原始自然语言查询
    ontology_code: str,             # 本体编码（scope_code，如 "Enterprise"）
    structured_query: dict[str, Any],  # StructuredQuery 的 dict 表示（含 filters/select 等）
    on_event: Callable[[Any], None] | None = None,  # 可选流式回调
) -> ClarificationResult: ...

def analyze_query_clarification_compute(
    query: str,
    ontology_code: str,
    structured_compute: dict[str, Any],  # StructuredCompute（含 dimensions/metrics/filters 等）
    on_event: Callable[[Any], None] | None = None,
) -> ClarificationResult: ...
```

`ClarificationResult`（SDK 层，位于 `datacloud_knowledge.intent.clarification.models`）：

```python
@dataclass(frozen=True, slots=True)
class ClarificationResult:
    query: str                        # 规范化后的查询文本
    needs_clarification: bool = False # True → 存在无法自动消歧的歧义，需要追问用户
    form: str = ""                    # JSON 字符串：'{"paradigmList":[...]}'，透传给前端
    knowledge: str = ""               # JSON 字符串：KnowledgeMeta，format_* 步骤必需的内部元数据
```

内部流程（4步）：
```
Step 1: 术语提取 (extract_terms_query / extract_terms_compute)
Step 2: 知识召回 (_unified_recall，向量检索)
Step 3: LLM 确认 (llm_confirm_structured)
Step 4: 构建 paradigmList (build_paradigm_list)
```

#### 3.9.2 格式化接口

```python
def format_clarification_query(
    query: str,                         # 用户原始查询
    structured_query: dict[str, Any],   # 原始 StructuredQuery dict（中断前的参数）
    form: str,                          # 前端回传的 JSON，格式：'{"paradigmList":[...已填选择...]}'
    knowledge: str,                     # analyze_* 返回的 ClarificationResult.knowledge（内部元数据）
) -> dict[str, Any]: ...                # 返回：所有术语已替换为 field_code 的 StructuredQuery dict

def format_clarification_compute(
    query: str,
    structured_compute: dict[str, Any],
    form: str,
    knowledge: str,
) -> dict[str, Any]: ...                # 返回：StructuredCompute dict（dimensions/metrics/filters 均为 field_code）
```

**调用方（plugin 层）与 SDK 接口的映射关系**：

| plugin 层变量 | SDK 接口参数 | 来源 |
|-------------|------------|------|
| `query` | `query` | before_callback 从 tool_params 中 pop |
| `scope_code`（从 tool_name 提取） | `ontology_code` | `query_Enterprise` → `Enterprise` |
| `dict(ctx["tool_params"])` | `structured_query/compute` | interrupt 前的 tool_params |
| `json.dumps(form_dict)` | `form` | resume_value 去除暗字段后的部分 |
| `resume_value["_clarify_knowledge"]` | `knowledge` | interrupt payload 暗字段，前端原样回传 |

#### 3.9.3 前端约定

前端在提交 resume 时，必须将 interrupt payload 中的暗字段原样包含在 resume body 中：

```json
// interrupt payload（系统发给前端）
{
  "ask_user_payload": {"paradigmList": [...]},
  "_clarify_knowledge": "...",   // 前端保存
  "_clarify_query": "..."        // 前端保存
}

// resume body（前端提交给系统）
{
  "paradigmList": [{...用户已填选择...}],
  "_clarify_knowledge": "...",   // 原样回传（必须）
  "_clarify_query": "..."        // 原样回传（可选，备用）
}
```

`_clarify_knowledge` 丢失时，`format_*` 以空 knowledge 降级运行，可能导致字段映射精度下降，但不中断主流程。



## 4. 验收用例

本章按改动模块分组，每条用例给出：**前提条件、输入、期望行为、不应发生的行为**。  
用例编号格式：`T<模块编号>-<序号>`，与 §3 章节对应。

---

### 4.1 参数 Schema 变更（对应 §3.1 / §3.2）

#### T1-1：query_{code} Schema 包含 query 必填字段

**前提**：调用 `build_query_schema(fields)` 生成 schema。

**验证**：
- `schema["properties"]` 中存在 `"query"` 字段
- `schema["required"]` 列表包含 `"query"`
- `query` 字段的 `description` 包含 "必填" 或 "完整自然语言"
- `schema["properties"]` 中存在 `"complex_conditions"`，类型为 `array`，`items.type` 为 `string`，`default` 为 `[]`

**不应发生**：
- `"intent_reason"` / `"extraction_confidence"` / `"ambiguous_params"` 出现在 `properties` 中

---

#### T1-2：compute_{code} Schema 包含 query 必填字段及 MetricItem 扩展

**前提**：调用 `build_compute_schema(fields)` 生成 schema。

**验证**：
- `schema["required"]` 包含 `"query"`
- `properties["metrics"]["items"]` 中存在 `"expr"` 字段（type: string）
- `properties["metrics"]["items"]` 中存在 `"filters"` 字段（type: array）
- `properties["metrics"]["items"]["required"]` 为 `["agg", "as"]`（不含 `"field"`）

**不应发生**：
- `metrics.items.required` 仍为 `["field", "agg", "as"]`

---

#### T1-3：Schema 字段约束放开，允许填中文

**前提**：调用 `build_query_schema(fields)` 和 `build_compute_schema(fields)`。

**验证（query schema）**：
- `properties["filters"]["items"]["properties"]["field"]` 中**不含** `"const"` 关键字
- `properties["select"]["items"]` 中**不含** `"enum"` 关键字
- `properties["order_by"]["items"]["properties"]["field"]` 中**不含** `"enum"` 关键字
- `properties["select"]` 中存在 `"x-dc-field-catalog"` 扩展字段，内容为 `[{"name": ..., "code": ...}]`

**验证（compute schema）**：
- `dimensions` 字段约束中**不含** `"const"`
- `metrics.items.properties["field"]` 中**不含** `"const"`

---

#### T1-4：inject_query_fields 替代 inject_ambiguity_fields

**前提**：调用 `inject_query_fields(tool)` 注入字段。

**验证**：
- 注入后工具的 `args_schema` 中包含 `query` 和 `complex_conditions` 字段
- 实际调用工具时，传入 `query="xxx"` 和 `complex_conditions=["yyy"]` **不报参数错误**
- 工具底层执行体（coroutine/func）被调用时，`query` 和 `complex_conditions` 已被 **pop**（底层收不到这两个字段）

**不应发生**：
- 底层 `QueryExecutor.execute()` / `ComputeExecutor.execute()` 收到 `query` 或 `complex_conditions` 参数

---

### 4.2 Prompt 变更（对应 §3.3）

#### T2-1：旧元字段规则已从 Prompt 中移除

**前提**：调用 `_build_exec_zh()` 获取 prompt 文本。

**验证**：
- 返回文本中**不含** `intent_reason`
- 返回文本中**不含** `extraction_confidence`
- 返回文本中**不含** `ambiguous_params`
- 返回文本中含 `query` 和 `complex_conditions` 的填写说明
- 返回文本中含关键词"溢出过滤区"或"字面常量"

---

#### T2-2：工具命名规则 Prompt 固定返回，不依赖环境变量

**前提**：分别在未设置、设置 `DATACLOUD_ONTOLOGY_LOAD_MODE=db_query`、设置 `=ontology_query` 三种环境下调用 `_get_query_tool_hint_zh()`。

**验证**：
- 三种情况返回结果**完全一致**
- 返回文本含 `query_{对象编码}` 和 `compute_{对象编码}`
- 返回文本含 `complex_conditions` 相关说明（无需手动切换工具）

**不应发生**：
- 返回文本含 `data_query_` 作为 Agent 主动调用的工具名

---

### 4.3 before_callback Step 1：复杂度判断（对应 §3.4.1 / §3.4.2）

#### T3-1：complex_conditions 非空 → 触发 redirect

**输入**（`query_Enterprise` 工具调用）：
```json
{
  "query": "找出亩产效益后30%的地块上的企业",
  "filters": [{"field": "效能等级", "op": "eq", "value": "龙头"}],
  "complex_conditions": ["亩产效益后30%的地块"]
}
```

**期望行为**：
- `before_call_back` 返回 `{"action": "redirect", "tool": "data_query_Enterprise", "params": {"query": "...", "contextKnowledge": "..."}}`
- `contextKnowledge` 中包含已解析的 filters 信息（效能等级 eq 龙头）
- 原 `query_Enterprise` 执行体**不被调用**
- `data_query_Enterprise` 被调用，接收 `query` + `contextKnowledge`

---

#### T3-2：complex_conditions 为空列表 → 不触发 redirect

**输入**：
```json
{
  "query": "查询本月新增企业",
  "filters": [{"field": "创建日期", "op": "between", "value": ["2026-04-01", "2026-04-30"]}],
  "complex_conditions": []
}
```

**期望行为**：
- Step 1 判断 `is_complex = False`
- 进入 Step 2 歧义判断
- 不返回 redirect 决策

---

#### T3-3：complex_conditions 字段缺失（兼容旧调用） → 视为空列表

**输入**（不含 complex_conditions 字段）：
```json
{
  "query": "查询本月新增企业",
  "select": ["企业名称"]
}
```

**期望行为**：
- `complex_conditions` 默认值为 `[]`，`is_complex = False`
- 正常进入 Step 2，不报错

---

#### T3-4：redirect 目标工具名从 tool_name 正确提取

| 原工具名 | 期望 redirect 目标 |
|---------|-------------------|
| `query_Enterprise` | `data_query_Enterprise` |
| `compute_Enterprise` | `data_query_Enterprise` |
| `query_scene_farmland_analysis` | `data_query_scene_farmland_analysis` |
| `compute_scene_farmland_analysis` | `data_query_scene_farmland_analysis` |

---

#### T3-5：旧元字段在新版 plugin 中被静默剥除（兼容过渡期）

**输入**（旧版 LLM 仍填了元字段）：
```json
{
  "query": "查询龙头企业",
  "intent_reason": "用户想看龙头企业",
  "extraction_confidence": 0.9,
  "ambiguous_params": [],
  "complex_conditions": []
}
```

**期望行为**：
- `intent_reason` / `extraction_confidence` / `ambiguous_params` 从 `tool_params` 中被 pop，不透传给底层
- 流程正常继续，不报错

---

### 4.4 before_callback Step 2：术语映射（对应 §3.4.3）

#### T4-1：字段中文名 1:1 命中 → CLEAR + 替换为 field_code

**前提**：`OntologyLoader` 已加载 `Enterprise` 对象，字段 `revenue`（field_name="营收"）、`created_date`（field_name="创建日期"）。

**输入 tool_params**：
```json
{
  "select": ["企业名称", "营收"],
  "filters": [{"field": "创建日期", "op": "between", "value": ["2026-04-01", "2026-04-30"]}],
  "order_by": [{"field": "营收", "direction": "desc"}]
}
```

**期望行为**：
- `_resolve_terms_from_params` 返回 `MappingResult(all_resolved=True)`
- `completed_params["select"]` = `["enterprise_name", "revenue"]`
- `completed_params["filters"][0]["field"]` = `"created_date"`
- `completed_params["order_by"][0]["field"]` = `"revenue"`
- Step 2 返回 `AmbiguityResult(status="CLEAR", resolved_params=...)`
- `ctx["tool_params"]` 中字段均已替换为 field_code
- 最终 `QueryExecutor.execute()` 接收到的 `filters.field` 全部为 field_code

---

#### T4-2：字段别名命中 → 等同于 field_name 命中，CLEAR

**前提**：字段 `revenue`（field_name="营收"，aliases=["总营收", "营业收入"]）。

**输入**：
```json
{"select": ["总营收"]}
```

**期望行为**：
- `catalog["总营收"]` = `"revenue"`（别名展平进 catalog）
- `completed_params["select"]` = `["revenue"]`
- 状态 CLEAR，不触发歧义流程

---

#### T4-3：field_code 直接填写 → 原样通过，CLEAR

**输入**：
```json
{"select": ["revenue", "enterprise_name"]}
```

**期望行为**：
- `catalog["revenue"]` = `"revenue"`（field_code 本身也在 catalog 中）
- `completed_params["select"]` = `["revenue", "enterprise_name"]`
- 状态 CLEAR

---

#### T4-4：术语在 catalog 中找不到 → A3_not_found，进入通道 2

**输入**：
```json
{"filters": [{"field": "收益能力", "op": "gt", "value": 0}]}
```

**期望行为**：
- `"收益能力"` 不在 catalog → 加入 `ambiguous_items`，type="A3_not_found"
- `MappingResult(all_resolved=False)`
- 触发通道 2（`expand_query`）

---

#### T4-5：loader 不可用时降级处理

**前提**：`ctx["metadata"]["loader"]` 为 `None`（测试环境未挂载 loader）。

**期望行为**：
- `_get_field_catalog` 返回空字典 `{}`
- 所有术语均视为未命中，进入 `ambiguous_items`
- 触发通道 2（或 NEED_CONFIRM）
- **不抛出异常**，不使流程崩溃

---

#### T4-6：View 字段映射（property_code / property_name）

**前提**：`OntologyLoader` 已加载 View `scene_farmland_analysis`，字段 `avg_revenue`（property_name="平均营收"）。

**输入**（`query_scene_farmland_analysis`）：
```json
{"select": ["平均营收"]}
```

**期望行为**：
- 通过 `loader.get_view("scene_farmland_analysis")` 读取 `ViewFieldMeta`
- `catalog["平均营收"]` = `"avg_revenue"`
- `completed_params["select"]` = `["avg_revenue"]`，状态 CLEAR

---

#### T4-7：compute_* 的 dimensions / metrics / metrics.filters 均被替换

**前提**：字段 `industry`（field_name="行业"）、`revenue`（field_name="营收"）、`enterprise_level`（field_name="企业级别"，aliases=["企业等级"]）。

**输入**（`compute_Enterprise`）：
```json
{
  "dimensions": ["行业"],
  "metrics": [
    {"field": "营收", "agg": "sum", "as": "total_revenue"},
    {
      "field": "营收",
      "filters": [{"field": "企业等级", "op": "eq", "value": "龙头"}],
      "agg": "sum",
      "as": "leading_revenue"
    }
  ]
}
```

**期望行为**：
- `completed_params["dimensions"]` = `["industry"]`
- `completed_params["metrics"][0]["field"]` = `"revenue"`
- `completed_params["metrics"][1]["field"]` = `"revenue"`
- `completed_params["metrics"][1]["filters"][0]["field"]` = `"enterprise_level"`（别名 "企业等级" 命中）
- 状态 CLEAR，全部替换完成

---

### 4.5 before_callback Step 2：歧义判断与中断恢复（对应 §3.4.4 / §3.4.5）

#### T5-1：术语找不到 → NEED_CONFIRM → interrupt 触发

**前提**：`"收益能力"` 不在 Enterprise 字段 catalog，通道 2 也无法从 query 上下文确认。

**期望行为**：
- `_judge_ambiguity` 返回 `AmbiguityResult(status="NEED_CONFIRM")`
- `_analyze_clarification` 被调用（当前返回占位值）
- `interrupt(...)` 被调用，payload 中含 `reason_code="PARADIGM_CLARIFICATION"`
- 节点挂起，等待用户响应

---

#### T5-2：中断恢复后 before_callback 不再触发二次中断

**前提**：用户在 interrupt 后选择了"营收 → revenue"，`_apply_resume` 将 `{field: "营收"}` 更新为 `{field: "revenue"}` 写回 `tool_params`。

**期望行为**：
- 节点重放，`before_callback` 再次运行
- 通道 1 检测 `"revenue"` → catalog 命中（field_code 直接匹配），CLEAR
- **不再触发** `interrupt`，不死循环
- 流程正常推进到 Executor 或 redirect

---

#### T5-3：COMPLEX + NEED_CONFIRM 恢复后路由到 data_query

**输入**（中断前）：
```json
{
  "query": "亩产效益后30%地块上的高效企业",
  "filters": [{"field": "效能等级", "op": "eq", "value": "高效"}],
  "complex_conditions": ["亩产效益后30%的地块"]
}
```

**用户选择**：效能等级 → `efficiency_level`，高效 → `high`

**恢复后期望行为**：
- `tool_params["filters"][0]` = `{"field": "efficiency_level", "op": "eq", "value": "high"}`
- `complex_conditions` 仍保留（未被清除）
- Step 1 重新计算：`is_complex = True`
- Step 2 通道 1：`efficiency_level` 在 catalog 中 → CLEAR
- 最终返回 redirect 到 `data_query_Enterprise`

---

### 4.6 redirect HookDecision（对应 §3.5）

#### T6-1：redirect 决策正确分派到目标工具

**前提**：`tools_map` 中已注册 `data_query_Enterprise`。

**输入**：`before_callback` 返回 `{"action": "redirect", "tool": "data_query_Enterprise", "params": {"query": "...", "contextKnowledge": "..."}}`

**期望行为**（`tool_wrapper.py` dispatch 层）：
- `data_query_Enterprise` 被调用，接收 `query` + `contextKnowledge`
- `query_Enterprise` 原工具**不被调用**
- `ctx["tool_name"]` 更新为 `"data_query_Enterprise"`

---

#### T6-2：redirect 目标工具不存在时返回错误，不崩溃

**前提**：`tools_map` 中**未注册** `data_query_Enterprise`。

**期望行为**：
- `ctx["tool_error"]` = `{"error_type": "RedirectToolNotFound", "message": "redirect 目标工具 'data_query_Enterprise' 未挂载"}`
- `ctx["tool_output"]` = `None`
- **不抛出未捕获异常**

---

#### T6-3：redirect 后不再触发 before_callback（避免递归）

**期望行为**：
- `data_query_Enterprise` 被直接调用（`_invoke_tool` 路径），**绕过** hook 链
- before_callback 在 redirect 后**不再被执行**
- 日志中没有第二次 `[before_callback]` 记录（tool=data_query_Enterprise）

---

### 4.7 react_loop resume 恢复机制（对应 §3.6）

#### T7-1：resume 时 _merge_resume_to_args 将用户选择写回 filters

**输入**：
- `pending_tool_call.args["filters"]` = `[{"field": "营收", "op": "gt", "value": 0}]`
- `resume_value["paradigmList"]` = `[{"name": "营收", "fieldName": "revenue", "selectedValue": null}]`

**期望行为**：
- `_merge_resume_to_args` 执行后，`args["filters"]` = `[{"field": "revenue", "op": "gt", "value": 0}]`
- 字段名从中文替换为 field_code，`value` 保持不变

---

#### T7-2：旧元字段在 resume replay 中仍被剥除（兼容）

**前提**：State 中保存的 pending_tool_call.args 含旧版元字段（过渡期可能存在）。

**期望行为**：
- `_tc_args.pop("ambiguous_params", None)` 等三行兼容代码正常执行
- 无 KeyError，流程正常

---

#### T7-3：resume_value 仅对第一个 pending tool call 生效

**前提**：`react_pending_tool_calls` 有 2 条记录（批量工具调用场景）。

**期望行为**：
- 第 1 条工具调用的 args 被 `_merge_resume_to_args` 更新
- 第 2 条工具调用的 args **不被修改**（`_resume_value = None` 后不再应用）

---

### 4.8 模式开关废弃（对应 §3.7）

#### T8-1：init_agent_conf 不再读取 DATACLOUD_ONTOLOGY_LOAD_MODE

**前提**：分别在 `DATACLOUD_ONTOLOGY_LOAD_MODE` 未设置、设置为 `db_query`、设置为 `ontology_query` 三种情况下运行。

**期望行为**：
- 三种情况下，`AgentConfig` 中注册的工具集**完全一致**，均为 `query_*` / `compute_*` 虚拟动作工具
- `skip_action_families` 始终为 `frozenset()`（不跳过任何动作族）
- 不存在 `_build_db_query_tools` / `_ontology_load_mode` 方法

---

#### T8-2：data_query_{code} 仍在 tools_map 中（redirect 可命中）

**前提**：`OntologyToolLoader` 正常加载本体，`inject_virtual_actions` 执行完成。

**期望行为**：
- `tools_map` 中包含 `data_query_{code}` 工具（由 OntologyLoader 的 `data_query` 虚拟动作族自动注册）
- `before_callback` 的 redirect 决策可以在 `tools_map` 中找到目标工具

**不应发生**：
- `tools_map` 中缺少 `data_query_*`，导致所有 redirect 均以 `RedirectToolNotFound` 失败

---

### 4.9 完整流程集成验收用例

本节为 13 条端到端全流程集成用例，覆盖 query_* / compute_* × 简单/复杂 × 无歧义/可自动消歧/需手动确认的全部组合。

**统一测试环境约定**

```
数据源：yizhuang_brain（MySQL 127.0.0.1:3306）

主要对象：
  ads_enterprise_analysis     → 工具 query_ads_enterprise_analysis / compute_ads_enterprise_analysis
  ads_grid_analysis           → 工具 query_ads_grid_analysis / compute_ads_grid_analysis

复杂查询路由至：
  data_query_ads_enterprise_analysis  /  data_query_ads_grid_analysis

关键字段（来自 §5.2 真实本体，_get_field_catalog 期望输出见 §5.4）：
  ads_enterprise_analysis：
    enterprise_name      中文名="企业全称"
    stat_date            中文名="统计日期"        role=period（账期字段）
    industry_name        中文名="行业类型"
    enterprise_level_name 中文名="企业等级"       枚举文本如"龙头企业""骨干企业"
    total_revenue        中文名="企业总营收（万元）"
    total_profit         中文名="企业总利润（万元）"
    total_tax            中文名="企业总缴税（万元）"
    tax_rate             中文名="企业实际税负率（%）"
    energy_efficiency_Index 中文名="企业经济效益等级（高、中、低）"  枚举值"高""中""低"
    manage_grid_name     中文名="所属管理网格名称"
    phy_grid_id          中文名="企业所属物理网格编码"

  ads_grid_analysis：
    phy_grid_id          中文名="物理网格编码"    role=primary_key
    phy_grid_name        中文名="物理网格名称"
    stat_date            中文名="统计日期"
    output_per_mu        中文名="物理网格亩产效益（万元/亩）"  role=formula_metric
    total_revenue        中文名="物理网格总营收（万元）"
    manage_grid_name     中文名="管理网格名称"

歧义说明：
  "营收" → 不在 catalog（中文名为"企业总营收（万元）"，无"营收"短词条）→ A3_not_found
  "利润" → 不在 catalog（中文名为"企业总利润（万元）"）→ A3_not_found
  "效益指标" / "经济效益" → 不在 catalog（中文名含括号说明）→ A3_not_found
  枚举值"优质""高效"等 → 无对应枚举 code → A2_not_found
```

---

#### IT-01：简单 query_ 问题，无歧义，直接查询

**场景**：查询指定统计期内龙头企业明细，所有 field 均使用真实 field_code，直接命中 catalog。

**用户问题**：`"查询2026年3月统计期内的龙头和骨干企业，按总营收降序取前50条"`

**Agent 调用** `query_ads_enterprise_analysis`：
```json
{
  "query": "查询2026年3月统计期内的龙头和骨干企业，按总营收降序取前50条",
  "select": ["enterprise_name", "industry_name", "manage_grid_name", "total_revenue", "enterprise_level_name"],
  "filters": [
    {"field": "stat_date", "op": "eq", "value": "2026-03"},
    {"field": "enterprise_level_name", "op": "in", "value": ["龙头企业", "骨干企业"]}
  ],
  "order_by": [{"field": "total_revenue", "direction": "desc"}],
  "limit": 50,
  "complex_conditions": []
}
```

**执行轨迹**：

```
before_callback
  Step 1：complex_conditions=[] → is_complex=False
  Step 2 通道 1：
    "stat_date"              → stat_date              ✓（field_code 直接命中）
    "enterprise_level_name"  → enterprise_level_name  ✓
    "total_revenue"          → total_revenue          ✓
    "enterprise_name"        → enterprise_name        ✓
    "industry_name"          → industry_name          ✓
    "manage_grid_name"       → manage_grid_name       ✓
    值 "龙头企业"/"骨干企业" → DIMENSION name 型文本，通道1不检查 → 通过
    全部 1:1 → CLEAR
  ⚡ 通道 2 不触发（跳过 expand_query）
  ⚡ analyze_query_clarification_query 不触发
路由：patch → QueryExecutor.execute()
```

**期望结果**：
- 返回企业明细列表，`result_type="normal"`
- before_callback 总耗时 **< 100ms**

**不应发生**：调用 `expand_query`；触发 interrupt；调用 `data_query_ads_enterprise_analysis`

---

#### IT-02：复杂 query_ 问题，无歧义，走 data_query

**场景**：需跨对象关联（企业 JOIN 物理网格）+ PERCENT_RANK 截断，`complex_conditions` 非空；参数无术语歧义。

**用户问题**：`"找出亩产效益后30%的物理网格，查询这些网格上的龙头企业列表"`

**Agent 调用** `query_ads_enterprise_analysis`：
```json
{
  "query": "找出亩产效益后30%的物理网格，查询这些网格上的龙头企业列表",
  "select": ["enterprise_name", "enterprise_level_name", "manage_grid_name", "total_revenue"],
  "filters": [
    {"field": "stat_date", "op": "eq", "value": "2026-03"},
    {"field": "enterprise_level_name", "op": "eq", "value": "龙头企业"}
  ],
  "complex_conditions": ["亩产效益后30%的物理网格（需 JOIN ads_grid_analysis）"]
}
```

**执行轨迹**：

```
before_callback
  Step 1：complex_conditions 非空 → is_complex=True
  Step 2 通道 1：
    "stat_date"             → stat_date             ✓
    "enterprise_level_name" → enterprise_level_name ✓
    "enterprise_name"       → enterprise_name       ✓
    "manage_grid_name"      → manage_grid_name      ✓
    "total_revenue"         → total_revenue         ✓
    全部 1:1 → CLEAR
  ⚡ 通道 2 不触发；⚡ interrupt 不触发
路由：redirect → data_query_ads_enterprise_analysis
  contextKnowledge：
    "已确定的过滤条件：
       - stat_date eq 2026-03
       - enterprise_level_name eq 龙头企业
     需要计划处理的复杂条件：
       - 亩产效益后30%的物理网格（需 JOIN ads_grid_analysis）"
PlanAgent：WITH ranked AS (PERCENT_RANK OVER output_per_mu) → JOIN → 筛龙头企业
```

**期望结果**：
- `data_query_ads_enterprise_analysis` 被调用
- PlanAgent 生成含 `PERCENT_RANK() OVER (ORDER BY output_per_mu DESC)` 和 JOIN 的 SQL
- 返回企业列表，`result_type="normal"`

**不应发生**：`QueryExecutor`（OQL 单表）被调用；interrupt 触发

---

#### IT-03.1：简单 query_ 问题，有歧义，analyze 可自动消歧

**场景**：Agent 填入中文术语"营收"，catalog 无该词条（`total_revenue` 中文名含括号说明，未存短词），触发通道 2；`expand_query` 分析上下文后唯一确认 `total_revenue` → AUTO_DISAMBIG。

**用户问题**：`"查询2026年3月统计期内，总营收超过500万的企业"`

**Agent 调用** `query_ads_enterprise_analysis`：
```json
{
  "query": "查询2026年3月统计期内，总营收超过500万的企业",
  "select": ["enterprise_name", "industry_name", "营收"],
  "filters": [
    {"field": "stat_date", "op": "eq", "value": "2026-03"},
    {"field": "营收", "op": "gt", "value": 5000000}
  ],
  "complex_conditions": []
}
```

**执行轨迹**：

```
before_callback
  Step 1：complex_conditions=[] → is_complex=False
  Step 2 通道 1：
    "stat_date"  → stat_date ✓
    "营收"       → 无命中（catalog 中为"企业总营收（万元）"，无"营收"词条）→ A3_not_found
    MappingResult(all_resolved=False)
  通道 2：analyze_query_clarification_query(query, tool="query_ads_enterprise_analysis")
    expand_query("查询2026年3月...总营收超过500万") → NatQuery
    ads_enterprise_analysis 只有 total_revenue 一个营收指标 → 唯一确认
    → AUTO_DISAMBIG：营收 → total_revenue
  ⚡ interrupt 不触发
路由：patch → 更新 "营收"→"total_revenue" → QueryExecutor.execute()
```

**期望结果**：
- `filters[0]["field"]` 自动更新为 `"total_revenue"`，select 中 `"营收"` 更新为 `"total_revenue"`
- `QueryExecutor` 正常执行，返回结果
- **全程静默**，不打断用户

**不应发生**：interrupt 触发；`data_query_ads_enterprise_analysis` 被调用

---

#### IT-03.2：简单 query_ 问题，有歧义，analyze 无法自动消歧，中断追问

**场景**：`"经济效益好"` 语义模糊，catalog 无 `"经济效益"` 词条，枚举值 `"好"` 不在已知 code（高/中/低），需用户选择。

**用户问题**：`"查询2026年3月统计期内，经济效益好的企业列表"`

**Agent 调用** `query_ads_enterprise_analysis`：
```json
{
  "query": "查询2026年3月统计期内，经济效益好的企业列表",
  "select": ["enterprise_name", "industry_name", "energy_efficiency_Index"],
  "filters": [
    {"field": "stat_date", "op": "eq", "value": "2026-03"},
    {"field": "经济效益", "op": "eq", "value": "好"}
  ],
  "complex_conditions": []
}
```

**执行轨迹**：

```
before_callback
  Step 1：complex_conditions=[] → is_complex=False
  Step 2 通道 1：
    "stat_date"  → stat_date ✓
    "经济效益"   → 无命中（catalog 中为"企业经济效益等级（高、中、低）"）→ A3_not_found
    "好"         → 枚举无"好"code → A2_not_found
    MappingResult(all_resolved=False)
  通道 2：analyze_query_clarification_query
    → "经济效益" 候选：energy_efficiency_Index
    → "好" 候选枚举值：高 / 中 / 低
    → 无法从"好"唯一确认具体枚举值 → NEED_CONFIRM
  interrupt({
    ask_user_payload: {
      paradigmList: [
        {term:"经济效益", candidates:["企业经济效益等级（energy_efficiency_Index）"], context:"过滤字段"},
        {term:"好", candidates:["高","中","低"], context:"energy_efficiency_Index 枚举值"}
      ]
    }
  })
  → 节点挂起
```

**用户选择**：`"经济效益"` → `energy_efficiency_Index`，`"好"` → `"高"`

**恢复后执行轨迹**：

```
_apply_resume：
  filters[1] → {"field": "energy_efficiency_Index", "op": "eq", "value": "高"}
节点重放 → before_callback：
  Step 1：is_complex=False
  Step 2 通道 1：energy_efficiency_Index / stat_date 全部 1:1 ✓ → CLEAR
  ⚡ interrupt 不再触发
路由：QueryExecutor.execute()
```

**期望结果**：
- 用户收到歧义追问，选择后自动继续
- OQL 生成 `WHERE stat_date='2026-03' AND energy_efficiency_Index='高'`，返回企业列表

**不应发生**：第二次触发 interrupt；`data_query_ads_enterprise_analysis` 被调用

---

#### IT-04.1：复杂 query_ 问题，有歧义，analyze 可自动消歧

**场景**：`complex_conditions` 非空（COMPLEX）+ "营收"术语在 catalog 无短词 → 触发通道 2；上下文唯一确认 `total_revenue` → AUTO_DISAMBIG → 完成后 redirect。

**用户问题**：`"找出亩产效益后30%的物理网格，查询这些网格上营收超过100万的企业"`

**Agent 调用** `query_ads_enterprise_analysis`：
```json
{
  "query": "找出亩产效益后30%的物理网格，查询这些网格上营收超过100万的企业",
  "select": ["enterprise_name", "营收", "enterprise_level_name"],
  "filters": [
    {"field": "stat_date", "op": "eq", "value": "2026-03"},
    {"field": "营收", "op": "gt", "value": 1000000}
  ],
  "complex_conditions": ["亩产效益后30%的物理网格（需 JOIN ads_grid_analysis）"]
}
```

**执行轨迹**：

```
before_callback
  Step 1：complex_conditions 非空 → is_complex=True（标记，不立即路由）
  Step 2 通道 1：
    "营收" → 无命中 → A3_not_found
    MappingResult(all_resolved=False)
  通道 2：analyze_query_clarification_query
    → "营收" 唯一确认 total_revenue → AUTO_DISAMBIG
  ⚡ interrupt 不触发
  术语替换：filters[1].field → "total_revenue"，select 中"营收"→"total_revenue"
  is_complex=True → redirect
路由：data_query_ads_enterprise_analysis
  contextKnowledge：
    "已确定的过滤条件：
       - stat_date eq 2026-03
       - total_revenue gt 1000000
     需要计划处理的复杂条件：
       - 亩产效益后30%的物理网格（需 JOIN ads_grid_analysis）"
PlanAgent：PERCENT_RANK 筛物理网格 + JOIN 企业 + 过滤 total_revenue
```

**期望结果**：
- 静默消歧，不打断用户
- `data_query_ads_enterprise_analysis` 被调用，PlanAgent 生成正确 SQL

**不应发生**：interrupt 触发；`QueryExecutor`（OQL）被调用

---

#### IT-04.2：复杂 query_ 问题，有歧义，analyze 无法自动消歧，中断追问

**场景**：`complex_conditions` 非空（COMPLEX）+ 枚举歧义无法自动消歧，用户选择后 `complex_conditions` 保留，正确路由 data_query。

**用户问题**：`"找出亩产效益后30%的物理网格，查询这些网格上经济效益好的企业"`

**Agent 调用** `query_ads_enterprise_analysis`：
```json
{
  "query": "找出亩产效益后30%的物理网格，查询这些网格上经济效益好的企业",
  "select": ["enterprise_name", "enterprise_level_name", "energy_efficiency_Index"],
  "filters": [
    {"field": "stat_date", "op": "eq", "value": "2026-03"},
    {"field": "经济效益", "op": "eq", "value": "好"}
  ],
  "complex_conditions": ["亩产效益后30%的物理网格（需 JOIN ads_grid_analysis）"]
}
```

**执行轨迹**：

```
before_callback
  Step 1：complex_conditions 非空 → is_complex=True
  Step 2 通道 1：
    "经济效益" → 无命中 → A3_not_found
    "好"       → 枚举无此值 → A2_not_found
    MappingResult(all_resolved=False)
  通道 2：analyze_query_clarification_query
    → "经济效益" → energy_efficiency_Index
    → "好" → 候选：高/中/低，上下文不足唯一确认 → NEED_CONFIRM
  interrupt({
    ask_user_payload: {
      paradigmList: [
        {term:"经济效益", candidates:["energy_efficiency_Index"]},
        {term:"好", candidates:["高","中","低"], context:"energy_efficiency_Index 枚举值"}
      ]
    }
  })
  → 节点挂起
```

**用户选择**：`"经济效益"` → `energy_efficiency_Index`，`"好"` → `"高"`

**恢复后执行轨迹**：

```
_apply_resume：
  filters[1] → {"field": "energy_efficiency_Index", "op": "eq", "value": "高"}
  complex_conditions 保留：["亩产效益后30%的物理网格（需 JOIN ads_grid_analysis）"]
节点重放 → before_callback：
  Step 1：complex_conditions 非空 → is_complex=True
  Step 2 通道 1：energy_efficiency_Index / stat_date 全部 1:1 ✓ → CLEAR
  ⚡ interrupt 不再触发
  redirect → data_query_ads_enterprise_analysis
    contextKnowledge：
      "已确定的过滤条件：
         - stat_date eq 2026-03
         - energy_efficiency_Index eq 高
       需要计划处理的复杂条件：
         - 亩产效益后30%的物理网格（需 JOIN ads_grid_analysis）"
PlanAgent：多步 SQL
```

**期望结果**：
- 用户仅被打断一次
- 恢复后 `complex_conditions` 仍保留，正确路由 data_query
- PlanAgent 生成含 PERCENT_RANK + JOIN + 枚举过滤的 SQL

**不应发生**：第二次触发 interrupt；complex_conditions 丢失

---

#### IT-05：简单 compute_ 问题，无歧义，直接统计

**场景**：按行业统计企业数和总营收，所有字段均直接命中 catalog。

**用户问题**：`"统计2026年3月各行业类型的企业数量和总营收"`

**Agent 调用** `compute_ads_enterprise_analysis`：
```json
{
  "query": "统计2026年3月各行业类型的企业数量和总营收",
  "dimensions": ["industry_name"],
  "metrics": [
    {"agg": "count_all", "as": "enterprise_count"},
    {"field": "total_revenue", "agg": "sum", "as": "total_revenue_sum"}
  ],
  "filters": [{"field": "stat_date", "op": "eq", "value": "2026-03"}],
  "order_by": [{"field": "total_revenue_sum", "direction": "desc"}],
  "limit": 50,
  "complex_conditions": []
}
```

**执行轨迹**：

```
before_callback
  Step 1：complex_conditions=[] → is_complex=False
  Step 2 通道 1：
    "industry_name"  → industry_name  ✓
    "total_revenue"  → total_revenue  ✓
    "stat_date"      → stat_date      ✓
    全部 1:1 → CLEAR
  ⚡ 通道 2 不触发；⚡ interrupt 不触发
路由：patch → ComputeExecutor.execute()

期望 SQL（ComputeExecutor 生成）：
SELECT industry_name, COUNT(*) AS enterprise_count, SUM(total_revenue) AS total_revenue_sum
FROM ads_enterprise_analysis
WHERE stat_date = '2026-03'
GROUP BY industry_name
ORDER BY total_revenue_sum DESC LIMIT 50;
```

**期望结果**：
- `ComputeExecutor` 正常生成 SQL 并执行，返回分组统计结果
- before_callback 总耗时 **< 100ms**

**不应发生**：interrupt 触发；`data_query_ads_enterprise_analysis` 被调用

---

#### IT-06：复杂 compute_ 问题，无歧义，走 data_query

**场景**：按管理网格统计亩产效益后30%物理网格的企业平均营收，含 PERCENT_RANK；参数无术语歧义。

**用户问题**：`"统计各管理网格中，亩产效益后30%物理网格上企业的平均总营收"`

**Agent 调用** `compute_ads_grid_analysis`：
```json
{
  "query": "统计各管理网格中，亩产效益后30%物理网格上企业的平均总营收",
  "dimensions": ["manage_grid_name"],
  "metrics": [{"field": "total_revenue", "agg": "avg", "as": "avg_revenue"}],
  "filters": [{"field": "stat_date", "op": "eq", "value": "2026-03"}],
  "complex_conditions": ["亩产效益后30%的物理网格（PERCENT_RANK BY output_per_mu）"]
}
```

**执行轨迹**：

```
before_callback
  Step 1：complex_conditions 非空 → is_complex=True
  Step 2 通道 1：
    "manage_grid_name" → manage_grid_name ✓（ads_grid_analysis 的 catalog）
    "total_revenue"    → total_revenue    ✓
    "stat_date"        → stat_date        ✓
    全部 1:1 → CLEAR
  ⚡ 通道 2 不触发；⚡ interrupt 不触发
路由：redirect → data_query_ads_grid_analysis
  contextKnowledge：
    "已确定的维度：manage_grid_name
     已确定的指标：avg(total_revenue) as avg_revenue
     已确定的过滤条件：stat_date eq 2026-03
     需要计划处理的复杂条件：
       - 亩产效益后30%的物理网格（PERCENT_RANK BY output_per_mu）"

期望 SQL（参考 §5.6 IT-06 模板）：
WITH grid_ranked AS (
    SELECT phy_grid_id, manage_grid_name,
           PERCENT_RANK() OVER (ORDER BY output_per_mu DESC) AS pct_rank
    FROM ads_grid_analysis WHERE stat_date = '2026-03'
),
low_grids AS (SELECT phy_grid_id, manage_grid_name FROM grid_ranked WHERE pct_rank >= 0.70)
SELECT lg.manage_grid_name, AVG(e.total_revenue) AS avg_revenue
FROM ads_enterprise_analysis e JOIN low_grids lg ON e.phy_grid_id = lg.phy_grid_id
WHERE e.stat_date = '2026-03' GROUP BY lg.manage_grid_name ORDER BY avg_revenue DESC;
```

**期望结果**：
- `data_query_ads_grid_analysis` 被调用，PlanAgent 生成正确多步 SQL
- before_callback 通道 1 检查耗时 **< 100ms**

**不应发生**：`ComputeExecutor`（OQL）被调用；interrupt 触发

---

#### IT-07.1：简单 compute_ 问题，有歧义，analyze 可自动消歧

**场景**：Agent 填入"利润"，catalog 无该短词（中文名为"企业总利润（万元）"），触发通道 2；上下文唯一确认 `total_profit` → AUTO_DISAMBIG。

**用户问题**：`"统计2026年3月各行业的平均利润"`

**Agent 调用** `compute_ads_enterprise_analysis`：
```json
{
  "query": "统计2026年3月各行业的平均利润",
  "dimensions": ["industry_name"],
  "metrics": [{"field": "利润", "agg": "avg", "as": "avg_profit"}],
  "filters": [{"field": "stat_date", "op": "eq", "value": "2026-03"}],
  "complex_conditions": []
}
```

**执行轨迹**：

```
before_callback
  Step 1：complex_conditions=[] → is_complex=False
  Step 2 通道 1：
    "industry_name" → industry_name ✓
    "利润"          → 无命中 → A3_not_found
    MappingResult(all_resolved=False)
  通道 2：analyze_query_clarification_compute
    → "利润" → ads_enterprise_analysis 中唯一利润指标 total_profit → AUTO_DISAMBIG
  ⚡ interrupt 不触发
路由：patch → metrics[0].field → "total_profit" → ComputeExecutor.execute()
```

**期望结果**：
- `metrics[0]["field"]` 自动更新为 `"total_profit"`
- `ComputeExecutor` 正常执行，静默消歧，用户无感知

---

#### IT-07.2：简单 compute_ 问题，有歧义，analyze 无法自动消歧，中断追问

**场景**：`"效益指标"` 语义模糊，catalog 无该词，且 `ads_enterprise_analysis` 有多个指标候选（`total_revenue`/`total_profit`/`energy_efficiency_Index`），无法自动确认。

**用户问题**：`"统计2026年3月各行业的平均效益指标"`

**Agent 调用** `compute_ads_enterprise_analysis`：
```json
{
  "query": "统计2026年3月各行业的平均效益指标",
  "dimensions": ["industry_name"],
  "metrics": [{"field": "效益指标", "agg": "avg", "as": "avg_kpi"}],
  "filters": [{"field": "stat_date", "op": "eq", "value": "2026-03"}],
  "complex_conditions": []
}
```

**执行轨迹**：

```
before_callback
  Step 1：complex_conditions=[] → is_complex=False
  Step 2 通道 1：
    "效益指标" → 无命中 → A3_not_found
    MappingResult(all_resolved=False)
  通道 2：analyze_query_clarification_compute
    → "效益指标" 候选：total_revenue（总营收）/ total_profit（总利润）/ energy_efficiency_Index（效益等级）
    → 多候选，上下文无法唯一确认 → NEED_CONFIRM
  interrupt({
    ask_user_payload: {
      paradigmList: [{
        term:"效益指标",
        candidates:["总营收（total_revenue）","总利润（total_profit）","总缴税（total_tax）"],
        context:"compute 统计指标"
      }]
    }
  })
  → 节点挂起
```

**用户选择**：`"效益指标"` → `total_profit`（总利润）

**恢复后执行轨迹**：

```
_apply_resume：
  metrics[0].field → "total_profit"
节点重放 → before_callback：
  Step 1：is_complex=False
  Step 2 通道 1：total_profit / industry_name / stat_date 全部 1:1 ✓ → CLEAR
  ⚡ interrupt 不再触发
路由：ComputeExecutor.execute()
  SQL：SELECT industry_name, AVG(total_profit) AS avg_kpi
       FROM ads_enterprise_analysis WHERE stat_date='2026-03' GROUP BY industry_name
```

**期望结果**：
- 用户仅被打断一次
- 消歧后 ComputeExecutor 正常执行，返回按行业的平均利润统计

**不应发生**：第二次触发 interrupt；`data_query_ads_enterprise_analysis` 被调用

---

#### IT-08.1：复杂 compute_ 问题，有歧义，analyze 可自动消歧

**场景**：`complex_conditions` 非空（COMPLEX）+ "利润"未命中 catalog → 通道 2 唯一确认 `total_profit` → AUTO_DISAMBIG → redirect。

**用户问题**：`"统计各管理网格中，亩产效益前20%物理网格上企业的平均利润"`

**Agent 调用** `compute_ads_enterprise_analysis`：
```json
{
  "query": "统计各管理网格中，亩产效益前20%物理网格上企业的平均利润",
  "dimensions": ["manage_grid_name"],
  "metrics": [{"field": "利润", "agg": "avg", "as": "avg_profit"}],
  "filters": [{"field": "stat_date", "op": "eq", "value": "2026-03"}],
  "complex_conditions": ["亩产效益前20%的物理网格（需 JOIN ads_grid_analysis）"]
}
```

**执行轨迹**：

```
before_callback
  Step 1：complex_conditions 非空 → is_complex=True
  Step 2 通道 1：
    "利润" → 无命中 → A3_not_found
    MappingResult(all_resolved=False)
  通道 2：analyze_query_clarification_compute
    → "利润" → total_profit（唯一利润指标）→ AUTO_DISAMBIG
  ⚡ interrupt 不触发
  术语替换：metrics[0].field → "total_profit"
  is_complex=True → redirect
路由：data_query_ads_enterprise_analysis
  contextKnowledge：
    "已确定的维度：manage_grid_name
     已确定的指标：avg(total_profit) as avg_profit
     已确定的过滤条件：stat_date eq 2026-03
     需要计划处理的复杂条件：
       - 亩产效益前20%的物理网格（需 JOIN ads_grid_analysis）"
PlanAgent：PERCENT_RANK 筛前20% 网格 + JOIN 企业 + AVG(total_profit)
```

**期望结果**：
- 静默消歧，不打断用户
- `data_query_ads_enterprise_analysis` 被调用，PlanAgent 生成含 PERCENT_RANK 的 SQL

---

#### IT-08.2：复杂 compute_ 问题，有歧义，analyze 无法自动消歧，中断追问

**场景**：`complex_conditions` 非空（COMPLEX）+ 多字段歧义无法自动消歧，用户选择后 `complex_conditions` 保留，正确路由 data_query。

**用户问题**：`"统计各管理网格中，亩产效益后30%物理网格上经济效益好的企业的平均效益指标"`

**Agent 调用** `compute_ads_enterprise_analysis`：
```json
{
  "query": "统计各管理网格中，亩产效益后30%物理网格上经济效益好的企业的平均效益指标",
  "dimensions": ["manage_grid_name"],
  "metrics": [{"field": "效益指标", "agg": "avg", "as": "avg_kpi"}],
  "filters": [
    {"field": "stat_date", "op": "eq", "value": "2026-03"},
    {"field": "经济效益", "op": "eq", "value": "好"}
  ],
  "complex_conditions": ["亩产效益后30%的物理网格（需 JOIN ads_grid_analysis）"]
}
```

**执行轨迹**：

```
before_callback
  Step 1：complex_conditions 非空 → is_complex=True
  Step 2 通道 1：
    "效益指标" → 无命中 → A3_not_found
    "经济效益" → 无命中 → A3_not_found
    "好"       → 枚举无此值 → A2_not_found
    MappingResult(all_resolved=False)
  通道 2：analyze_query_clarification_compute
    → "效益指标" 候选：total_revenue / total_profit / total_tax → 多候选 → 无法自动确认
    → "经济效益" → energy_efficiency_Index，"好" → 高/中/低 → 无法自动确认
    → NEED_CONFIRM
  interrupt({
    ask_user_payload: {
      paradigmList: [
        {term:"效益指标", candidates:["总营收（total_revenue）","总利润（total_profit）","总缴税（total_tax）"]},
        {term:"经济效益",  candidates:["企业经济效益等级（energy_efficiency_Index）"]},
        {term:"好",        candidates:["高","中","低"], context:"energy_efficiency_Index 枚举值"}
      ]
    }
  })
  → 节点挂起，两个歧义在同一次 interrupt 合并追问
```

**用户选择**：
- `"效益指标"` → `total_profit`（总利润）
- `"经济效益"` → `energy_efficiency_Index`，`"好"` → `"高"`

**恢复后执行轨迹**：

```
_apply_resume：
  metrics[0].field → "total_profit"
  filters[1] → {"field": "energy_efficiency_Index", "op": "eq", "value": "高"}
  complex_conditions 保留：["亩产效益后30%的物理网格（需 JOIN ads_grid_analysis）"]
节点重放 → before_callback：
  Step 1：complex_conditions 非空 → is_complex=True
  Step 2 通道 1：total_profit / manage_grid_name / stat_date / energy_efficiency_Index 全部 1:1 ✓ → CLEAR
  ⚡ interrupt 不再触发
  redirect → data_query_ads_enterprise_analysis
    contextKnowledge：
      "已确定的维度：manage_grid_name
       已确定的指标：avg(total_profit) as avg_kpi
       已确定的过滤条件：
         - stat_date eq 2026-03
         - energy_efficiency_Index eq 高
       需要计划处理的复杂条件：
         - 亩产效益后30%的物理网格（需 JOIN ads_grid_analysis）"
PlanAgent：多步 SQL（PERCENT_RANK + JOIN + energy_efficiency_Index 过滤 + AVG）
```

**期望结果**：
- 用户仅被打断一次，三个歧义在同一次 interrupt 中合并追问
- 恢复后 `complex_conditions` 仍保留，正确路由 data_query
- PlanAgent 生成精确执行计划，返回统计结果

**不应发生**：第二次触发 interrupt；complex_conditions 丢失；`ComputeExecutor`（OQL）被调用

---

### 4.10 验收矩阵

| 用例 ID | 工具类型 | 复杂度 | 歧义状态 | 消歧路径 | 执行路径 | 优先级 |
|--------|---------|--------|---------|---------|---------|-------|
| T1-1 ~ T1-4 | — | — | — | — | generator.py / node.py 单元测试 | P0 |
| T2-1 ~ T2-2 | — | — | — | — | prompts.py 单元测试 | P1 |
| T3-1 ~ T3-5 | — | — | — | — | query_clarification_plugin Step 1 单元测试 | P0 |
| T4-1 ~ T4-7 | — | — | — | — | _get_field_catalog / _resolve_terms_from_params 单元测试 | P0 |
| T5-1 ~ T5-3 | — | — | — | — | interrupt / resume / NEED_CONFIRM 集成测试 | P0 |
| T6-1 ~ T6-3 | — | — | — | — | tool_wrapper redirect 分支 单元测试 | P0 |
| T7-1 ~ T7-3 | — | — | — | — | react_loop resume replay 单元测试 | P1 |
| T8-1 ~ T8-2 | — | — | — | — | init_agent_conf / tools_map 集成测试 | P1 |
| IT-01 | query_* | SIMPLE | 无歧义 | — | QueryExecutor（OQL） | P0 |
| IT-02 | query_* | COMPLEX | 无歧义 | — | redirect → data_query | P0 |
| IT-03.1 | query_* | SIMPLE | 有歧义 | 自动消歧 | QueryExecutor | P0 |
| IT-03.2 | query_* | SIMPLE | 有歧义 | interrupt → 用户选择 | QueryExecutor | P0 |
| IT-04.1 | query_* | COMPLEX | 有歧义 | 自动消歧 | redirect → data_query | P0 |
| IT-04.2 | query_* | COMPLEX | 有歧义 | interrupt → 用户选择 → redirect | redirect → data_query | P0 |
| IT-05 | compute_* | SIMPLE | 无歧义 | — | ComputeExecutor（OQL） | P0 |
| IT-06 | compute_* | COMPLEX | 无歧义 | — | redirect → data_query | P0 |
| IT-07.1 | compute_* | SIMPLE | 有歧义 | 自动消歧 | ComputeExecutor | P0 |
| IT-07.2 | compute_* | SIMPLE | 有歧义 | interrupt → 用户选择 | ComputeExecutor | P0 |
| IT-08.1 | compute_* | COMPLEX | 有歧义 | 自动消歧 | redirect → data_query | P0 |
| IT-08.2 | compute_* | COMPLEX | 有歧义 | interrupt → 用户选择 → redirect | redirect → data_query | P0 |
| IT-09* | 任意 | — | — | loader=None 降级 | 降级至通道2/NEED_CONFIRM | P1 |

> *IT-09 为兜底容错用例，覆盖 loader 不可用时全流程不崩溃的保障。

**验收通过标准**：
- IT-01 / IT-02 / IT-05 / IT-06（无歧义路径）：before_callback 耗时 **< 100ms**，无 LLM 调用
- IT-03.1 / IT-04.1 / IT-07.1 / IT-08.1（自动消歧路径）：**不触发 interrupt**，用户无感知
- IT-03.2 / IT-04.2 / IT-07.2 / IT-08.2（手动消歧路径）：**仅触发一次 interrupt**，恢复后不再二次中断
- IT-04.2 / IT-08.2（COMPLEX + 手动消歧）：恢复后 **complex_conditions 仍保留**，正确路由到 data_query
- T1~T8 单元/集成用例：P0 全部通过后方可合并；P1 用例允许在合并后 1 个迭代内补充


## 5. 测试相关数据

> 本章记录集成测试所使用的真实数据定义，覆盖数据库连接、4 个对象（Object）、2 个视图（View）及其字段清单，测试用例中的问题、字段编码、期望 SQL 均以此为基准。
>
> **源文件路径**：
> - 对象定义：`byclaw-data/resource/import_package_owl_onto/ontology/objects/`
> - 视图定义：`byclaw-data/resource/import_package_owl_onto/ontology/views/`
> - 数据源：`byclaw-data/resource/import_package_owl_onto/ontology/dbsources/dbsource.owl`

---

### 5.1 数据源

| 数据源别名 | 类型 | 连接串 |
|-----------|------|--------|
| `yizhuang_brain` | MySQL | `jdbc:mysql://127.0.0.1:3306/yizhuang_brain` |

```
用户名：root
密 码：111111
连接池：pool_min=5, pool_max=20
字符集：UTF-8，时区：Asia/Shanghai
```

---

### 5.2 本体对象（Object）

#### 5.2.1 ads_enterprise_analysis — 企业综合分析表

> 企业综合分析宽表，整合企业经营、税收、利润、网格活力、产业环节、企业等级、风险等级等信息，主键为 `enterprise_id`。

**物理表**：`yizhuang_brain.ads_enterprise_analysis`

| 字段编码 | 中文名（property_name） | 类型 | 角色 | role_rule | 备注 |
|---------|------------------------|------|------|-----------|------|
| `enterprise_id` | 企业唯一ID | STRING | MEASURE | primary_key | 主键，必填 |
| `enterprise_name` | 企业全称 | STRING | DIMENSION | name | 企业名称术语集 |
| `stat_date` | 统计日期 | STRING | DIMENSION | period | 账期字段 |
| `industry_name` | 行业类型 | STRING | DIMENSION | name | 行业术语集 |
| `business_status_name` | 企业经营状态 | STRING | DIMENSION | name | 如"正常""注销" |
| `lng_lat` | 企业经纬度(lng,lat) | STRING | DIMENSION | description | — |
| `height` | 地理高度(米) | DOUBLE | MEASURE | raw_number | — |
| `address` | 企业详细地址 | STRING | DIMENSION | description | — |
| `total_revenue` | 企业总营收（万元） | DOUBLE | MEASURE | basic_metric | 核心指标 |
| `total_tax` | 企业总缴税（万元） | DOUBLE | MEASURE | basic_metric | 核心指标 |
| `total_profit` | 企业总利润（万元） | DOUBLE | MEASURE | basic_metric | 核心指标 |
| `tax_rate` | 企业实际税负率（%） | DOUBLE | MEASURE | derived_metric | `CASE WHEN total_revenue>0 THEN total_tax/total_revenue*100 ELSE NULL END` |
| `avg_foot_traffic` | 企业所属物理网格人流量(人次) | DOUBLE | MEASURE | basic_metric | — |
| `foot_active_level_name` | 企业所属物理网格人流活跃等级 | STRING | DIMENSION | name | — |
| `avg_vehicle_flow` | 企业所属物理网格车流量(辆次) | DOUBLE | MEASURE | basic_metric | — |
| `vehicle_active_level_name` | 企业所属物理网格车流活跃等级 | STRING | DIMENSION | name | — |
| `avg_light_intensity` | 企业灯光强度 | DOUBLE | MEASURE | basic_metric | — |
| `light_active_level_name` | 企业灯光活跃等级 | STRING | DIMENSION | name | — |
| `chain_code` | 所属产业环节编码 | STRING | DIMENSION | id | — |
| `chain_name` | 所属产业环节名称 | STRING | DIMENSION | name | — |
| `manage_grid_id` | 所属管理网格编码 | STRING | DIMENSION | id | — |
| `manage_grid_name` | 所属管理网格名称 | STRING | DIMENSION | name | — |
| `enterprise_level_name` | 企业等级 | STRING | DIMENSION | name | 如"龙头企业""骨干企业" |
| `risk_level_name` | 企业综合风险等级 | STRING | DIMENSION | name | — |
| `phy_grid_id` | 企业所属物理网格编码 | STRING | DIMENSION | id | — |
| `phy_grid_name` | 企业所属物理网格名称 | STRING | DIMENSION | name | — |
| `energy_efficiency_Index` | 企业经济效益等级（高、中、低） | STRING | DIMENSION | name | **歧义触发字段**：枚举值为"高""中""低" |
| `revenur_rule` | 营收规则 | STRING | — | — | 辅助字段 |
| `data_source` | 数据来源 | STRING | — | — | — |
| `update_time` | 分析表更新时间 | DATETIME | — | — | — |
| `etl_create_time` | 分析表创建时间 | DATETIME | — | — | — |

**关联关系**：
- `ads_enterprise_analysis` → `ads_manage_grid_analysis`（join: `manage_grid_id`）
- `ads_enterprise_analysis` → `ads_grid_analysis`（join: `phy_grid_id`）
- `ads_enterprise_analysis` → `ads_chain_analysis`（join: `chain_code`）

---

#### 5.2.2 ads_grid_analysis — 物理网格综合分析表

> 物理网格综合分析宽表，聚合物理网格面积、经营指标、IoT 活跃度、灯光、POI 密度，并关联所属管理网格信息，主键为 `phy_grid_id`。

**物理表**：`yizhuang_brain.ads_grid_analysis`

| 字段编码 | 中文名 | 角色 | role_rule | 备注 |
|---------|--------|------|-----------|------|
| `phy_grid_id` | 物理网格编码 | MEASURE | primary_key | 主键，必填 |
| `phy_grid_name` | 物理网格名称 | DIMENSION | name | — |
| `grid_lnt_lat` | 物理网格中心点经纬度(lng,lat) | DIMENSION | description | — |
| `wkt` | 物理网格边界组(左下、右下、右上、左上) | DIMENSION | description | — |
| `stat_date` | 统计日期 | DIMENSION | period | 账期字段 |
| `area_sqm` | 物理网格面积（平方米） | MEASURE | raw_number | — |
| `manage_grid_id` | 管理网格编码 | DIMENSION | id | 关联管理网格 |
| `manage_grid_name` | 管理网格名称 | DIMENSION | name | — |
| `total_revenue` | 物理网格总营收（万元） | MEASURE | basic_metric | — |
| `total_tax` | 物理网格总缴税（万元） | MEASURE | basic_metric | — |
| `total_profit` | 物理网格总利润（万元） | MEASURE | basic_metric | — |
| `tax_rate` | 物理网格实际税负率（%） | MEASURE | derived_metric | `CASE WHEN total_revenue>0 THEN total_tax/total_revenue*100 ELSE NULL END` |
| `output_per_mu` | **物理网格亩产效益（万元/亩）** | MEASURE | formula_metric | `CASE WHEN area_sqm>0 THEN total_revenue/(area_sqm/10000) ELSE NULL END` — **复杂查询核心字段** |
| `avg_foot_traffic` | 物理网格日均人流量（人次） | MEASURE | basic_metric | — |
| `foot_active_level_name` | 物理网格人流活跃等级 | DIMENSION | name | — |
| `avg_vehicle_flow` | 物理网格日均车流量（辆次） | MEASURE | basic_metric | — |
| `vehicle_active_level_name` | 物理网格车流活跃等级 | DIMENSION | name | — |
| `avg_light_intensity` | 灯光强度 | MEASURE | basic_metric | — |
| `light_active_level_name` | 灯光活跃等级 | DIMENSION | name | — |
| `poi_density` | POI 密度 | MEASURE | basic_metric | — |
| `update_time` | 更新时间 | DATETIME | — | — |
| `etl_create_time` | 创建时间 | DATETIME | — | — |

**关联关系**：`ads_grid_analysis` → `ads_manage_grid_analysis`（join: `manage_grid_id`）

---

#### 5.2.3 ads_manage_grid_analysis — 管理网格综合分析表

> 管理网格综合分析宽表，聚合管理网格面积、经营指标、IoT 活跃度、灯光、POI 密度等信息，主键为 `manage_grid_id`。

**物理表**：`yizhuang_brain.ads_manage_grid_analysis`

| 字段编码 | 中文名 | 角色 | role_rule | 备注 |
|---------|--------|------|-----------|------|
| `manage_grid_id` | 管理网格编码 | MEASURE | primary_key | 主键，必填 |
| `manage_grid_name` | 管理网格名称 | DIMENSION | name | — |
| `grid_lnt_lat` | 管理网格中心经纬度 | DIMENSION | description | — |
| `manage_wkt` | 管理网格边界组 | DIMENSION | description | — |
| `stat_date` | 统计日期 | DIMENSION | period | — |
| `area_sqm` | 管理网格面积（平方米） | MEASURE | raw_number | — |
| `total_revenue` | 管理网格总营收（万元） | MEASURE | basic_metric | — |
| `total_tax` | 管理网格总缴税（万元） | MEASURE | basic_metric | — |
| `total_profit` | 管理网格总利润（万元） | MEASURE | basic_metric | — |
| `tax_rate` | 实际税负率（%） | MEASURE | derived_metric | 同 ads_grid_analysis 公式 |
| `output_per_mu` | 亩产效益（万元/亩） | MEASURE | formula_metric | 同 ads_grid_analysis 公式 |
| `avg_foot_traffic` | 日均人流量（人次） | MEASURE | basic_metric | — |
| `foot_active_level_name` | 人流活跃等级 | DIMENSION | name | — |
| `avg_vehicle_flow` | 日均车流量（辆次） | MEASURE | basic_metric | — |
| `vehicle_active_level_name` | 车流活跃等级 | DIMENSION | name | — |
| `avg_light_intensity` | 灯光强度 | MEASURE | basic_metric | — |
| `light_active_level_name` | 灯光活跃等级 | DIMENSION | name | — |
| `com_num` | 企业数量 | MEASURE | basic_metric | — |
| `grid_num` | 物理网格数量 | MEASURE | basic_metric | — |
| `poi_density` | POI 密度 | MEASURE | basic_metric | — |
| `energy_efficiency_Index` | 经济效益等级（高、中、低） | DIMENSION | name | — |
| `update_time` | 更新时间 | DATETIME | — | — |
| `etl_create_time` | 创建时间 | DATETIME | — | — |

---

#### 5.2.4 ads_chain_analysis — 产业链分析表

> 记录产业环节信息、环节企业数量、营收、税收、利润等指标，用于分析产业链效益。主键为 `city_name + chain_item_code`。

**物理表**：`yizhuang_brain.ads_chain_analysis`

| 字段编码 | 中文名 | 角色 | role_rule | 备注 |
|---------|--------|------|-----------|------|
| `city_name` | 城市名称 | MEASURE | primary_key | 联合主键一 |
| `chain_item_code` | 产业链环节编码 | MEASURE | primary_key | 联合主键二 |
| `chain_type_code` | 产业大类编码 | DIMENSION | id | — |
| `chain_type_name` | 产业大类名称 | DIMENSION | name | — |
| `chain_code` | 产业链编码 | DIMENSION | id | — |
| `chain_name` | 产业链名称 | DIMENSION | name | — |
| `item_type` | 产业链环节类型 | DIMENSION | name | — |
| `chain_item_name` | 产业链环节名称 | DIMENSION | name | — |
| `ENTERPRISE_NUM` | 产业链环节企业数量 | MEASURE | basic_metric | — |
| `total_revenue` | 产业链环节总营收（万元） | MEASURE | basic_metric | — |
| `total_tax` | 产业链环节总缴税（万元） | MEASURE | basic_metric | — |
| `total_profit` | 产业链环节总利润（万元） | MEASURE | basic_metric | — |
| `item_strengths` | 产业链环节优势描述 | STRING | — | 文本字段 |
| `item_weaknesses` | 产业链环节劣势描述 | STRING | — | 文本字段 |
| `industrial_chain_efficiency` | 产业链效益指数 | MEASURE | basic_metric | — |
| `update_time` | 更新时间 | DATETIME | — | — |
| `etl_create_time` | 创建时间 | DATETIME | — | — |

---

### 5.3 本体视图（View）

#### 5.3.1 scene_enterprise_analysis — 企业综合分析视图

> 以企业为 anchor，整合经营、税收、风险、网格活力、产业链等指标的联合分析视图。

```
view_code:  scene_enterprise_analysis
view_name:  企业综合分析视图
datasource: yizhuang_brain（全部对象同源）

包含对象：
  ads_enterprise_analysis   ← 主表（anchor）
  ads_grid_analysis         ← 物理网格
  ads_manage_grid_analysis  ← 管理网格
  ads_chain_analysis        ← 产业链

关联关系（join 路径）：
  ads_enterprise_analysis  →  ads_grid_analysis          (phy_grid_id)
  ads_enterprise_analysis  →  ads_manage_grid_analysis   (manage_grid_id)
  ads_enterprise_analysis  →  ads_chain_analysis         (chain_code)
  ads_grid_analysis        →  ads_manage_grid_analysis   (manage_grid_id)
```

**使用场景**：企业 + 网格 + 产业链跨对象联合查询，`complex_conditions` 非空时由 PlanAgent 处理。

---

#### 5.3.2 scene_grid_analysis — 物理网格综合分析视图

> 以物理网格为 anchor，聚合网格经营指标、IoT 活力、亩产效益，关联所属管理网格。

```
view_code:  scene_grid_analysis
view_name:  物理网格综合分析视图
datasource: yizhuang_brain

包含对象：
  ads_grid_analysis         ← 主表（anchor）
  ads_manage_grid_analysis  ← 管理网格

关联关系：
  ads_grid_analysis → ads_manage_grid_analysis (manage_grid_id)
```

**使用场景**：网格亩产效益分析、管理网格对比。`output_per_mu` 为 `formula_metric`，PERCENT_RANK 截断须走 PlanAgent。

---

### 5.4 字段术语集（_get_field_catalog 期望返回值）

> `_get_field_catalog("query_ads_enterprise_analysis", ctx)` 的期望输出，供通道 1 术语映射检查基准对照。

```python
# flat dict: term → field_code
# 来源：OntologyField.field_code（自映射） + OntologyField.field_name（中文名映射）
{
    # ── field_code 自映射 ──
    "enterprise_id": "enterprise_id",
    "enterprise_name": "enterprise_name",
    "stat_date": "stat_date",
    "industry_name": "industry_name",
    "business_status_name": "business_status_name",
    "total_revenue": "total_revenue",
    "total_tax": "total_tax",
    "total_profit": "total_profit",
    "tax_rate": "tax_rate",
    "avg_foot_traffic": "avg_foot_traffic",
    "foot_active_level_name": "foot_active_level_name",
    "avg_vehicle_flow": "avg_vehicle_flow",
    "vehicle_active_level_name": "vehicle_active_level_name",
    "avg_light_intensity": "avg_light_intensity",
    "light_active_level_name": "light_active_level_name",
    "chain_code": "chain_code",
    "chain_name": "chain_name",
    "manage_grid_id": "manage_grid_id",
    "manage_grid_name": "manage_grid_name",
    "enterprise_level_name": "enterprise_level_name",
    "risk_level_name": "risk_level_name",
    "phy_grid_id": "phy_grid_id",
    "phy_grid_name": "phy_grid_name",
    "energy_efficiency_Index": "energy_efficiency_Index",
    # ── 中文名（property_name）映射 ──
    "企业唯一ID": "enterprise_id",
    "企业全称": "enterprise_name",
    "统计日期": "stat_date",
    "行业类型": "industry_name",
    "企业经营状态": "business_status_name",
    "企业总营收（万元）": "total_revenue",
    "企业总缴税（万元）": "total_tax",
    "企业总利润（万元）": "total_profit",
    "企业实际税负率（%）": "tax_rate",
    "企业所属物理网格人流量(人次)": "avg_foot_traffic",
    "企业所属物理网格人流活跃等级": "foot_active_level_name",
    "企业所属物理网格车流量(辆次)": "avg_vehicle_flow",
    "企业所属物理网格车流活跃等级": "vehicle_active_level_name",
    "企业灯光强度": "avg_light_intensity",
    "企业灯光活跃等级": "light_active_level_name",
    "所属产业环节编码": "chain_code",
    "所属产业环节名称": "chain_name",
    "所属管理网格编码": "manage_grid_id",
    "所属管理网格名称": "manage_grid_name",
    "企业等级": "enterprise_level_name",
    "企业综合风险等级": "risk_level_name",
    "企业所属物理网格编码": "phy_grid_id",
    "企业所属物理网格名称": "phy_grid_name",
    "企业经济效益等级（高、中、低）": "energy_efficiency_Index",
}
```

```python
# ads_grid_analysis 术语集（关键字段摘要）
{
    "output_per_mu": "output_per_mu",
    "物理网格亩产效益（万元/亩）": "output_per_mu",   # IT-02 直接命中
    "total_revenue": "total_revenue",
    "物理网格总营收（万元）": "total_revenue",
    "area_sqm": "area_sqm",
    "物理网格面积（平方米）": "area_sqm",
    "manage_grid_name": "manage_grid_name",
    "管理网格名称": "manage_grid_name",
    "foot_active_level_name": "foot_active_level_name",
    "物理网格人流活跃等级": "foot_active_level_name",
    # ... 其余字段同结构
}
```

---

### 5.5 测试问题 → 术语映射速查

> 对照各集成用例（IT-01 ～ IT-08.2），列出用户问题中关键术语的通道 1 判断结果，辅助验收人员快速核对。

| 用例 | 用户术语 | 通道 1 映射结果 | 触发规则 |
|------|---------|--------------|---------|
| IT-01 | `enterprise_level_name` | → `enterprise_level_name` ✓ | field_code 直接命中 |
| IT-01 | `龙头企业` | → 字面值（DIMENSION name 型，Agent 直填文本，通道1 不检查枚举值）✓ | 不触发 |
| IT-01 | `stat_date` | → `stat_date` ✓ | field_code 直接命中 |
| IT-02 | `output_per_mu` | → `output_per_mu` ✓ | field_code 直接命中 |
| IT-02 | `manage_grid_name` | → `manage_grid_name` ✓ | field_code 直接命中 |
| IT-02 | `亩产效益后30%` | → complex_conditions（overflow） | 不参与通道 1 |
| IT-03.x | `营收` | → **无命中**（catalog 中无该词条） | A3_not_found → 进入通道 2 |
| IT-03.x | `经济效益等级` | → **无命中**（catalog 词条为"企业经济效益等级（高、中、低）"） | A3_not_found → 进入通道 2 |
| IT-03.x | `高效` | → **无命中**（枚举值无 code 映射） | A2_not_found → 进入通道 2 |
| IT-05 | `industry_name` | → `industry_name` ✓ | field_code 直接命中 |
| IT-05 | `total_revenue` | → `total_revenue` ✓ | field_code 直接命中 |
| IT-07.x | `利润` | → **无命中**（catalog 词条为"企业总利润（万元）"） | A3_not_found → 进入通道 2 |
| IT-08.x | `企业级别` | → **无命中**（catalog 词条为"企业等级"） | A3_not_found → 进入通道 2 |
| IT-08.x | `优质` | → **无命中**（枚举值无 code 映射） | A2_not_found → 进入通道 2 |

---

### 5.6 期望 SQL 模板

> 以下 SQL 为验收时对比 PlanAgent / QueryExecutor / ComputeExecutor 生成结果的参考基准。

#### IT-01：SIMPLE query，QueryExecutor 直接生成

```sql
-- 查询 2026 年 Q1 龙头企业列表（按总营收降序）
SELECT enterprise_name, total_revenue, enterprise_level_name, manage_grid_name
FROM ads_enterprise_analysis
WHERE stat_date BETWEEN '2026-01-01' AND '2026-03-31'
  AND enterprise_level_name = '龙头企业'
ORDER BY total_revenue DESC
LIMIT 20;
```

#### IT-02：COMPLEX query，data_query，PlanAgent 生成（参考模板）

```sql
-- 各管理网格中亩产效益后30%的物理网格列表
WITH ranked AS (
    SELECT
        phy_grid_id,
        phy_grid_name,
        manage_grid_name,
        output_per_mu,
        PERCENT_RANK() OVER (PARTITION BY manage_grid_id ORDER BY output_per_mu DESC) AS pct_rank
    FROM ads_grid_analysis
    WHERE stat_date = '2026-03'
)
SELECT phy_grid_id, phy_grid_name, manage_grid_name, output_per_mu
FROM ranked
WHERE pct_rank >= 0.70   -- 降序排列，后30% 对应 PERCENT_RANK >= 0.70
ORDER BY manage_grid_name, output_per_mu;
```

#### IT-05：SIMPLE compute，ComputeExecutor 生成

```sql
-- 按行业统计企业数和总营收
SELECT
    industry_name,
    COUNT(*)            AS enterprise_count,
    SUM(total_revenue)  AS total_revenue_sum
FROM ads_enterprise_analysis
WHERE stat_date = '2026-03'
GROUP BY industry_name
ORDER BY total_revenue_sum DESC
LIMIT 50;
```

#### IT-06：COMPLEX compute，data_query，PlanAgent 生成（参考模板）

```sql
-- 各管理网格中，亩产效益后30%的物理网格上的企业平均营收
WITH grid_ranked AS (
    SELECT
        phy_grid_id,
        manage_grid_name,
        PERCENT_RANK() OVER (ORDER BY output_per_mu DESC) AS pct_rank
    FROM ads_grid_analysis
    WHERE stat_date = '2026-03'
),
low_grids AS (
    SELECT phy_grid_id, manage_grid_name
    FROM grid_ranked
    WHERE pct_rank >= 0.70
)
SELECT
    lg.manage_grid_name,
    AVG(e.total_revenue) AS avg_revenue
FROM ads_enterprise_analysis e
JOIN low_grids lg ON e.phy_grid_id = lg.phy_grid_id
WHERE e.stat_date = '2026-03'
GROUP BY lg.manage_grid_name
ORDER BY avg_revenue DESC;
```

#### IT-03.2 / IT-04.2（手动消歧后，"营收" → total_revenue）

```sql
-- 消歧后：用户选择"总营收" → total_revenue；OQL 路径示例
SELECT enterprise_name, total_revenue, enterprise_level_name
FROM ads_enterprise_analysis
WHERE stat_date = '2026-03'
  AND enterprise_level_name IN ('龙头企业', '骨干企业')
ORDER BY total_revenue DESC
LIMIT 50;
```

#### IT-07.2 / IT-08.2（手动消歧后，统计类）

```sql
-- 消歧后：用户选择"利润" → total_profit；按行业统计
SELECT
    industry_name,
    SUM(total_profit) AS total_profit_sum,
    AVG(tax_rate)     AS avg_tax_rate
FROM ads_enterprise_analysis
WHERE stat_date = '2026-03'
GROUP BY industry_name
ORDER BY total_profit_sum DESC;
```

