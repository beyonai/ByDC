# datacloud-analysis 测试用例梳理（2026-04-21）

## 0. 基线与证据

- 扫描范围：`packages/datacloud-analysis/tests/dca`
- 测试文件数：`67`
- 命令与结果：
  - `PYTHONPATH=src uv run python -m pytest -q`
    - 结果：`2 errors during collection`
    - 错误文件：
      - `tests/dca/unit/test_worker_resume_regressions.py`
      - `tests/dca/unit/test_worker_skill_capabilities.py`
    - 根因：`ModuleNotFoundError: dill`
  - `PYTHONPATH=src uv run python -m pytest -q --ignore=tests/dca/unit/test_worker_resume_regressions.py --ignore=tests/dca/unit/test_worker_skill_capabilities.py`
    - 结果：`46 failed, 332 passed, 40 skipped`

> 结论口径：本次仅做“梳理与分批建议”，不在本文内直接改代码。

---

## 批次1：清理过时/冗余用例（先减负）

### 删除

1. `tests/dca/unit/test_apply_resume_to_params.py`
   - 全文件 `15` 条用例均为 `skip`
   - skip 原因明确：`_apply_resume_to_params removed from plugin`
   - 判定：历史私有函数验证，已失效，建议整文件删除
2. `tests/dca/unit/test_query_clarification_plugin.py`
   - 全文件 `11` 条用例均为 `skip`
   - skip 原因明确：`_format_knowledge_for_prompt removed from plugin`
   - 判定：旧实现专用测试，建议整文件删除
3. `tests/dca/unit/test_query_clarification_plugin_v4.py`
   - 全文件 `3` 条用例均为 `skip`
   - skip 原因明确：`V0.1->V0.3` 缓存/interrupt 方案已废弃
   - 判定：纯历史方案回归，建议整文件删除
4. `tests/dca/unit/test_interrupt_resume_integration.py`
   - 全文件 `9` 条用例均为 `skip`
   - skip 原因明确：`before_call_back` 不再走 `interrupt()`，改为 `ClarificationNeededError`
   - 判定：旧中断模型验收，建议整文件删除（由新流程用例替代）

### 修改

1. 清理上述文件后，更新测试文档引用与注释中的 V0.1/V0.2 历史描述，避免后续误导。

### 新增

1. 新建“当前澄清流程”替代用例文件（建议名）：`tests/dca/unit/test_query_clarification_current_flow.py`
   - 覆盖 `ClarificationNeededError` 新流程
   - 覆盖 resume 后参数回填
   - 覆盖 redirect 到 `data_query_*` 行为

---

## 批次2：保留但跑不通的用例整改（必须红转绿）

### 删除

1. `tests/dca/unit/test_query_clarification_plugin_v2.py` 中直接 patch 私有符号 `_call_query_clarification` 的断言路径不建议保留原样（属于实现耦合）；建议删除这部分断言并改写为“公共行为断言”。
2. `tests/dca/unit/test_query_clarification_plugin_v3.py` 中直接导入 `_apply_resume_to_tool_params` 的断言路径（符号已不存在）建议删除并改写。

### 修改

1. 依赖与收集层（2 个 collection error）
   - 文件：
     - `tests/dca/unit/test_worker_resume_regressions.py`
     - `tests/dca/unit/test_worker_skill_capabilities.py`
   - 处理建议：
     - 方案A：补齐测试依赖（`dill`）到测试环境
     - 方案B：在测试内隔离 `by_framework` 重依赖（monkeypatch/stub），避免收集期硬失败
   - 判定：应保留，需整改

2. Clarification 分析返回契约漂移（5 fail）
   - 文件：
     - `tests/dca/unit/test_analyze_clarify_node.py`（4）
     - `tests/dca/unit/test_clarification_plugin_v03.py`（1）
   - 根因：测试桩返回 2 元组，现实现按 3 元组解包
   - 判定：应保留，修改测试桩或对齐实现契约

3. execution/react_loop 调用签名漂移（6 fail）
   - 文件：
     - `tests/dca/unit/test_execution_node_knowledge.py`（4）
     - `tests/dca/unit/test_knowledge_pipeline_integration.py`（2，`stable_system_prompt`）
   - 根因：mock 函数未兼容新增 kwargs（`loader` / `stable_system_prompt`）
   - 判定：应保留，修改 mock 签名

4. LLM fallback 调用签名漂移（4 fail）
   - 文件：`tests/dca/unit/test_llm_integration.py`
   - 根因：`_invoke_llm_with_fallback` 新增必填 kw-only 参数 `thinking_message_id`
   - 判定：应保留，更新调用与断言

5. 思维流事件发射契约变化（3 fail）
   - 文件：`tests/dca/unit/test_react_loop_thinking_helpers.py`
   - 根因：`emit_chunk` 调用位置/参数变化，旧断言不成立
   - 判定：应保留，改为校验当前事件契约（含 `event_type/message_id`）

6. 澄清插件用例与私有实现耦合（11 fail）
   - 文件：
     - `tests/dca/unit/test_query_clarification_plugin_v2.py`（7）
     - `tests/dca/unit/test_query_clarification_plugin_v3.py`（3）
     - `tests/dca/unit/test_knowledge_pipeline_integration.py`（1 组相关）
   - 根因：
     - patch 了不存在的私有符号 `_call_query_clarification`
     - 导入了已移除的 `_apply_resume_to_tool_params`
     - 对 `intent_reason/extraction_confidence` 等字段处理预期与现实现不一致
   - 判定：应保留“业务行为”，但要去私有化重写

7. field_name_cn 协议/Schema/Prompt 规范冲突（13 fail）
   - 文件：
     - `tests/dca/unit/test_field_name_cn_prompt_prohibition.py`（3）
     - `tests/dca/unit/test_field_name_cn_schema.py`（2）
     - `tests/dca/unit/test_field_residuals.py`（5）
     - `tests/dca/unit/test_metrics_field_name_cn_required.py`（2）
     - `tests/dca/unit/test_query_fields_schema.py`（1）
   - 根因：测试期望与当前工具 schema/prompt 仍使用 `field` 的事实冲突
   - 判定：应保留，但需要先做“协议定版”（`field` vs `field_name_cn`），再统一改测试或改实现

8. 文件工具用例自身缺陷（1 fail）
   - 文件：`tests/dca/unit/test_new_tools.py::TestFileIOTools::test_write_file_creates_file`
   - 根因：临时目录作用域结束后再读文件，导致 `FileNotFoundError`
   - 判定：应保留，修复测试写法

### 新增

1. 新增“调用签名守护”测试：
   - `execution_node -> run_react_loop` 关键 kwargs（`loader`、`stable_system_prompt`）必须透传
2. 新增“LLM fallback 参数契约”测试：
   - `thinking_message_id` 缺失时应明确报错，存在时主备模型路径可运行
3. 新增“公共接口行为测试”替代私有函数 patch：
   - 只断言 `before_call_back` 输入输出决策，不断言私有函数是否被调用

---

## 批次3：继续新增测试用例（覆盖盲区）

### 删除

1. 无（本批次以补覆盖为主）。

### 修改

1. 将 `field_name_cn` 相关旧用例整理成单一规范套件，避免同一规则在 5 个文件重复维护。
2. 将 plugin v2/v3 重叠场景参数化，减少重复样板代码。

### 新增

1. 新增“规范定版后”的 Schema 快照测试（query/compute 各一组）：
   - 锁定 required 字段、description、examples 键
2. 新增“澄清链路端到端”测试：
   - ambiguous -> ClarificationNeededError -> resume -> params 回填 -> dispatch 成功
3. 新增“可选依赖隔离”测试：
   - 当 `by_framework` 可选依赖缺失时，测试应被显式 skip 或可降级执行，而非 collection 失败
4. 新增“提示词残留词”守卫测试：
   - 防止旧字段术语（如历史文案）在 prompt/tool description 中回归

---

## 汇总结论（对应提问）

1. 冗余/过时、建议删除：
   - `test_apply_resume_to_params.py`
   - `test_query_clarification_plugin.py`
   - `test_query_clarification_plugin_v4.py`
   - `test_interrupt_resume_integration.py`
   - 以及 v2/v3 中“直连私有实现”的个别断言路径（删除并改写）

2. 应保留但跑不通、需要整改：
   - `2` 个 collection error（`dill` 依赖）
   - `46` 个失败用例（按上文 8 类修改项分批修复）

3. 需要继续新增：
   - 当前澄清流程替代用例
   - 调用签名/参数契约守护用例
   - Schema/Prompt 定版快照用例
   - 可选依赖隔离与端到端澄清链路用例
