# datacloud-analysis 代码冗余清理清单（2026-04-21）

## 1. 扫描范围
- `packages/datacloud-analysis/src/datacloud_analysis`
- `packages/datacloud-analysis/tests/dca`
- `packages/datacloud-analysis` 包目录下的非源码文件

## 2. 判定标准
- 项目内无任何引用（源码+测试全局检索）
- 整文件或整组用例长期 `skip`，且 skip 原因明确指向已废弃版本路径
- 明确标注 `deprecated/legacy` 且已被新实现替代
- 缓存/临时产物（不应进入包目录）

## 3. 可立即清理（高置信）

### 3.1 非源码冗余产物

| 路径 | 现状 | 建议 |
|---|---|---|
| `packages/datacloud-analysis/.mypy_cache/` | 缓存目录，约 `83.16 MB`（2815 文件） | 直接删除；确保不提交 |
| `packages/datacloud-analysis/.pytest_cache/` | 缓存目录，约 `0.05 MB` | 直接删除 |
| `packages/datacloud-analysis/.ruff_cache/` | 缓存目录，约 `0.01 MB` | 直接删除 |
| `packages/datacloud-analysis/**/__pycache__/` | 字节码缓存，约 `2.38 MB`（212 文件） | 直接删除 |
| `packages/datacloud-analysis/.tmp_pytest.xml` | 临时测试输出（未跟踪） | 直接删除 |
| `packages/datacloud-analysis/.tmp_pytest_output.txt` | 临时测试输出（未跟踪） | 直接删除 |
| `packages/datacloud-analysis/by-framework.log` | 本地运行日志 | 直接删除或迁移到统一 `logs/` |

### 3.2 高置信“孤儿代码”

1. `src/datacloud_analysis/demo.py`
- 证据：存在占位实现（`...`、`ClarificationResult(...)`、`ConfirmedQuery(...)`）
  - `src/datacloud_analysis/demo.py:14`
  - `src/datacloud_analysis/demo.py:15`
  - `src/datacloud_analysis/demo.py:16`
  - `src/datacloud_analysis/demo.py:19`
  - `src/datacloud_analysis/demo.py:24`
- 证据：项目 `.gitignore` 已显式忽略该文件路径
  - `.gitignore:191`
- 结论：历史演示/占位残留，建议删除。

2. `src/datacloud_analysis/workspace/mount.py`
- 证据：仅定义 `SandboxMounter`，仓库内无任何引用
  - `src/datacloud_analysis/workspace/mount.py:32`
- 结论：未落地的旧设计残留，建议删除该模块或并入实际调用路径。

3. `src/datacloud_analysis/tools/data_ask_user.py`
- 证据：`DataAskUserContext/complex_ask_user` 仅在本文件内出现，无内部调用
  - `src/datacloud_analysis/tools/data_ask_user.py:12`
  - `src/datacloud_analysis/tools/data_ask_user.py:13`
- 结论：历史实验代码概率高，建议删除；若需保留，请补充真实接入点和测试。

4. `src/datacloud_analysis/config/models.py`
- 证据：`LLMRole/TaskContext/TaskResult` 未被项目代码引用
  - `src/datacloud_analysis/config/models.py:15`
  - `src/datacloud_analysis/config/models.py:24`
  - `src/datacloud_analysis/config/models.py:36`
- 证据：域内已有另一套 `TaskResult` 契约（重复语义来源）
  - `src/datacloud_analysis/orchestration/shared/contracts.py:166`
- 结论：建议删除该文件，或先迁移为单一契约来源后再删除重复定义。

## 4. 测试冗余（建议清理）

### 4.1 长期跳过、指向旧版本路径的用例

1. `tests/dca/unit/test_apply_resume_to_params.py`
- `pytestmark` 整文件跳过，原因明确是 V0.2 -> V0.3 已移除旧函数
  - `tests/dca/unit/test_apply_resume_to_params.py:19`

2. `tests/dca/unit/test_query_clarification_plugin.py`
- `pytestmark` 整文件跳过，原因明确是 V0.2 -> V0.3 已移除旧实现
  - `tests/dca/unit/test_query_clarification_plugin.py:15`

3. `tests/dca/unit/test_interrupt_resume_integration.py`
- 9 个测试均 `@pytest.mark.skip`，skip reason 均为 V0.2 -> V0.3 行为迁移
  - 典型位置：`tests/dca/unit/test_interrupt_resume_integration.py:199-200`

4. `tests/dca/unit/test_query_clarification_plugin_v4.py`
- 3 个测试均 `@pytest.mark.skip`，skip reason 均为 V0.1 -> V0.3 机制替换
  - 典型位置：`tests/dca/unit/test_query_clarification_plugin_v4.py:91-92`

结论：以上文件已失去回归价值，建议删除或迁移到 `tests/dca/archive/`。

### 4.2 可合并的重复测试样板

以下 4 个文件重复定义了几乎一致的 `_FakeField`（可抽共用 fixture/helper）：
- `tests/dca/unit/test_field_name_cn_schema.py:21`
- `tests/dca/unit/test_query_fields_schema.py:11`
- `tests/dca/unit/test_tool_description_field_residuals.py:19`
- `tests/dca/unit/test_ontology_tool_loader_agent_friendly.py:28`

结论：建议抽到 `tests/dca/unit/_fixtures/fake_field.py`（或 `conftest.py`）减少维护成本。

## 5. 兼容层冗余（需确认外部依赖后再删）

1. `src/datacloud_analysis/command_plugins/get_file_by_page_command.py` 中兼容别名
- `handle_ext_command` 明确标注为兼容旧导入
  - `src/datacloud_analysis/command_plugins/get_file_by_page_command.py:41`
  - `src/datacloud_analysis/command_plugins/get_file_by_page_command.py:47`
- 建议：确认下游无旧导入后移除。

2. `src/datacloud_analysis/tools/sandbox.py`
- 模块显式 `deprecated` 且注明“下个 major 删除”
  - `src/datacloud_analysis/tools/sandbox.py:3`
  - `src/datacloud_analysis/tools/sandbox.py:35`
- 当前仍有兼容性测试覆盖；建议按版本窗口移除。

## 6. 建议清理顺序（最小风险）

1. 先清理缓存/临时文件（3.1）。
2. 删除高置信孤儿模块（`demo.py`、`workspace/mount.py`、`tools/data_ask_user.py`）。
3. 处理 `config/models.py` 与 `contracts.py` 的重复契约，收敛后删冗余文件。
4. 删除长期 skip 的历史测试文件，保留仍有执行价值的 V3 现行测试。
5. 最后处理兼容层（`handle_ext_command` 别名、`tools/sandbox.py`），需先验证外部依赖。
